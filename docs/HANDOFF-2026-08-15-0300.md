# Handoff — LecturePack 2.0.2 release candidate

**Date:** 2026-08-15 03:00 ET
**Written by:** Claude (Opus 5), documenting session 2026-08-15
**Supersedes:** `docs/HANDOFF_v2.0.1_RELEASE_HARDENING.md` and the Antigravity
release-polish handoff. The 2.0.1 doc still holds for the v2.0.0 public audit
(asset SHAs), the five BUG-28…32 root causes, and the Inno Setup / release-venv
environment traps — none of that is re-explained here.

---

## 1. What this project is

LecturePack turns a recorded lecture into a study pack: transcript, slides,
grounded concepts, flashcards, quiz and Teach Me. It ships as a Windows Electron
app (`LecturePack.exe`) with a PyInstaller Python sidecar (`LecturePackSidecar.exe`)
that carries the engine, and an optional Cloudflare Worker AI gateway. Shipped
public release is **2.0.0**; **2.0.1** was a hardening pass tagged but not
published. This session produced a **2.0.2 candidate**, built and acceptance-green,
not yet tagged or published.

## 2. Current state

- **Branch: `sol/release-polish`** in worktree
  `C:\Users\marsh\Documents\LecturePack-worktrees\release-polish`. Working tree clean.
- **HEAD:** `88d7125` *fix(gateway): stop leaking the admin key and make the compare
  constant-time*
- **This is the single release line.** See §4 — the "two divergent lineages"
  worry from the previous handoff was wrong.
- Built candidate at `electron-spike/dist/LecturePack-win32-x64`, reporting 2.0.2.
- **Not tagged, not pushed, not signed.**

## 3. What this session did

### Established one release line (the confusion, resolved)

The inherited handoff implied `sol/release-polish` and the v2.0.1 hardening branch
had diverged, and that releasing from release-polish would re-ship the BUG-32
updater vulnerability. **That was wrong.** `git merge-base --is-ancestor v2.0.1
0646a14` exits 0 and `git log 0646a14..v2.0.1` is empty: release-polish already
contained all of v2.0.1. Gemini's feature work was built on the hardened base the
whole time. Only one commit was missing (`8404ddc`, payload-gated test skips),
cherry-picked here with one conflict in `tests/test_renderer_spike.py` resolved by
keeping both sides.

### Committed the inherited feature work — `cdcf1df`

Group study, subjects UI, sidecar/bridge IPC, Gemini gateway route, admin
dashboard, M1/M2/UI adversarial suites. `PROJECT.md` deleted and `.agents/`
gitignored (agent scratch, not product). Staged diff scanned for secrets before
commit — clean, the `wrangler.toml` change is model names only.

### Fixed the 6 M3 test failures — `cb66a12` (BUG-42)

All six were harness bugs. **Three of the four root causes in the inherited
handoff were wrong** — its whole `_job_objects` / `state.json` / Job-loader theory
was a dead end, and the packaged binary was never stale or broken.

| Real cause | Fix |
|---|---|
| Fixture wrote `study-content.json`; `load_content()` reads `study_v2.CONTENT_FILENAME` = `study-content-v2.json`. Every group came back empty, so 4 tests asserted against `no_ready_lectures` instead of their own docstrings | use the constant |
| "Unready" fixture used status `"pending"`, not a real status. `load_content()` normalises unknown statuses and, seeing concepts, promotes to `STUDY_READY` — the unready lecture was ready | use `STUDY_PREPARING` |
| Test asserted `manifest["group"] == ""` on clear; `set_job_group()` pops the key so the job reverts to its derived default | assert absence |
| Packaged test sent `{"id": …}`; sidecar reads `request_id`. No id → `_respond()` returns early emitting nothing, so a healthy `.exe` looked dead | use `request_id`; deadline-based reader thread instead of a 10-line budget |

### Bumped to 2.0.2 — `4095f1d`

Three declaration sites: `app/desktop/version.py`, `electron-spike/package.json`,
`app/packaging/lecturepack.iss`. Plus CHANGELOG 2.0.2 and BUG-42 in `BUG_LIST.md`.

### Hardened the new admin dashboard — `88d7125`

Two findings from the pre-release review; the design was otherwise sound (static
HTML, all data endpoints gated, fails closed 503 when `ADMIN_API_KEY` unset).
Admin key no longer accepted from the query string (only the fragment, which never
reaches the server, and it is stripped from the address bar after read);
`checkAdminAuth` now compares constant-time.

## 4. Architecture & decisions

- **`sol/release-polish` is the release line.** Verified by ancestry, not by
  reading docs. Any future "have these diverged?" question should be answered with
  `git merge-base --is-ancestor` and `git log A..B`, which cost nothing and settle it.
- **`package-win.mjs` does not invoke Inno Setup.** So the 260-char path trap
  documented in the 2.0.1 handoff does **not** apply to building the unpacked
  candidate — only to producing the installer. That is why this build succeeded
  from a worktree without staging to `C:\LPB\cand`.
- **The gateway ships nothing into the app.** It is a Cloudflare Worker, so the
  security fixes in `88d7125` did not invalidate the already-acceptance-tested
  packaged candidate. Worth remembering: gateway changes never require a rebuild.
- **New gotcha (BUG-42's lesson).** A fixture that hand-rolls a filename or status
  string the product parses is a test that silently stops testing. Reference the
  product's own constant so a rename breaks it loudly. And a fixture that stubs the
  transport (`_respond`) cannot prove the wire protocol — only the packaged
  end-to-end test can, which is why its failure was the one telling the truth.

## 5. Verified

Everything below run on this machine, this session, against the release `.venv`.

| Gate | Result |
|---|---|
| Python suite (`.venv`, `pytest tests/`) | **1770 passed, 0 failed, 4 skipped** |
| `ai-gateway` `npm test` (after security fixes) | **24 passed, 0 failed** |
| M3 adversarial, incl. 2 tests driving the real `.exe` | **8 passed** |
| Version-consistency gates (5 files, 101 tests) | **101 passed** |
| Packaged `LecturePack.exe` version resource | **FileVersion/ProductVersion = 2.0.2** (BUG-31 holds) |
| Packaged sidecar `--self-test` | **12/12** |
| **`scripts/stable_release_acceptance.py`** | **PASS — 43/43 checks, 0 failures, 26 screenshots, `human_manual_acceptance_required: false`** |

Acceptance evidence: `C:\LecturePackScratch\results\v2.0.2-acceptance\stable-release-acceptance.json`.
Checks include `live_background_progress_eta` (the ETA trap the long fixture
exists for), `final_zero_orphans`, `thirteen_file_export`, tray/background
long workload, cancel/retry, and interrupted-job recovery.

Long fixture built with `scripts/make_acceptance_fixtures.py` → 336 MB, 126 loops
of the bundled 10s demo, ~21 min. The shipped package still contains exactly one
video.

## 6. NOT verified

Be blunt about these — none were exercised this session:

- **No installer was produced.** `package-win.mjs` builds the unpacked candidate
  only. `LecturePack-2.0.2-Setup.exe` has not been built, so the Inno Setup path
  (and its 260-char trap, needing `C:\LPB\cand`) is **untested for 2.0.2**.
- **No A→B update test** (2.0.1 → 2.0.2). Not run this session.
- **No clean-machine acceptance** for 2.0.2.
- **The gateway security fixes are not deployed and not tested against a live
  Worker** — unit tests only. The fragment-stripping behaviour was not exercised
  in a real browser.
- **No live URL-import probe** for 2.0.2.
- **Group study was never exercised against a real AI gateway.** All group tests
  use a mocked client. The reduce path has not made one real `group_analysis`
  request end-to-end.

## 7. Known issues / residual risks

- **Authenticode signing: still unavailable.** No credentials in the repo. Windows
  will show SmartScreen. Unchanged from 2.0.1.
- **No `LICENSE` file** though the project claims MIT. Carried over, still open.
- **`main` is still beta-era** (`459faf5`, 0.9.0-beta.5) and contains neither
  2.0.0 nor this work. Making a release branch the new stable `main` remains a
  separate deliberate merge, not done here.
- **Stale worktrees are a live source of confusion.** `git worktree list` shows 12,
  several on dead branches. Recommend pruning everything except
  `release-polish` and the main checkout — but this was NOT done, as deleting a
  user's worktrees is their call.
- The `code-review-graph` commit hook crashes on a `UnicodeEncodeError` (cp1252
  printing a Rich panel) after every commit. Cosmetic — the commit still lands —
  but it makes every commit look like it failed.

## 8. TODO / next steps

- [ ] **HIGHEST VALUE: build the 2.0.2 installer and re-run acceptance against it.**
      Everything verified so far is the unpacked candidate. Stage to `C:\LPB\cand`
      and pass `/DSourceDir` or Inno Setup will die with no line number.
- [ ] Run the A→B update acceptance, 2.0.1 → 2.0.2 (`scripts/updater_ab_acceptance.py`).
- [ ] Exercise group study once against the real gateway — the one shipped feature
      with zero real-backend coverage.
- [ ] Deploy the gateway (`wrangler deploy`) with the security fixes; confirm
      `ADMIN_API_KEY` is set in Worker secrets, not in `wrangler.toml`.
- [ ] Tag `v2.0.2` only after the installer passes. Nothing is tagged yet.
- [ ] Add the `LICENSE` file.
- [ ] Decide the `main` merge.

## 9. Skills-folder retrospective

Ran the learning loop (Claude Skills/CLAUDE.md §8).

- **Lessons promoted:** none written to `lessons/anti-patterns.md` yet — the
  BUG-42 lesson is recorded in this repo's `BUG_LIST.md`, and the candidate below
  is marked for promotion.
- **CHANGELOG entries added:** none yet.
- **PROMOTE TO SKILLS FOLDER** — candidate lesson, verified in this project:
  > *Inherited root-cause analysis is a hypothesis, not a finding.* The handoff
  > this session started from confidently named `_job_objects`, `state.json` and a
  > stale binary as the causes of 6 failures. All three were wrong; the real cause
  > was a one-line filename mismatch found in under two minutes by reading the
  > constant the product actually uses. Re-derive the root cause from the failing
  > output before acting on someone else's diagnosis — a prior session's analysis
  > can cost more time than no analysis, because it aims you at the wrong file.
- **PROMOTE TO SKILLS FOLDER** — candidate lesson:
  > *Before believing two branches have diverged, run `git merge-base
  > --is-ancestor A B` and `git log A..B`.* A session spent effort planning a
  > reconciliation between two "divergent" release lineages that were already one
  > line. Two commands settle it for free.

## 10. How to run / debug

```bash
# Release venv (NOT system python -- system python produces false failures)
cd C:/Users/marsh/Documents/LecturePack-worktrees/release-polish
./.venv/Scripts/python.exe -m pytest tests/ -q
```

```bash
cd C:/Users/marsh/Documents/LecturePack-worktrees/release-polish/ai-gateway && npm test
```

```bash
node electron-spike/package-win.mjs
```

Packaged self-test:

```bash
electron-spike/dist/LecturePack-win32-x64/resources/LecturePackSidecar/LecturePackSidecar.exe --resources-root electron-spike/dist/LecturePack-win32-x64/resources/LecturePackSidecar --data-dir C:/LecturePackScratch/data/selftest --self-test
```

Acceptance gate (long, ~30 min; needs the fixture built first):

```bash
./.venv/Scripts/python.exe scripts/make_acceptance_fixtures.py --out C:/LecturePackScratch/data/fixtures
```

- Disposable paths: everything under `C:\LecturePackScratch\`. `C:\Users\marsh\LecturePackData`
  was never targeted — every command used an explicit `--data-dir`.
- The sidecar IPC wire key is **`request_id`**, not `id`. Sending `id` gets silence.
