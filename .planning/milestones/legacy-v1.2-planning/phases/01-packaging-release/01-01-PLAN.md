---
phase: 01-packaging-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lecturepack/__init__.py
  - lecturepack/constants.py
  - build_release.py
  - LecturePack.spec
  - tests/test_packaging_and_safety.py
  - docs/DECISIONS.md
autonomous: true
requirements:
  - REQ-version-bump
  - REQ-packaging-spec-audit
  - REQ-architecture-layers
  - REQ-test-framework
must_haves:
  truths:
    - "All executable release-version consumers resolve exactly 1.2.0 from the sole canonical runtime authority, lecturepack.__version__; the spec header is only a synchronized non-authoritative label."
    - "[edge:REQ-version-bump:idempotency] Running the version wiring change repeatedly leaves one release-version authority and does not change algorithm or prompt cache keys."
    - statement: "[edge:REQ-version-bump:concurrency] Concurrent import consumers observe the same immutable package version without an initialization-order dependency."
      verification: backstop
    - "[edge:REQ-packaging-spec-audit:empty] The PyInstaller hidden-import list contains every named dynamic v1.1/v1.2 module required by REQ-packaging-spec-audit and is not empty."
    - statement: "[edge:REQ-packaging-spec-audit:idempotency] Repeated clean analysis of the same spec produces the same module and optimization configuration."
      verification: backstop
  flagged_assumptions:
    - "[edge:REQ-packaging-spec-audit:adjacency] The probe's adjacency classification has no clear semantic boundary for a flat hidden-import list; real frozen-import proof is deferred to Plan 01-03 and this remains flagged."
    - "[edge:REQ-packaging-spec-audit:ordering] Hidden-import ordering is treated as grouping for auditability, not as a runtime contract; this remains a flagged assumption until the real build proves collection."
    - "[edge:REQ-packaging-spec-audit:concurrency] build_release.py is treated as a single-writer release operation; concurrent builds against shared build/dist directories are unsupported and remain a flagged assumption."
  prohibitions:
    - statement: "MUST NOT change DETECTOR_VERSION or REPAIR_PROMPT_VERSION while changing release metadata."
      status: unverified
      flagged: true
    - statement: "MUST NOT allow PyInstaller optimization to remove the assertions exercised by the frozen self-test."
      status: unverified
      flagged: true
  artifacts:
    - path: "lecturepack/__init__.py"
      provides: "Dependency-free canonical __version__ value 1.2.0"
    - path: "lecturepack/constants.py"
      provides: "APP_VERSION alias to the package version authority"
    - path: "build_release.py"
      provides: "Release archive and manifest version sourced from lecturepack.__version__"
    - path: "LecturePack.spec"
      provides: "Audited onedir module collection with assertions preserved"
    - path: "tests/test_packaging_and_safety.py"
      provides: "Static release-metadata and spec-drift regression coverage"
    - path: "docs/DECISIONS.md"
      provides: "Dated decision record defining canonical runtime version authority and synchronized non-authoritative labels"
  key_links:
    - from: "lecturepack/__init__.py"
      to: "lecturepack/constants.py"
      via: "from lecturepack import __version__; APP_VERSION = __version__"
      pattern: "APP_VERSION\\s*=\\s*__version__"
    - from: "lecturepack/__init__.py"
      to: "build_release.py"
      via: "VERSION = __version__"
      pattern: "VERSION\\s*=\\s*__version__"
    - from: "lecturepack/constants.py"
      to: "lecturepack/models/job.py"
      via: "Job.init_new writes APP_VERSION into manifest['app_version']"
      pattern: "app_version.*APP_VERSION"
  probe_accounting:
    surfaced_edges: 7
    authored_truths_or_backstops: 4
    flagged_assumptions: 3
---

<objective>
Make release metadata single-sourced at 1.2.0 and bring the PyInstaller specification up to the current module tree before any full-suite or frozen-build gate runs.

Purpose: Prevent version drift, missing frozen imports, and assertion removal from invalidating the release.
Output: Updated version flow, audited PyInstaller spec, and focused regression tests.
</objective>

## Artifacts this phase produces

- A dependency-free `lecturepack.__version__` authority consumed by application and release tooling.
- A PyInstaller onedir spec that explicitly collects the named current UI, service, and infrastructure modules with `optimize=0`.
- Focused automated tests guarding release metadata, job-manifest versioning, hidden imports, and optimization level.

<execution_context>
@C:/Users/marsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/marsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-packaging-release/01-RESEARCH.md
@.planning/phases/01-packaging-release/01-PATTERNS.md
@docs/PRODUCT_SPEC.md
@docs/ARCHITECTURE.md
@docs/DECISIONS.md
@docs/IMPLEMENTATION_PLAN.md
@lecturepack/models/job.py
</context>

<task_scope>
Authorized phase: Phase 1 — Packaging &amp; Release.
Exact goal: Single-source release 1.2.0 and audit the current PyInstaller spec.
Permitted files: `lecturepack/__init__.py`, `lecturepack/constants.py`, `build_release.py`, `LecturePack.spec`, `tests/test_packaging_and_safety.py`, and `docs/DECISIONS.md` only.
Required tests: The three named focused packaging/version tests, followed by the complete `tests/test_packaging_and_safety.py` module.
Non-goals: No Phase 2 cancellation, ffprobe-threading, non-ASCII image-I/O, detector-deduplication, provider, feature, dependency, telemetry, or network changes; no cache-key changes.
Completion evidence: Passing focused pytest output, scoped diff, and `01-01-SUMMARY.md`.
</task_scope>

<nyquist_warning>
`01-RESEARCH.md` does not contain `## Validation Architecture`. This planning gap remains visible; every task in all three plans therefore carries a concrete automated verification command, and the real build/frozen checks are not replaced by static or mocked proof.
</nyquist_warning>

<tasks>

<task type="auto">
  <name>Task 1: Single-source release 1.2.0 and lock the version flow with tests</name>
  <read_first>
    docs/PRODUCT_SPEC.md
    docs/ARCHITECTURE.md
    docs/DECISIONS.md
    docs/IMPLEMENTATION_PLAN.md
    lecturepack/__init__.py
    lecturepack/constants.py
    build_release.py
    lecturepack/models/job.py
    tests/test_packaging_and_safety.py
  </read_first>
  <files>lecturepack/__init__.py, lecturepack/constants.py, build_release.py, tests/test_packaging_and_safety.py, docs/DECISIONS.md</files>
  <action>After reading all four AGENTS.md-mandated documents, add `test_release_version_has_single_authority` and `test_new_job_manifest_uses_release_version` to `tests/test_packaging_and_safety.py`. The first must assert `lecturepack.__version__ == "1.2.0"`, `lecturepack.constants.APP_VERSION is derived from that value`, and `build_release.VERSION` equals it. The second must create a `Job` under `tmp_path` and assert the persisted manifest carries `"app_version": "1.2.0"`. Then set the only executable/canonical release semantic-version authority in `lecturepack/__init__.py`, import it into `lecturepack/constants.py` and bind `APP_VERSION = __version__`, and import it into `build_release.py` and bind `VERSION = __version__`. Add a new topmost dated `2026-07-18` Accepted entry to `docs/DECISIONS.md` defining `lecturepack.__version__` as the canonical runtime/build authority; state that human-facing build labels such as the spec header are synchronized but non-authoritative, give the rationale (prevent runtime, manifest, and archive drift), and record the rejected alternatives of independent literals in each consumer and parsing the spec header at runtime. Keep the package initializer dependency-free, preserve all unrelated comments/docstrings, preserve the five-step build pipeline, and leave `DETECTOR_VERSION` and `REPAIR_PROMPT_VERSION` unchanged.</action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_packaging_and_safety.py::test_release_version_has_single_authority tests/test_packaging_and_safety.py::test_new_job_manifest_uses_release_version -v</automated>
  </verify>
  <acceptance_criteria>Both focused tests pass; new job manifests report 1.2.0; `constants.APP_VERSION` and `build_release.VERSION` contain no independent release-version literal; the dated decision record defines the runtime authority and label synchronization policy.</acceptance_criteria>
  <done>Release metadata has one import-safe authority and all three consumers agree on 1.2.0 without changing cache-key versions.</done>
</task>

<task type="auto">
  <name>Task 2: Audit the frozen-module collection and preserve self-test assertions</name>
  <read_first>
    LecturePack.spec
    lecturepack/app.py
    .planning/phases/01-packaging-release/01-RESEARCH.md
    .planning/phases/01-packaging-release/01-PATTERNS.md
    tests/test_packaging_and_safety.py
  </read_first>
  <files>LecturePack.spec, tests/test_packaging_and_safety.py</files>
  <action>Update the spec header to the synchronized non-authoritative release label 1.2.0; it is deliberately not an executable version authority and the static regression test must keep it equal to `lecturepack.__version__`. Add each fully qualified module named by REQ-packaging-spec-audit exactly once, including all seven `lecturepack.ui.pages` modules, `slide_grid`, `context_repair_panel`, `transcript_store`, `groq_transcription`, `ai_repair_service`, `study_service`, `transcription_backends`, `video_reader`, `transcription_engines`, `ollama_client`, `process_tree`, `secret_store`, and `whisper_detector`; retain the already-present `crop_selector`. Preserve the existing `Analysis`/`EXE`/`COLLECT` onedir layout and third-party imports. Set the Analysis optimization level to zero so `run_selftest` assertions remain executable. Add `test_spec_includes_current_hiddenimports_and_keeps_asserts` that reads the spec from the repository root, checks the complete required module set, synchronized header label, and zero optimization setting, and treats this static test only as configuration drift protection rather than frozen-build proof.</action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_packaging_and_safety.py::test_spec_includes_current_hiddenimports_and_keeps_asserts -v</automated>
  </verify>
  <acceptance_criteria>The focused spec test passes; every named current module appears once in `hiddenimports`; the spec header reports 1.2.0; the Analysis block uses `optimize=0`.</acceptance_criteria>
  <done>The package specification matches the approved current module tree and cannot optimize away the self-test assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source metadata to runtime manifests | Package version data crosses from import-time constants into persisted job metadata. |
| Python module graph to PyInstaller analysis | Dynamically imported modules must be named so the frozen executable contains the same trusted application surface as development. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | `__version__`, `APP_VERSION`, `VERSION` | medium | mitigate | One authority plus regression tests assert runtime and manifest equality at 1.2.0. |
| T-01-02 | Denial of Service | `LecturePack.spec` hidden imports | high | mitigate | Enumerate the required current modules and gate them with a static set test before the real build in Plan 01-03. |
| T-01-03 | Tampering | PyInstaller `optimize` setting | high | mitigate | Pin `optimize=0` and test it so the packaged self-test retains its sanity assertion. |
| T-01-04 | Information Disclosure | PyInstaller data collection | medium | mitigate | Preserve the existing narrow `datas` list; do not add models, user data, credentials, logs, or job directories. |
</threat_model>

<verification>
Run `.venv\Scripts\python.exe -m pytest tests/test_packaging_and_safety.py -v` and inspect `git diff -- lecturepack/__init__.py lecturepack/constants.py build_release.py LecturePack.spec tests/test_packaging_and_safety.py` to confirm only scoped release metadata, spec collection, and regression tests changed.
</verification>

<success_criteria>
- Exactly one executable/canonical release semantic-version authority reports 1.2.0; the spec header is a synchronized non-authoritative label guarded by the static test.
- New `Job` manifests persist 1.2.0.
- All named current modules are present in `LecturePack.spec` without duplicate `crop_selector`.
- PyInstaller assertions remain enabled through `optimize=0`.
- Focused packaging and safety tests pass.
</success_criteria>

<output>
Create `.planning/phases/01-packaging-release/01-01-SUMMARY.md` when done.
</output>
