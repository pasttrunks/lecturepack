---
phase: 01-runtime-contract-bootstrap
plan: 03
type: execute
wave: 2
depends_on: [01-02]
files_modified: [app/desktop/bridge.py, app/desktop/engine_adapter.py, lecturepack/controllers/runtime_diagnostics_controller.py, lecturepack/services/runtime_diagnostics.py, tests/test_adapter_startup.py, tests/test_runtime_diagnostics.py, docs/HANDOFF_PHASE_1.md]
autonomous: true
requirements: [RUNT-02, RUNT-05]
must_haves:
  truths:
    - No adapter, controller, job, navigation, or optional-provider work begins before HEALTHY, and a healthy bootstrap produces exactly one normal-ready transition.
    - A healthy CPU launch produces no setup, error, gate, or status noise; a broken optional engine produces a separately identifiable post-HEALTHY fallback notice.
    - The QWebChannel status consumer receives the canonical runtime-health snapshot and cannot derive another required-runtime inventory.
  artifacts: [app/desktop/bridge.py, app/desktop/engine_adapter.py, lecturepack/controllers/runtime_diagnostics_controller.py, lecturepack/services/runtime_diagnostics.py, tests/test_adapter_startup.py, tests/test_runtime_diagnostics.py, docs/HANDOFF_PHASE_1.md]
  key_links:
    - MainWindow constructs Backend; Backend completes RuntimeBootstrapService admission before it constructs an EngineAdapter.
    - Backend delegates runtime-health reads to EngineAdapter, then RuntimeDiagnosticsController and RuntimeDiagnosticsService; the service reads the immutable bootstrap result and Wave 0 canonical inventory.
    - The bridge emits the canonical JSON snapshot and a separately typed fallback notice to the QWebChannel consumer without direct infrastructure discovery.
---
<objective>Install desktop admission and canonical runtime-health status on the shipped MainWindow-to-QWebChannel-to-EngineAdapter path. Purpose: make validated CPU health the sole entry condition for normal application behavior and expose its evidence without a parallel required-file inventory. Output: bridge/adapter status seam, controller/service snapshot boundary, regression tests, and honest Phase 1 handoff.</objective>
<execution_context>@C:/Users/marsh/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@.planning/phases/01-runtime-contract-bootstrap/01-CONTEXT.md
@.planning/phases/01-runtime-contract-bootstrap/01-RESEARCH.md
@.planning/phases/01-runtime-contract-bootstrap/01-02-SUMMARY.md
@app/desktop/main.py
@app/desktop/bridge.py
@app/desktop/engine_adapter.py</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Gate the active bridge-to-adapter startup path on HEALTHY</name><files>app/desktop/bridge.py, app/desktop/engine_adapter.py, tests/test_adapter_startup.py</files><behavior>- Backend completes RuntimeBootstrapService assessment before it constructs an adapter, controller, job action, navigation action, or optional-provider probe per D-05.
- A healthy CPU assessment produces exactly one normal-ready transition and no setup, error, gate, or status event per D-06.
- A broken optional preference emits one structured requested/resolved/reason fallback notice only after HEALTHY per D-08; it neither changes admission nor creates a second ready transition.</behavior><action>Write ordering tests against the actual `MainWindow` to `Backend` QWebChannel construction seam, then make `Backend` own a narrow desktop startup coordinator. The coordinator invokes `RuntimeBootstrapService` before `make_adapter`, permits adapter construction and normal bridge delegation only for HEALTHY, and retains SETUP_REQUIRED as a stable structured result for the deferred Phase 2 gate. Make `EngineAdapter` expose the already-admitted runtime-health result without initiating validation, controller construction, jobs, navigation, or optional probing. Emit exactly one normal-ready transition only for healthy admission. On the D-08 branch, forward the service-produced fallback record after healthy admission through a distinct bridge event; it must be distinguishable from readiness and ordinary status. Do not add setup or repair UI, downloads, demo startup, alternative required-file inventory, or direct bridge-to-infrastructure calls. Preserve controller-to-service-to-infrastructure layering, unrelated comments/docstrings, original videos, user data, and immutable payload per D-05/D-06/D-08/D-14.</action><verify><automated>pytest tests/test_adapter_startup.py tests/test_runtime_bootstrap.py -q; pytest -q</automated></verify><done>The active desktop bridge admits normal behavior exactly once after HEALTHY and reports optional fallback independently.</done></task>
<task type="auto" tdd="true"><name>Task 2: Publish the canonical runtime-health snapshot through the bridge</name><files>app/desktop/bridge.py, app/desktop/engine_adapter.py, lecturepack/controllers/runtime_diagnostics_controller.py, lecturepack/services/runtime_diagnostics.py, tests/test_runtime_diagnostics.py</files><behavior>- `RuntimeDiagnosticsService.runtime_health_snapshot()` returns a stable structured snapshot with canonical inventory identity, admission state, per-component evidence, smoke mode/evidence, and optional fallback notice from RuntimeBootstrapService.
- `RuntimeDiagnosticsController.runtime_health_snapshot()` is the adapter-facing controller boundary; it delegates to the service and never builds a required-component list.
- The bridge status consumer receives that canonical JSON snapshot through EngineAdapter and never derives its own inventory; healthy CPU emits no status noise while D-08 fallback remains separately identifiable.</behavior><action>Write tests first, then create `RuntimeDiagnosticsService` as a read-only service adapter over the immutable bootstrap result and Wave 0 canonical inventory. Create `RuntimeDiagnosticsController` as the sole controller boundary for the adapter. Have `EngineAdapter` delegate snapshot retrieval to that controller and have `Backend` expose it through a QWebChannel slot plus a signal only when the UI needs a pushed update; serialize the controller result unchanged except for JSON transport. Keep the fallback notice a distinct structured bridge payload rather than folding it into normal-ready or generic status. Add regression tests that prove the bridge/adapter consumer uses the snapshot identity and has no independent component-list construction or infrastructure discovery. Do not change Qt Widgets diagnostics, ConfigManager discovery, repair flows, or UI navigation; do not let bridge or adapter call infrastructure directly. Preserve the controller-to-service-to-infrastructure path and protected data per D-02/D-03/D-06/D-08/D-14.</action><verify><automated>pytest tests/test_runtime_diagnostics.py tests/test_adapter_startup.py tests/test_runtime_inventory.py -q; pytest -q</automated></verify><done>QWebChannel status exposes one canonical runtime-health snapshot and a separately identifiable fallback record without alternate inventory logic.</done></task>
<task type="auto"><name>Task 3: Write the Phase 1 honest handoff</name><files>docs/HANDOFF_PHASE_1.md</files><action>Write the required handoff with completed work, remaining work, blockers, exact targeted and full pytest output locations/results, packaged smoke argv/evidence/failure reason when applicable, active bridge/adapter snapshot-consumer evidence, RUNT-09 approval status, and the statement that Phase 2 remains blocked until its ADR post-checkpoint task passes. Preserve unrelated comments/docstrings and do not modify other documentation or scope per D-13/D-14.</action><verify><automated>pytest tests/test_adapter_startup.py tests/test_runtime_diagnostics.py tests/test_runtime_bootstrap.py tests/test_runtime_packaged_smoke.py -q; pytest -q</automated></verify><done>Handoff accurately records runtime-health admission/status evidence, remaining work, and blockers.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| bootstrap result to QWebChannel bridge | runtime health determines whether the desktop may construct normal engine behavior |
| controller snapshot to web status consumer | health evidence crosses JSON transport and must retain one canonical inventory source |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-05 | Elevation | bridge startup coordinator | high | mitigate | ASVS-L1 V5 ordering tests prove Backend does not create adapter/controller/job/navigation/optional work until coordinator-approved HEALTHY and emits one normal-ready transition. |
| T-01-09 | Tampering | bridge runtime-health payload | high | mitigate | ASVS-L1 V5 controller/service snapshot uses Wave 0 canonical identity and immutable bootstrap evidence; bridge/adapter tests reject direct infrastructure discovery and secondary required-component construction. |
</threat_model>
<verification>Run targeted and full pytest. Exercise healthy CPU/no-fallback and post-HEALTHY optional-fallback cases separately; preserve packaged proof as a blocking Phase 1 prerequisite.</verification>
<success_criteria>RUNT-02 and RUNT-05 are enforced on the active desktop chain, with exactly one canonical runtime-health source and truthful Phase 1 evidence.</success_criteria>
<output>Create .planning/phases/01-runtime-contract-bootstrap/01-03-SUMMARY.md when done</output>
