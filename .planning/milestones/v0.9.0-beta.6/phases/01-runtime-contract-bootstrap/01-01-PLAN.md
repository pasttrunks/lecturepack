---
phase: 01-runtime-contract-bootstrap
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: [lecturepack/infrastructure/runtime_inventory.py, lecturepack/infrastructure/runtime_validation.py, app/packaging/build.py, app/packaging/assets/runtime-smoke.wav, tests/test_runtime_inventory.py, tests/test_runtime_bootstrap.py, tests/test_runtime_packaged_smoke.py, tests/fixtures/mock_runtime_hang.py]
autonomous: true
requirements: [RUNT-01, RUNT-02, RUNT-04]
must_haves:
  truths:
    - "D-01: The canonical required admission set is packaged FFmpeg, ffprobe, CPU Whisper CLI, every required CPU DLL, bundled ggml-base.en.bin, and the project-owned smoke WAV; CUDA, Vulkan, Ollama, Groq, and yt-dlp never determine core admission health."
    - The canonical inventory and packaged smoke contract exist before implementation plans consume them.
    - A real disposable onedir smoke is mandatory evidence, never a skipped test or mock substitute.
  artifacts: [lecturepack/infrastructure/runtime_inventory.py, lecturepack/infrastructure/runtime_validation.py, app/packaging/assets/runtime-smoke.wav, tests/test_runtime_inventory.py, tests/test_runtime_bootstrap.py, tests/test_runtime_packaged_smoke.py, tests/fixtures/mock_runtime_hang.py]
  key_links:
    - Packaging, validation, bootstrap tests, and the packaged smoke all consume one inventory.
---
<objective>Create every missing Wave 0 validation seam and the non-skippable packaged-runtime evidence harness. Purpose: establish RUNT-01/02/04 contracts before bootstrap wiring. Output: canonical inventory, smoke asset/harness, and failing-to-passing test scaffolds.</objective>
<execution_context>@C:/Users/marsh/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@.planning/phases/01-runtime-contract-bootstrap/01-CONTEXT.md
@.planning/phases/01-runtime-contract-bootstrap/01-RESEARCH.md
@.planning/phases/01-runtime-contract-bootstrap/01-VALIDATION.md
@.planning/phases/01-runtime-contract-bootstrap/01-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Create canonical inventory, validator, fixtures, and Wave 0 contracts</name><files>lecturepack/infrastructure/runtime_inventory.py, lecturepack/infrastructure/runtime_validation.py, app/packaging/assets/runtime-smoke.wav, tests/test_runtime_inventory.py, tests/test_runtime_bootstrap.py, tests/fixtures/mock_runtime_hang.py</files><behavior>- The inventory contains only packaged FFmpeg, ffprobe, CPU Whisper CLI, every resolved CPU DLL, base-English model, and smoke WAV per D-01/D-02.
- The checked-in WAV is a project-owned 1.000 s, 16 kHz, mono, signed 16-bit little-endian non-silent synthetic tone; it is hashed and maps to `smoke/runtime-smoke.wav`.
- The validator records argv, exit, stdout, stderr, duration, and reason for success, nonzero, and timeout; the hang fixture proves bounded timeout.</behavior><action>Write the Wave 0 failing tests, then create the canonical ordered, root-contained relative inventory and payload identity. Add the deterministic WAV asset and package mapping. Build an injectable QProcess-compatible validator with argument arrays and safely handled Unicode paths, no PATH/settings/network/user-content discovery, and bounded tree termination only for the smoke. Seed bootstrap test contracts for fresh/stale/partial/identity/update/repair and migration behavior; later plans own their policy implementation. Provision or rebuild a clean onedir fixture as a test prerequisite; its absence or failure must fail the packaged-smoke test with a recorded reason, never pytest-skip. Preserve unrelated comments/docstrings and never modify original videos, user data, immutable portable payload, or the user's main worktree per D-12/D-14.</action><verify><automated>pytest tests/test_runtime_inventory.py tests/test_runtime_bootstrap.py -q</automated></verify><done>All Wave 0 inventory, runner, WAV, hang, and policy seams exist with automated fault contracts.</done></task>
<task type="auto" tdd="true"><name>Task 2: Wire canonical package membership and execute the blocking disposable packaged smoke</name><files>app/packaging/build.py, tests/test_runtime_packaged_smoke.py</files><behavior>- The build copies `app/packaging/assets/runtime-smoke.wav` to `smoke/runtime-smoke.wav` and derives required payload checks from the canonical inventory.
- A copied/rebuilt clean onedir fixture under a Unicode-and-space path executes exactly `<runtime_root>/bin/whisper-cli.exe -m <runtime_root>/models/ggml-base.en.bin -f <runtime_root>/smoke/runtime-smoke.wav -t 1 -nt` with fresh `LECTUREPACK_DATA_DIR`.
- Success is exit 0 in under 30 seconds with backend DLL-from-canonical-bin, model/model-size, WAV-read, and processing evidence; no transcript/output artifact is created.</behavior><action>Make package assembly/checks consume the inventory, including the smoke WAV. Implement the real disposable smoke harness: provision/rebuild/copy an onedir fixture, copy it to an explicit path containing spaces and non-ASCII characters, create fresh `LECTUREPACK_DATA_DIR`, and invoke FFmpeg, ffprobe, and the exact Whisper argv above with the 30,000 ms deadline and no output-file flag. Persist test evidence containing argv, exit code, stdout, stderr, duration, and reason; assert the required four Whisper evidence classes, no output transcript, and persisted healthy identity. Include forced nonzero and deterministic hang timeout cases, where timeout keeps admission closed. Fixture absence, provision failure, process failure, missing DLL/model/WAV evidence, or timeout blocks completion rather than skipping. This is a Phase-1 disposable proof only: no repair/setup or Phase 2+ work. Preserve unrelated comments/docstrings and all protected user/original-video data per D-12/D-14.</action><verify><automated>pytest tests/test_runtime_inventory.py tests/test_runtime_bootstrap.py tests/test_runtime_packaged_smoke.py tests/test_beta3_packaging.py -q; pytest -q</automated></verify><done>The package contains the canonical smoke asset and a real packaged proof executes or fails blocking with diagnostic evidence.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| package payload to validator | release files and paths are untrusted until inventory and process checks pass |
| validator to CLI | fixed local arguments cross to bundled executables |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Tampering | inventory/payload | high | mitigate | ASVS-L1 V5 root containment, duplicate/traversal rejection, hash/identity tests, and real DLL/model/WAV smoke evidence. |
| T-01-02 | Denial of service | smoke process | high | mitigate | ASVS-L1 V5 bounded separate argv/QProcess timeout, hang-fixture gate, captured reason/streams, and smoke-tree-only termination. |
</threat_model>
<verification>Wave 0 must complete every listed validation seam. Preserve targeted/full pytest output and real packaged evidence; no skipped fixture result is acceptable.</verification>
<success_criteria>RUNT-01/02/04 test infrastructure and actual disposable packaged evidence are executable before Wave 1.</success_criteria>
<output>Create .planning/phases/01-runtime-contract-bootstrap/01-01-SUMMARY.md when done</output>
