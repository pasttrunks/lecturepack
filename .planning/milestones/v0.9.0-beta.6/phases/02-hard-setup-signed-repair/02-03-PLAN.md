---
phase: 02-hard-setup-signed-repair
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - lecturepack/services/runtime_repair.py
  - app/desktop/repair_worker.py
  - app/desktop/bridge.py
  - tests/test_runtime_repair.py
autonomous: true
requirements: [REPR-01, REPR-03, REPR-04, REPR-05, REPR-06, REPR-07, REPR-08, REPR-09, REPR-10]
must_haves:
  truths:
    - "Repair all begins an operation-bound metadata-only pre-consent offer: it requests only the exact manifest and signature, authenticates signature/schema/version/key/inventory, derives the exact four-ZIP byte sum and affected-component fields, and never requests archive payloads or starts staging/activation before Confirm & repair (D-06, D-19, D-20; REPR-03/04)."
    - "The repair coordinator emits one ordered JSON-safe event stream: started, progress, retrying, cancel_requested, cancelled/failed/activated/admitted, with exactly one matching terminal outcome (D-11, D-12, D-22; REPR-08)."
    - "The bridge keeps normal collaborators and guarded operations unavailable during SETUP_REQUIRED, exposes only narrow repair/diagnostic commands, and constructs adapter/updater only after canonical post-repair HEALTHY admission (D-23; REPR-01/09)."
    - "Offline exhaustion is classified separately and exposes no offline import/per-file mechanism (D-18; REPR-10)."
    - "Retrying the same failed repair and receiving concurrent/cancel events is idempotent by operation ID and safe-boundary state (REPR-02/03/04/05/08/10 edge backstop)."
  artifacts:
    - "lecturepack/services/runtime_repair.py: RuntimeRepairService, RepairOperation, RepairEvent, RepairFailure"
    - "app/desktop/repair_worker.py: RuntimeRepairWorker and repair event signals"
    - "app/desktop/bridge.py: start_runtime_repair, cancel_runtime_repair, retry_runtime_assessment, repair_event"
  key_links:
    - "Bridge holds the authenticated metadata-only offer in operation-safe state, returns its version/source/affected-content/exact-download-size fields to the UI, invalidates it on stale/cancel/failure/retry admission changes, and starts payload acquisition only when the matching offer is confirmed."
  prohibitions:
    - statement: "MUST NOT construct adapters, updater, job activation, navigation, optional probing, or normal service access while runtime health is SETUP_REQUIRED."
      status: resolved
      verification: judgment
      resolution: "Task 2 acceptance criteria and `python -m pytest tests/test_runtime_repair.py tests/test_adapter_startup.py -q` prove guarded bridge operations and healthy-only collaborator construction."
    - statement: "MUST NOT offer manual per-file repair, offline repair import, user-selected URLs, or background/silent repair."
      status: resolved
      verification: judgment
      resolution: "Task 1 acceptance criteria and `tests/test_runtime_repair.py` require fixed URLs, explicit consent, and reject manual/offline/per-file paths."
---

<objective>
Authorized phase: 02 — Hard Setup & Signed Repair. Integrate the authenticated transaction with the desktop admission boundary without weakening Phase 1 guards.

Purpose: Make repair a consented, cancellable, observable consumer of the trust/generation contracts and intentionally close Phase 1's deferred repair-consumer evidence.
Permitted files: only listed service, desktop worker/bridge, and test files. Required tests: transaction fault matrix and real bridge ordering checks; full pytest at Wave 2 end. Non-goals: UI rendering, portable-bundle mutation, generic updater reuse, manual/offline imports, telemetry. Completion evidence: damaged disposable runtime retains/rolls back and healthy admission constructs collaborators only afterward.
</objective>

<execution_context>@C:/Users/marsh/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@docs/DECISIONS.md
@.planning/phases/02-hard-setup-signed-repair/02-CONTEXT.md
@.planning/phases/02-hard-setup-signed-repair/02-RESEARCH.md
@.planning/phases/02-hard-setup-signed-repair/02-PATTERNS.md
@.planning/phases/01-runtime-contract-bootstrap/01-VERIFICATION.md
@app/desktop/bridge.py
@app/desktop/updater.py</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement consent-gated repair coordination and transactional error mapping</name>
  <files>lecturepack/services/runtime_repair.py, tests/test_runtime_repair.py</files>
  <read_first>Read `AGENTS.md`, `docs/PRODUCT_SPEC.md`, `docs/ARCHITECTURE.md`, `docs/DECISIONS.md`, `docs/IMPLEMENTATION_PLAN.md`, `release_trust.py`, `runtime_generation.py`, `lecturepack/services/runtime_bootstrap.py`, `app/desktop/updater.py`, `tests/test_update_integration.py`, `tests/test_runtime_generation.py`, and AD-19 before editing.</read_first>
  <behavior>- `begin_repair_offer` issues only the fixed exact-version manifest and signature GETs, verifies raw signature before parsing, validates schema/version/key id/R1 four-record inventory, and emits a metadata-ready event containing version, source, friendly affected components, and checked exact four-ZIP-byte total.\n- Confirmed payload acquisition is impossible without the matching authenticated offer; no HEAD/range/ZIP request, extraction, staging, pointer mutation, or activation occurs before confirmation.\n- Injected transport returns only fixed official exact-version URLs; transient connection errors retry a bounded three attempts with event evidence, exhausted metadata or payload network errors map offline, and all other errors map failed.\n- Download/validation/permission/cancel/activation/admission failures retain prior generation and provide sanitized diagnostic facts.\n- Success emits `admitted` only after full `assess(trigger='repair')` is HEALTHY.</behavior>
  <action>Create Qt-free `RuntimeRepairService` with constructor-injected transport, clock/backoff, generation store, and bootstrap assessor; model `RepairOperation`, authenticated offer, `RepairEvent`, and typed `RepairFailure`. Per R3, implement `begin_repair_offer` as the only pre-consent acquisition path: GET the two fixed metadata names, verify signature before parse, validate exact schema/version/key/inventory, map current unhealthy canonical paths to the four friendly categories, and checked-sum the four signed ZIP `size_bytes`; emit that offer without a ZIP request. Require an unexpired matching operation-bound offer for Confirm & repair; invalidate it on cancel, terminal failure, retry, or changed admission state. Only then use fixed AD-19 ZIP URLs and size-limited streaming. Use a bounded three-attempt exponential backoff as discretion under D-14. Check cancellation between download/verification/extraction steps, make pointer activation indivisible, and map diagnostics through a secret-redaction function. Do not reuse updater's feed/host flexibility or direct-to-live installer semantics.</action>
  <acceptance_criteria>Tests prove pre-consent requests are exactly manifest plus signature and produce authenticated confirmation fields; tests prove no HEAD/range/ZIP request, staging, extraction, pointer mutation, or activation before a matching Confirm & repair; stale/failed/cancelled offers cannot be confirmed; every rejection/failure keeps prior generation bytes/pointer; retry/offline classification is deterministic; post-activation failure restores selection; success requires canonical admission.</acceptance_criteria>
  <verify><automated>python -m pytest tests/test_runtime_repair.py tests/test_runtime_generation.py -q</automated></verify>
  <done>A pure repair service reliably coordinates trusted acquisition, staging, rollback, retry, cancellation, diagnostics, and admission.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Expose narrow worker and bridge repair integration</name>
  <files>app/desktop/repair_worker.py, app/desktop/bridge.py, tests/test_runtime_repair.py</files>
  <read_first>Read `AGENTS.md`, `docs/PRODUCT_SPEC.md`, `docs/ARCHITECTURE.md`, `docs/DECISIONS.md`, `docs/IMPLEMENTATION_PLAN.md`, Task 1 service/tests, `app/desktop/bridge.py`, `app/desktop/updater.py`, `tests/test_adapter_startup.py`, `02-PATTERNS.md`, and Phase 1 verification evidence.</read_first>
  <behavior>- `RuntimeRepairWorker` owns the worker lifecycle and sends operation-ID metadata-ready/repair events without Qt objects crossing the service boundary.\n- Bridge exposes an offer-start slot and matching-confirm slot while SETUP_REQUIRED; the former returns only authenticated confirmation metadata and the latter alone permits archives; every existing normal bridge operation remains guarded.\n- Bridge re-assesses on repair admission and creates adapter/updater exactly once only for HEALTHY; stale/double terminal events or invalidated offers do not construct them twice.\n- Retry assessment emits canonical setup/healthy data without a repair download.</behavior>
  <action>Implement a QThread `RuntimeRepairWorker` with explicit cancellation and JSON-safe `repair_event` signal; add narrowly named bridge slots to begin the metadata-only offer, confirm its matching offer ID, cancel, retry assessment, copy/save sanitized diagnostics, and exit following existing bridge conventions. Serialize the authenticated offer fields (exact app version, official source, friendly affected content, exact download bytes) to the UI and invalidate bridge-held offers on operation replacement, cancellation, failure, or admission change. Maintain `_ADMISSION_GUARDED_OPERATIONS`; centralize operation ownership and destroy/clear worker after its unique terminal event. On matching admitted event invoke the canonical bootstrap assessment with `trigger='repair'`, reconstruct collaborators only after HEALTHY, and publish an admitted event only then. Preserve existing comments/docstrings and do not construct an updater just to repair.</action>
  <acceptance_criteria>Qt-slot and direct-call tests show the unhealthy bridge exposes only metadata-offer then matching confirmation repair support, blocks every normal collaborator operation, sends exact authenticated UI fields without ZIP traffic before confirm, and invalidates stale offers; admitted path builds collaborators after health; stale/out-of-order events, repeat cancels, and terminal duplicates leave state stable.</acceptance_criteria>
  <verify><automated>python -m pytest tests/test_runtime_repair.py tests/test_adapter_startup.py -q</automated></verify>
  <done>The desktop boundary is a real repair consumer and Phase 1's healthy-only construction guarantee remains intact.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| user consent→repair operation | A network-capable transaction begins only after explicit confirmation. |
| repair service→desktop bridge | Background events influence admission/construction state. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-07 | Information disclosure | transport | high | mitigate | Fixed official asset URLs only after confirmation; injected test transport proves no pre-consent/unrelated requests. |
| T-02-08 | Tampering/Availability | repair transaction | high | mitigate | Ordered service events, atomic-store failure matrix, bounded retry/cancel and redacted diagnostics. |
| T-02-09 | Elevation of privilege | bridge.py | high | mitigate | Preserve guarded-operation registry and instantiate collaborators only after canonical HEALTHY. |
| T-02-10 | Tampering | worker events | high | mitigate | Operation ID, unique terminal event, stale/out-of-order event tests. |
</threat_model>
<verification>Run targeted repair/bridge tests after each task, then `python -m pytest -q` at Wave 2 end.</verification>
<success_criteria>Signed repair is an integrated, consented desktop workflow that cannot bypass Phase 1 admission or corrupt an existing runtime.</success_criteria>

## Artifacts this phase produces
- `RuntimeRepairService`, `RepairOperation`, `RepairEvent`, and `RepairFailure`.
- `RuntimeRepairWorker`, operation-ID repair signal, and bridge repair/retry/cancel/diagnostic methods.
- `tests/test_runtime_repair.py` consent, network, rollback, admission-order, and stale-event coverage.

<output>Create `.planning/phases/02-hard-setup-signed-repair/02-03-SUMMARY.md` when done</output>
