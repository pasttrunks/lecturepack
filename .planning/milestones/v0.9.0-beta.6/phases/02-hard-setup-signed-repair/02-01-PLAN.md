---
phase: 02-hard-setup-signed-repair
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/requirements.txt
  - requirements.txt
  - lecturepack/infrastructure/release_trust.py
  - tests/fixtures/release_trust/manifest.json
  - tests/fixtures/release_trust/manifest.sig
  - tests/fixtures/release_trust/manifest-altered.json
  - tests/fixtures/release_trust/release-layout.json
  - tests/test_release_trust.py
autonomous: false
requirements: [REPR-03, REPR-04, REPR-05, REPR-06]
must_haves:
  truths:
    - "Selecting Repair all may acquire only the authenticated manifest and detached signature metadata before confirmation; it never requests archive payloads, stages, extracts, mutates the pointer, or activates a runtime until Confirm & repair (D-06, D-19, D-20; REPR-03/04)."
    - "Only the exact running-version official release origin and all six AD-19 assets are eligible: LecturePack-{app_version}-RuntimeManifest-v1.json, its .sig, and the ffmpeg, whisper-cpu, model-base-en, and smoke-fixture ZIPs with their exact R1 names (D-19, D-20; REPR-04)."
    - "A raw 64-byte detached Ed25519 signature verifies the exact received canonical manifest bytes using the compiled 64-lowercase-hex raw public key before JSON parsing (D-19; REPR-05)."
    - "Wrong schema/version/tag, noncanonical or duplicate-key JSON, missing/extra inventory entries, invalid hashes, unsafe paths, duplicate or bomb archive members, and mixed-release assets are rejected before activation (D-20; REPR-06)."
    - "[flagged assumption] REPR-01 has no separate edge classification; canonical Phase-1 SETUP_REQUIRED evidence remains the admission input and is exercised by later bridge/UI plans."
  artifacts:
    - "lecturepack/infrastructure/release_trust.py: ReleaseTrustVerifier, ReleaseManifest, ReleaseLayout, official_release_urls"
    - "tests/fixtures/release_trust/: frozen approved vector, altered-byte vector, exact layout table"
  key_links:
    - "ReleaseTrustVerifier authenticates manifest/signature bytes before strict parsing and yields an operation-bound authenticated offer (exact version, friendly affected components, four-archive records, and checked sum of four signed ZIP sizes) to RuntimeRepairService and the bridge/UI."
  prohibitions:
    - statement: "MUST NOT contact any endpoint other than the fixed exact-version official LecturePack release assets after explicit consent; no telemetry or release discovery is permitted."
      status: resolved
      verification: judgment
      resolution: "Plan 02-03 Task 1 acceptance criteria and `tests/test_runtime_repair.py` prove metadata-only pre-consent traffic and fixed official URLs; Plan 02-05 packaged evidence records the real repair command/logs."
    - statement: "MUST NOT treat GitHub TLS, a reserialized manifest, or a checksum fetched independently as the project trust root."
      status: resolved
      verification: judgment
      resolution: "Task 2 acceptance criteria and `python -m pytest tests/test_release_trust.py -q` require raw-byte detached-signature verification before parsing and reject altered bytes."
---

<objective>
Authorized phase: 02 — Hard Setup & Signed Repair. Implement the independently testable trust-root and exact release-layout contract that a later repair transaction consumes.

Purpose: Prevent trust-root substitution and ambiguous release content before any installer or UI is wired.
Permitted files: only the frontmatter-listed files. Required tests: frozen-vector unit suite and final full pytest in the release-evidence plan. Non-goals: downloading, activation, UI, manual imports, telemetry, or portable-bundle writes. Completion evidence: a real frozen Ed25519 accept/reject vector plus strict rejection matrix.
</objective>

<execution_context>
@C:/Users/marsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/marsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/DECISIONS.md
@.planning/phases/02-hard-setup-signed-repair/02-CONTEXT.md
@.planning/phases/02-hard-setup-signed-repair/02-RESEARCH.md
@.planning/phases/02-hard-setup-signed-repair/02-PATTERNS.md
@.planning/phases/02-hard-setup-signed-repair/02-VALIDATION.md
@.planning/phases/01-runtime-contract-bootstrap/01-VERIFICATION.md
</context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Verify the locked cryptography package provenance</name>
  <files>app/requirements.txt, requirements.txt</files>
  <read_first>Read `AGENTS.md`, `docs/PRODUCT_SPEC.md`, `docs/ARCHITECTURE.md`, `docs/DECISIONS.md`, `docs/IMPLEMENTATION_PLAN.md`, the Package Legitimacy Audit in `02-RESEARCH.md`, AD-19 in `docs/DECISIONS.md`, and both current requirements files before approving the dependency.</read_first>
  <what-built>Package legitimacy gate for the locked `cryptography==49.0.0` dependency, which research classified SUS despite AD-19 selecting it.</what-built>
  <how-to-verify>Open https://pypi.org/project/cryptography/49.0.0/, verify the project/version and the Windows wheel SHA-256 against the release evidence planned here, then approve its use. This approval is required before changing either requirements file or importing the library.</how-to-verify>
  <action>Hold the dependency change until a human verifies the PyPI project and the selected Windows wheel SHA-256. This checkpoint implements D-19 and the research legitimacy disposition; it does not authorize substituting a different verifier package.</action>
  <acceptance_criteria>The approver records that the PyPI project is cryptography 49.0.0 and that the intended Windows wheel hash matches release evidence.</acceptance_criteria>
  <verify><automated>python -m pytest tests/test_signing_adr_contract.py -q</automated></verify>
  <done>Human approval unblocks the exact locked dependency; a mismatch stops implementation for correction.</done>
  <resume-signal>Type "approved" after verifying the PyPI project and Windows-wheel hash, or describe the mismatch.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Lock the signed release bytes and exact layout contract</name>
  <files>app/requirements.txt, requirements.txt, tests/fixtures/release_trust/manifest.json, tests/fixtures/release_trust/manifest.sig, tests/fixtures/release_trust/manifest-altered.json, tests/fixtures/release_trust/release-layout.json, tests/test_release_trust.py</files>
  <read_first>Read `AGENTS.md`, `docs/PRODUCT_SPEC.md`, `docs/ARCHITECTURE.md`, `docs/DECISIONS.md`, `docs/IMPLEMENTATION_PLAN.md`, `docs/DECISIONS.md` AD-19, the Package Legitimacy Audit in `02-RESEARCH.md`, `tests/test_signing_adr_contract.py`, `tests/test_update_service.py`, `lecturepack/infrastructure/runtime_inventory.py`, and `app/packaging/build.py` before modifying the listed files.</read_first>
  <behavior>- The frozen manifest/signature accepts with the approved raw key and fails when one byte changes.\n- The exact layout table contains all six AD-19 assets: metadata-only `LecturePack-{app_version}-RuntimeManifest-v1.json` and `.sig`, plus `LecturePack-{app_version}-Runtime-ffmpeg.zip`, `LecturePack-{app_version}-Runtime-whisper-cpu.zip`, `LecturePack-{app_version}-Runtime-model-base-en.zip`, and `LecturePack-{app_version}-Runtime-smoke-fixture.zip`; it maps each ZIP to R1's exact forward-slash members and canonical destination.\n- The four archive records have signed SHA-256/non-negative integer sizes and their checked unsigned-64-bit sum is the exact confirmation download size.\n- The exact `cryptography==49.0.0` pin is present in both project requirement surfaces only after the blocking checkpoint is approved.</behavior>
  <action>Per D-19/D-20/D-23 and R1–R3, add the exact approved cryptography pin; create immutable fixture bytes from the release signing process (not test-generated signatures) and an explicit complete release-layout table containing all six exact AD-19 names, four ZIP member-to-canonical-inventory mappings, signed ZIP hashes/sizes, and the metadata-only manifest/signature roles. Write failing vector, schema-contract, archive-set, and checked-total tests first. Keep all fixture content synthetic and non-secret; no private signing material, GitHub call, downloader, or archive extraction belongs in this task.</action>
  <acceptance_criteria>Both requirement files contain only `cryptography==49.0.0` for this new dependency; fixtures are byte-stable; tests prove the approved vector and altered-byte failure, exact six-asset R1 layout, exact four-archive byte total, and no payload-derived offer before production verifier code exists.</acceptance_criteria>
  <verify><automated>python -m pytest tests/test_release_trust.py -q</automated></verify>
  <done>Frozen release vector, altered-byte vector, and canonical layout fixture establish the test oracle for AD-19.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fail-closed exact-byte release trust verification</name>
  <files>lecturepack/infrastructure/release_trust.py, tests/test_release_trust.py</files>
  <read_first>Read `AGENTS.md`, `docs/PRODUCT_SPEC.md`, `docs/ARCHITECTURE.md`, `docs/DECISIONS.md`, `docs/IMPLEMENTATION_PLAN.md`, the fixtures and failing cases created in Task 1; then read `app/desktop/update_service.py`, `lecturepack/infrastructure/runtime_inventory.py`, `02-PATTERNS.md`, and the AD-19 section of `docs/DECISIONS.md`.</read_first>
  <behavior>- `ReleaseTrustVerifier.verify_manifest(raw_manifest, raw_signature)` authenticates raw bytes before decoding/parsing.\n- Strict validation rejects malformed raw key/signature, duplicate or unknown JSON fields, noncanonical serialization, wrong app version/schema/tag/key id, any missing/extra one of R1's four ZIP records, invalid digests/sizes, unsafe/case-colliding paths, and inventory drift.\n- `official_release_urls(app_version)` returns only `https://github.com/pasttrunks/lecturepack/releases/download/v{app_version}/` with all six R1 exact filenames.\n- `authenticated_offer` derives the exact version, affected-component mapping supplied by current admission evidence, four archive records, and checked unsigned-64-bit total without requesting a ZIP.</behavior>
  <action>Create Qt-free `ReleaseTrustVerifier`, immutable `ReleaseManifest`/`ReleaseLayout`/authenticated-offer dataclasses, and `official_release_urls`; use `cryptography.hazmat.primitives.asymmetric.ed25519.Ed25519PublicKey` only for signature verification. Require a 32-byte compiled raw key whose source representation is exactly 64 lowercase hexadecimal characters, a 64-byte raw detached signature, exact UTF-8 canonical bytes, strict supported schema/version/tag/key-id/inventory equality, all six R1 asset names, and fixed path/size/hash bounds. The authenticated offer must derive the confirmation data from verified manifest metadata and current admission evidence only, including the checked exact sum of the four ZIP sizes; it must not initiate archive acquisition. Do not add host overrides, latest-release lookup, flexible asset selection, user-provided URLs, or any network operation.</action>
  <acceptance_criteria>Every malicious fixture category named in REPR-05/06 raises a typed fail-closed validation error before a caller can acquire or extract an archive; valid fixture returns a fully typed manifest, all-six-name exact URL set, and metadata-only authenticated offer with the exact four-ZIP total.</acceptance_criteria>
  <verify><automated>python -m pytest tests/test_release_trust.py -q</automated></verify>
  <done>Production trust policy is deterministic, testable without Qt/network, and ready for a transaction consumer.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| release host→trust verifier | Untrusted downloaded bytes cross into the compiled project trust root. |
| manifest→transaction | Authenticated bytes become constrained installation metadata. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Tampering | release_trust.py | high | mitigate | Exact-byte Ed25519 verification before strict parsing; frozen accept/reject vectors. |
| T-02-02 | Tampering | manifest schema/layout | high | mitigate | Exact schema/version/tag/assets/inventory/path/hash validation with negative tests. |
| T-02-03 | Denial of service | archive metadata | high | mitigate | Bound member/count/size/path policy exported for generation extraction tests. |
| T-02-SC | Tampering | cryptography install | high | mitigate | Blocking PyPI and Windows-wheel hash verification before use. |
</threat_model>

<verification>Run the frozen verifier suite after each implementation task; retain the fixture byte hashes for the Phase 5 frozen packaged self-test.</verification>
<success_criteria>AD-19's verifier and exact release contract are implemented with a real immutable vector and exhaustive reject cases.</success_criteria>

## Artifacts this phase produces
- `ReleaseTrustVerifier`, `ReleaseManifest`, `ReleaseLayout`, and `official_release_urls` in `release_trust.py`.
- Frozen manifest, detached signature, altered manifest, and release-layout fixtures.
- `tests/test_release_trust.py` verifier/schema/release-origin contract coverage.

<output>Create `.planning/phases/02-hard-setup-signed-repair/02-01-SUMMARY.md` when done</output>
