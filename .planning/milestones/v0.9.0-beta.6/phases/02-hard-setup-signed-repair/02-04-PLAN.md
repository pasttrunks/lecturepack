---
phase: 02-hard-setup-signed-repair
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - app/ui/index.html
  - app/ui/app.js
  - app/ui/bridge.js
  - tests/test_setup_gate_repair.py
autonomous: true
requirements: [REPR-01, REPR-02, REPR-03, REPR-08, REPR-09, REPR-10]
must_haves:
  truths:
    - "SETUP_REQUIRED opens a highest-z-index, full-viewport, non-dismissible Runtime setup overlay that leaves the beta-5 app visible but inert and inaccessible (D-01, D-05; REPR-01)."
    - "Gate state shows compact Runtime needs repair copy and the canonical ordered, plain-language friendly component list (D-02, D-03); Repair all is primary while Retry/Open diagnostics/always-visible Exit are secondary (D-04), including the zero-component fallback (REPR-02)."
    - "Confirm state requires Back or one Confirm & repair click and visibly states Official LecturePack release, exact Version, What will be repaired, Download size (D-06, D-07), concise reassurance (D-10), and expandable cryptographic Technical details (D-08); it adds no checkbox/legal warning (D-09) (REPR-03)."
    - "Repair all requests only an operation-bound authenticated manifest/signature offer before confirmation; the confirmation fields come from that offer, and Confirm & repair alone allows ZIP payload acquisition, staging, activation, or admission (R3; REPR-03)."
    - "Repairing has exactly one compositor scaleX progressbar and friendly Downloading/Verifying/Installing safely/Almost there text; cancel is safe-boundary-aware; failures and offline show only their locked actions (D-11–14, D-18; REPR-08/10)."
    - "Only matching nonterminal operation events update state; matching admitted event yields ready then automatic entry after 800ms or immediately for reduced motion (D-15; REPR-09)."
    - "Diagnostics remain in overlay with friendly summary then disclosure, copy/save feedback, Back focus restoration, and sanitized values only (D-16/17; REPR-02/08)."
    - "New UI reuses beta-5 shadows, pressed effect, palette, typography, overlay/focus helpers, transitions, and motion without redesign or general flicker cleanup (D-05)."
    - "UI consideration: invoked Retry disables/announces checking while Exit remains; canonical result determines the next state."
    - "UI consideration: component list supports 0/1/many canonical rows, partial identities, friendly wrapping/ellipsis/title/accessible name, and no panel-wide horizontal scroll."
    - "UI consideration: diagnostics body/technical-value-only scrolling, narrow 820px action wrapping, 44px controls, responsive reachability, copy/save error feedback, and friendly populated report are preserved."
    - "UI consideration: focus containment blocks Escape, Tab escape, shortcuts, pointer events and scrolling behind every gate state."
    - statement: "UI consideration backstop: automated coverage proves focus/keyboard/pointer/scroll cannot reach underlying app in every gate state."
      verification: backstop
    - statement: "UI consideration backstop: visual/reduced-motion coverage proves progress and ready state remain understandable with movement disabled."
      verification: backstop
  artifacts:
    - "#runtime-setup-overlay and semantic state panels in app/ui/index.html"
    - "RuntimeSetupGate reducer, render/transition/event handlers in app/ui/app.js"
    - "repair_event bridge subscription and repair command wrappers in app/ui/bridge.js"
  key_links:
    - "get_bootstrap setup_required controls RuntimeSetupGate before normal UI activity; begin-offer metadata provides exact version/source/affected-content/four-ZIP byte total; repair_event operation IDs drive the reducer; matching Confirm & repair consumes that offer and admitted alone releases inert app state."
  prohibitions:
    - statement: "MUST NOT provide scrim/Escape dismissal, background navigation, settings/job activity, manual import, per-file repair, raw error data, or normal entry before admitted HEALTHY."
      status: resolved
      verification: judgment
      resolution: "Task 2 acceptance criteria and `python -m pytest tests/test_setup_gate_repair.py -q` prove input containment, healthy-admitted-only release, and pre-confirmation payload blocking; Plan 02-05 Task 3 records packaged evidence."
    - statement: "MUST NOT introduce a redesign, scale-on-press behavior, independent overlay/focus system, telemetry copy, or broad artifact-cleanup changes."
      status: resolved
      verification: judgment
      resolution: "Task 1/2 acceptance criteria and the UI contract suite require established beta-5 primitives, no second overlay/focus system, and no telemetry copy; Plan 02-05 Task 3 verifies the packaged UI."
---

<objective>
Authorized phase: 02 — Hard Setup & Signed Repair. Implement the locked beta-5-compatible setup gate and consume the bridge repair protocol.

Purpose: Users must be safely blocked, informed, and able to consent to repair without a restart.
Permitted files: only listed UI/test files. Required tests: reducer/DOM/bridge integration plus manual packaged UI evidence; full pytest at Wave 3 end. Non-goals: visual redesign, broad flicker cleanup, alternate repair mechanisms, or backend trust changes. Completion evidence: every reducer state, accessibility/inert behavior, stale-event rejection, and packaged gate flow pass.
</objective>

<execution_context>@C:/Users/marsh/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@.planning/phases/02-hard-setup-signed-repair/02-CONTEXT.md
@.planning/phases/02-hard-setup-signed-repair/02-UI-SPEC.md
@.planning/phases/02-hard-setup-signed-repair/02-PATTERNS.md
@app/ui/index.html
@app/ui/app.js
@app/ui/bridge.js
@tests/test_adapter_startup.py</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add semantic beta-5 setup overlay structure and bridge event surface</name>
  <files>app/ui/index.html, app/ui/bridge.js, tests/test_setup_gate_repair.py</files>
  <read_first>Read `AGENTS.md`, `docs/PRODUCT_SPEC.md`, `docs/ARCHITECTURE.md`, `docs/DECISIONS.md`, `docs/IMPLEMENTATION_PLAN.md`, `02-UI-SPEC.md` completely, the overlay/focus/progress analog locations from `02-PATTERNS.md`, `app/ui/index.html`, `app/ui/bridge.js`, bridge event names from 02-03, and existing UI test harnesses.</read_first>
  <behavior>- Overlay has dialog/modal semantics, highest z-index, inert background, visible Exit in every state, one semantic progressbar, live regions, and accessible technical disclosure.\n- All visible labels exactly follow the UI contract, concise controls gain contextual title/aria labels, and backend values are inserted as text/escaped values.\n- bridge.js subscribes to repair_event and exposes narrow begin-offer/confirm/retry/cancel/diagnostic calls.\n- Confirmation data is rendered only from the operation-bound authenticated metadata offer: exact version, official source, affected components, and exact four-ZIP byte total; no client-side size estimate is used.</behavior>
  <action>Per D-01–18 and R3, build `#runtime-setup-overlay` with state panels and controls using existing `.lp-scrim`, `.lp-pop`, `.lp-hit`, `.lp-press`/`.lp-press-sm`, `.lp-fill`, palette variables, spacing, typography, and hard-shadow/embedded-press rules. Include technical disclosure, sanitized diagnostics report region, one progressbar, assertive/polite live regions, 44px actions, responsive 820px layout, title/accessible name support, and no dismiss X/scrim close. Extend the synchronized bridge signal list with `repair_event`; expose specific begin-offer and matching confirm wrappers so Repair all gets metadata only and Confirm & repair starts payload repair. Render exact version/source/affected-content/download-size fields from bridge-supplied authenticated offer data; do not calculate from Content-Length or permit a confirmation without a valid offer. Do not add CSS tokens, external libraries, `innerHTML` interpolation for backend metadata, width animation for progress, or background controls.</action>
  <acceptance_criteria>Structural tests find every required state/action/ARIA binding and verify one progressbar, no dismiss affordance, escaped backend fields, authenticated offer field bindings, disabled confirmation without a valid offer, and bridge metadata/repair event subscriptions.</acceptance_criteria>
  <verify><automated>python -m pytest tests/test_setup_gate_repair.py -q</automated></verify>
  <done>Markup and bridge expose the entire accessible gate surface using established beta-5 primitives.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic gate reducer, input containment, and repair transitions</name>
  <files>app/ui/app.js, tests/test_setup_gate_repair.py</files>
  <read_first>Read `AGENTS.md`, `docs/PRODUCT_SPEC.md`, `docs/ARCHITECTURE.md`, `docs/DECISIONS.md`, `docs/IMPLEMENTATION_PLAN.md`, Task 1 structure/tests, `app/ui/app.js` bootstrap, `topOverlay`, `focusFirst`, `trapFocus`, `LP.motion`, `esc`, and keyboard/overlay handlers; then reread UI-SPEC state table and accessibility sections.</read_first>
  <behavior>- Reducer permits only `gate|diagnostics|confirm|repairing|offline|failed|ready` transitions and rejects stale operation IDs, duplicate terminal events, events after terminal, and metadata-ready events that fail signature/schema/version/key/inventory/size validation.\n- Repair all starts metadata-only offer acquisition; `confirm` requires matching authenticated offer fields, and only Confirm & repair passes its operation/offer ID to start archive work.\n- Bootstrap reads `runtime_health_state`/`setup_required` before normal activation; healthy only follows matching admitted repair.\n- Escape, normal shortcuts, focus/pointer/scroll behind the gate, repeated commands, and early ready dismissal are blocked.\n- Reduced motion removes ready delay/custom motion while preserving understandable static progress/status and focus restoration.</behavior>
  <action>Implement a single `RuntimeSetupGate` reducer/render controller that calls existing `topOverlay`, `focusFirst`, `trapFocus`, `LP.motion`, `LP.motion.close`, and `esc` rather than duplicating them. On Repair all begin the metadata-only offer operation and enter confirm only from its matching authenticated metadata-ready event; render exact version/source/affected-content/four-ZIP total, and invoke the matching confirm bridge slot only on Confirm & repair. Clear/invalidate UI offer state on Back, cancel, failed/offline terminal result, stale operation replacement, or a new admission result; never infer or change download bytes client-side. Render every other locked state/copy/action from the approved state machine, preserve `returnState`/invoker focus for diagnostics, make Retry assessment-pending only, and require matching `admitted` before ready dismissal. Map failed/offline/cancel/retry/progress events exactly; disable repeat cancel during safe activation; use native inert with tested fallback. Keep technical facts collapsible and sanitized, status changes announced, underlying app inert until ready dismissal, and no client retry loop.</action>
  <acceptance_criteria>Tests cover every reducer transition, metadata-only request then matching confirmation ordering, exact offer-field rendering, no ZIP/payload bridge call before confirm, invalid/stale/cancelled offer suppression, 0/1/many/long component rendering, retry/cancel boundary, offline-only controls, diagnostics copy/save errors, stale event suppression, no-entry-before-admitted, focus/keyboard/pointer/scroll containment, responsive selectors, and reduced-motion behavior.</acceptance_criteria>
  <verify><automated>python -m pytest tests/test_setup_gate_repair.py -q</automated></verify>
  <done>The UI deterministically gates normal entry and presents the complete consent/repair/recovery experience without abandoning beta-5 interaction conventions.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| backend JSON→WebEngine DOM | Repair status, diagnostics, and component metadata reach a user-facing renderer. |
| user action→bridge | UI requests can initiate/cancel repair or control application flow. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-11 | Elevation of privilege | gate reducer | high | mitigate | Non-dismissible highest overlay, inert fallback, guarded bootstrap and keyboard/pointer tests. |
| T-02-12 | Tampering | repair events | high | mitigate | Operation-ID/terminal-state reducer tests reject stale/out-of-order events. |
| T-02-13 | Information disclosure | diagnostics DOM | high | mitigate | Text/esc rendering and sanitization contract; copy/save test coverage. |
| T-02-14 | Availability | UI cancel/ready | high | mitigate | Safe-boundary cancel state, no progress-inferred success, matching admitted-only dismissal. |
</threat_model>
<verification>Run the UI integration suite after each task; at Wave 3 end run `python -m pytest -q`, then capture manual packaged evidence across both themes, narrow DPI, and reduced motion.</verification>
<success_criteria>Every unhealthy admission state is safely and accessibly blocked behind the approved familiar gate until a verified repair is admitted.</success_criteria>

## Artifacts this phase produces
- `#runtime-setup-overlay`, semantic state panels, progress/live/disclosure/inert elements and selectors.
- `RuntimeSetupGate` reducer, operation-ID transition handlers, render/focus/input-containment behavior.
- `repair_event` bridge subscription and narrowly scoped repair/retry/cancel/diagnostics commands.
- `tests/test_setup_gate_repair.py` reducer, DOM, accessibility, stale-event, and bridge integration coverage.

<output>Create `.planning/phases/02-hard-setup-signed-repair/02-04-SUMMARY.md` when done</output>
