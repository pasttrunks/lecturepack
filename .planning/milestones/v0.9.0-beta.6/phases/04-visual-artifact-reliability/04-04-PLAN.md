---
phase: 04-visual-artifact-reliability
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - docs/HANDOFF_PHASE_4.md
autonomous: false
requirements: [VIS-01, VIS-02, VIS-03, VIS-04, VIS-05]
must_haves:
  truths:
    - statement: "Packaged QtWebEngine first-frame theme, responsive/DPI/tour/gate interaction, and console cleanliness are evidenced on physical Windows rendering, not inferred from DOM tests."
      verification: "Blocking human verification checklist plus captured packaged evidence"
    - statement: "The approved beta-5 visual language remains intact while reported artifacts are absent."
      verification: "Side-by-side screenshot/video comparison against approved beta-5 baseline"
  artifacts:
    - "docs/HANDOFF_PHASE_4.md"
  key_links:
    - "Focused suite and full pytest output -> packaged visual matrix -> honest Phase 4 handoff"
  prohibitions:
    - statement: "MUST NOT silently redesign, flatten, or remove the approved beta-5 hard-shadow and embedded-press visual identity while repairing visual reliability."
      category: values
      status: unresolved
      verification: null
      resolution: flagged-unverified
      reason: "This is the retained bespoke product prohibition; explicit human visual judgment is required before Phase 5 approval."
---

<objective>
Collect the physical packaged evidence that completes Phase 4 and record the exact results without claiming unsupported visual behavior.

Purpose: distinguish reliable automated guards from Windows compositor/QtWebEngine perceptual evidence before Phase 5.
Output: full test output and an honest Phase 4 handoff with visual matrix evidence or explicit remaining blockers.
</objective>

<execution_context>
@C:/Users/marsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/marsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-visual-artifact-reliability/04-CONTEXT.md
@.planning/phases/04-visual-artifact-reliability/04-UI-SPEC.md
@.planning/phases/04-visual-artifact-reliability/04-VALIDATION.md
@.planning/phases/04-visual-artifact-reliability/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run final automated reliability suites and prepare evidence matrix</name>
  <files>docs/HANDOFF_PHASE_4.md</files>
  <action>Run the focused three-file UI suite and the complete `python -m pytest -q` suite after all implementation is integrated. Create the Phase 4 handoff with literal command output, environment/package identity, automated coverage per VIS-01 through VIS-05, the physical matrix to execute, and any failure/blocker; do not claim manual/packaged results before they exist. Include the absence of new dependencies and confirm Phase 4 did not alter source-derived versus AI-generated data boundaries. If an unforeseen consequential decision is required, stop for user approval rather than expanding the declared Phase 4 file scope.</action>
  <verify><automated>python -m pytest -q</automated></verify>
  <done>Handoff contains actual focused/full pytest output and a traceable evidence matrix, or clearly states any automated failure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify packaged visual reliability and preservation</name>
  <files>docs/HANDOFF_PHASE_4.md</files>
  <action>Perform and record the required packaged QtWebEngine visual matrix after the automated suite passes; this approval gate supplies the perceptual and physical evidence that cannot be inferred from source or DOM checks.</action>
  <what-built>Phase 4 visual reliability fixes with automated coverage for theme, navigation, tooltip, responsive layout, and tour/gate behavior.</what-built>
  <how-to-verify>
    1. Launch the packaged application repeatedly with a fresh profile and a saved Dark profile; capture the first visible frame and confirm Light/default or saved Dark appears complete without a flash.
    2. In both themes, compare Home, Settings, buttons, pressed states, hard shadows, typography, and navigation entrance against approved beta-5 screenshots/video. Confirm no flattening, redesigned palette, or new motion vocabulary.
    3. Click the active nav item; trigger backend progress/log/settings changes; change options; close gate, tour, dialog, and dropdown. Confirm the underlying page remains stationary and entrance motion only follows a real page change.
    4. At normal, 1220px, 820px, 640px, and a very-small window at 100%, 125%, and 150% Windows scale, inspect Home, Settings, gate, and every tour phase. Confirm vertical access to every required action, no horizontal page scroll/clipping, correct long-model tooltip with mouse and keyboard, and no console errors.
    5. Resize, scroll, and move between DPI-scaled monitors during a tour/gate. Confirm the real target is minimally revealed, spotlight/card stay in viewport, focus reaches highlighted action/tour controls/Exit only, and no fade, step restart, or page replay occurs.
  </how-to-verify>
  <verify><automated>python -m pytest -q tests/test_ui_tokens_motion_responsive.py tests/test_webview_theme.py tests/test_guided_tour.py</automated></verify>
  <done>Package-level observations, screenshots/video locations, scaling matrix, and any defect are recorded in the Phase 4 handoff before approval.</done>
  <resume-signal>Type "approved" with evidence locations, or describe the observed defect/blocker.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packaged QtWebEngine compositor -> user-visible result | First-frame, DPI, and native focus behavior require physical confirmation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-04-10 | Repudiation | visual/release evidence | medium | mitigate | Handoff records literal commands, outputs, environment/package identity, and evidence locations. |
| T-04-11 | Elevation of Privilege | packaged gate/tour focus | medium | mitigate | Blocking physical check confirms focus and pointer restrictions in the actual compositor. |
| T-04-12 | Denial of Service | packaged resize/DPI behavior | low | accept | Automated console/geometry checks plus physical DPI matrix detect failures before Phase 5; no remote service is exposed. |
| T-04-SC | Tampering | package installs | high | mitigate | No package-manager install is allowed in this phase. |
</threat_model>

<verification>
Automated: `python -m pytest -q tests/test_ui_tokens_motion_responsive.py tests/test_webview_theme.py tests/test_guided_tour.py` and `python -m pytest -q`. Human: blocking packaged first-frame, beta-5 comparison, viewport/DPI, focus, and console matrix above.
</verification>

<success_criteria>
Every VIS requirement has passing automated evidence plus recorded packaged/physical evidence, and the handoff honestly records any remaining approval-gate issue.
</success_criteria>

<output>
Create `.planning/phases/04-visual-artifact-reliability/04-04-SUMMARY.md` when done.
</output>
