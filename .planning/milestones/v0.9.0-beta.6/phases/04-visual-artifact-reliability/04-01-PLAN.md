---
phase: 04-visual-artifact-reliability
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [app/desktop/main.py, app/desktop/bridge.py, app/ui/index.html, app/ui/app.js, app/ui/app.css, tests/test_ui_tokens_motion_responsive.py, tests/test_webview_theme.py]
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03]
must_haves:
  truths:
    - "Fresh/saved theme is resolved before the first visible frame and user switching is atomic and immediately persistent."
    - "Only navigation to a different page runs the established entrance animation; dynamic and overlay updates remain in place."
    - "D-02: Clicking the navigation control for the page already open does not replay its entrance animation."
    - "D-03: Closing the setup gate, guided-tour surface, dialog, dropdown, or another overlay leaves the underlying page in place without entrance replay or layout jump."
    - statement: "Approved beta-5 hard shadows, embedded press effects, timing, palette, typography, and motion character remain unchanged."
      verification: "Plan 04 packaged beta-5 comparison"
  artifacts: [app/desktop/main.py, app/desktop/bridge.py, app/ui/app.js, tests/test_webview_theme.py]
  key_links: ["main.py pre-visible readiness -> bridge QSettings theme -> #app[data-theme]", "changed-target setScreen guard -> LP.motion.nav -> existing lprail entrance"]
  prohibitions:
    - statement: "MUST NOT silently redesign, flatten, or remove the approved beta-5 hard-shadow and embedded-press visual identity while repairing visual reliability."
      category: values
      status: unresolved
      verification: null
      resolution: flagged-unverified
      reason: "Perceptual preservation remains an explicit Plan 04 human judgment gate; no descriptor is fabricated."
---
<objective>Deliver green vertical TDD slices for first-frame theme reliability and navigation-only entrance ownership.</objective>
<execution_context>@C:/Users/marsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/marsh/.codex/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@.planning/phases/04-visual-artifact-reliability/04-CONTEXT.md
@.planning/phases/04-visual-artifact-reliability/04-RESEARCH.md
@.planning/phases/04-visual-artifact-reliability/04-VALIDATION.md
<interfaces>main.py owns page-load/show ordering; bridge.py `get_bootstrap()` owns the canonical QSettings default; app.js `setTheme` and `setScreen` own root palette and entrance behavior.</interfaces></context>

<decision_coverage>D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, and D-17 are implemented by this plan; D-10 through D-16 and D-18 are implemented by Plan 02.</decision_coverage>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Ship pre-visible Light/saved-theme transaction</name><files>app/desktop/main.py, app/desktop/bridge.py, app/ui/index.html, app/ui/app.js, app/ui/app.css, tests/test_webview_theme.py</files><behavior>- Fresh profile resolves Light before `win.show()`.
- Saved Light/Dark is injected into the root before visibility.
- User action changes one root palette transaction and persists one value; duplicate acknowledgement is a no-op.</behavior><action>First add the VIS-02 tests and run them locally to demonstrate RED; within the same task implement the existing-owner fix, then rerun until GREEN before committing. Use main.py as the strictly necessary pre-visible readiness owner: obtain/inject the canonical saved-or-Light theme and delay `win.show()` until the root is correct. Change bridge QSettings fallback to Light per D-05/D-06; retain root custom-property ownership and idempotent apply-versus-user-persist behavior per D-07, D-08, and D-09. Do not add a theme architecture, palette tween, crossfade, or visual redesign.</action><verify><automated>python -m pytest -q tests/test_ui_tokens_motion_responsive.py tests/test_webview_theme.py tests/test_guided_tour.py</automated></verify><done>RED→GREEN happens within this task and its commit is green; VIS-02 first-frame, atomic overlay, and persistence tests pass.</done></task>
<task type="auto" tdd="true"><name>Task 2: Ship idempotent navigation and in-place rendering</name><files>app/ui/app.js, app/ui/app.css, tests/test_ui_tokens_motion_responsive.py</files><behavior>- Changed-page user navigation alone emits the existing entrance event.
- Active-nav click, backend updates, option changes, and overlay closure do not replay entrance or jump layout.</behavior><action>First add and run deterministic VIS-01/VIS-03 animation-event and DOM/reducer assertions to show RED; implement the changed-target guard in setScreen and targeted updates/close behavior in the same task; rerun GREEN before committing. Apply D-01 through D-04 exactly, preserve beta-5 tokens/press/shadow vocabulary per D-17, and do not replace/reclass current page roots for live state.</action><verify><automated>python -m pytest -q tests/test_ui_tokens_motion_responsive.py tests/test_webview_theme.py tests/test_guided_tour.py</automated></verify><done>RED→GREEN happens within this task and its commit is green; VIS-01/VIS-03 regressions are covered without changing approved visual character.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| QSettings/desktop bridge -> initial DOM | Persisted preference becomes visible root state. |
| backend events -> UI DOM | Dynamic values update local surfaces. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-04 | Tampering | theme bootstrap/settings | medium | mitigate | Test root-only, pre-visible, idempotent application. |
| T-04-05 | Tampering | live DOM rendering | medium | mitigate | Keep targeted text-only updates. |
| T-04-06 | Denial of Service | repeated UI events | low | accept | Idempotent guards bound redundant work. |
| T-04-SC | Tampering | package installs | high | mitigate | No package-manager install is allowed. |
</threat_model>
<verification>Every task runs its local RED test before implementation, then the focused suite GREEN before its commit; run `python -m pytest -q` after this wave.</verification>
<success_criteria>VIS-01 through VIS-03 are implemented and every implementation commit is passing.</success_criteria>
<output>Create `.planning/phases/04-visual-artifact-reliability/04-01-SUMMARY.md` when done.</output>
