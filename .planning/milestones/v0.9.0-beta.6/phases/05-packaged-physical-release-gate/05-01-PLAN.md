---
phase: 05-packaged-physical-release-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_runtime_packaged_smoke.py
  - packaging/build.py
requirements: [REL-01, REL-02, REL-03]
must_haves:
  truths:
    - statement: "The release package proves its canonical inventory, approved manifest contract, and lack of bundled user/job/demo state."
      verification: "python packaging/build.py --no-installer"
    - statement: "Disposable packaged profile runs bundled FFmpeg, ffprobe, Whisper CLI/DLL set independent of developer environment."
      verification: "python -m pytest tests/test_runtime_packaged_smoke.py"
---

<objective>
Execute Phase 5 Wave 1: Package inventory/manifest build checks and disposable-profile packaged runtime smoke validation.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Verify package build clean-state gate and canonical CPU runtime inventory</name>
  <files>packaging/build.py</files>
  <action>Run the PyInstaller build script with --no-installer. Verify clean-state gate checks pass (no job/dev data bundled, engine payload present, portable zip and SHA256 checksums generated).</action>
  <verify><automated>python packaging/build.py --no-installer</automated></verify>
  <done>Build completes cleanly with validated release artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Run disposable packaged runtime smoke test suite</name>
  <files>tests/test_runtime_packaged_smoke.py</files>
  <action>Execute tests/test_runtime_packaged_smoke.py against the compiled onedir fixture. Verify bundled FFmpeg, ffprobe, Whisper CLI/DLL set, and model load execute cleanly on a fresh profile.</action>
  <verify><automated>python -m pytest tests/test_runtime_packaged_smoke.py -v</automated></verify>
  <done>Packaged smoke tests pass with complete command vectors, exit codes, and stdout/stderr evidence recorded.</done>
</task>
</tasks>
