---
phase: 04-visual-artifact-reliability
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/test_ui_tokens_motion_responsive.py
  - tests/test_webview_theme.py
  - tests/test_guided_tour.py
  - .planning/phases/04-visual-artifact-reliability/04-VALIDATION.md
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03, VIS-04, VIS-05]
must_haves:
  truths:
    - statement: "The focused suite can fail independently for each Phase 4 reliability contract before production changes are made."
      verification: "python -m pytest -q tests/test_ui_tokens_motion_responsive.py tests/test_webview_theme.py tests/test_guided_tour.py"
    - statement: "The automated harness records navigation entrance events, root-theme startup/persistence ordering, tooltip interaction, responsive reachability, tour focus/geometry, and WebEngine console output."
      verification: "Focused pytest suite"
  artifacts:
    - "tests/test_ui_tokens_motion_responsive.py"
    - "tests/test_webview_theme.py"
    - "tests/test_guided_tour.py"
  key_links:
    - "UI source seams -> deterministic DOM/reducer and QtWebEngine assertions"
  prohibitions:
    - statement: "MUST NOT silently redesign, flatten, or remove the approved beta-5 hard-shadow and embedded-press visual identity while repairing visual reliability."
      category: values
      status: unresolved
      verification: null
      resolution: flagged-unverified
      reason: "Perceptual preservation requires the beta-5 comparison gate; this bespoke product constraint is intentionally flagged for human judgment, with no routine or canon security prohibition minted."
---

<objective>
Create the Wave 0 executable regression seams required to prove Phase 4 reliability before implementation.

Purpose: make each visual defect class fail deterministically while preserving the existing pytest/Node/QtWebEngine test architecture.
Output: explicit VIS-01..VIS-05 coverage in the three existing UI test files.
</objective>

<execution_context>
@C:/Users/marsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/marsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-visual-artifact-reliability/04-CONTEXT.md
@.planning/phases/04-visual-artifact-reliability/04-UI-SPEC.md
@.planning/phases/04-visual-artifact-reliability/04-VALIDATION.md
@.planning/phases/04-visual-artifact-reliability/04-PATTERNS.md
@.planning/phases/03-empty-launch-guided-demo/03-03-SUMMARY.md
@.planning/phases/03-empty-launch-guided-demo/03-VERIFICATION.md
</context>

<source_audit>
| Source | Items audited | Coverage |
|--------|---------------|----------|
| GOAL | Visual artifacts, first-frame theme/repaint, entrance replay, long-value overflow, responsive/DPI/tour/gate defects, preservation | Plans 01-04 |
| REQ | VIS-01 | Plans 01, 02, 04 |
| REQ | VIS-02 | Plans 01, 02, 04 |
| REQ | VIS-03 | Plans 01, 02, 04 |
| REQ | VIS-04 | Plans 01, 03, 04 |
| REQ | VIS-05 | Plans 01, 03, 04 |
| RESEARCH | Existing HTML/CSS/JS and PySide6 stack only; root theme transaction; rAF/ResizeObserver guard; CSS spotlight; no third-party tooltip | Plans 01-03 |
| CONTEXT | D-01 through D-18 | Plans 01-03, with Phase 4 evidence in Plan 04 |

No in-scope source item is unplanned. The edge probe is resolved by explicit test cases for VIS-02/VIS-03 idempotency and concurrency, VIS-04 empty plus encoding/grapheme values, and structured physical backstops for VIS-01/VIS-05.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add motion, navigation, and responsive regression seams</name>
  <files>tests/test_ui_tokens_motion_responsive.py</files>
  <behavior>
    - Assert the approved beta-5 timing tokens, hard-shadow variables, press classes, and transform-based press behavior remain present.
    - Execute a deterministic DOM/reducer seam which observes page-root animationstart and proves only navigation to a different screen may emit an entrance event.
    - Cover active-navigation click, backend status/progress updates, option changes, and overlay closure as idempotent in-place paths.
    - Exercise representative desktop, 1220px, 820px, 640px, and very-small viewport fixtures for no horizontal page overflow and required-action reachability.
  </behavior>
  <action>Extend the established source-loaded/Node harness in this one test file with failing VIS-01 and VIS-03 assertions. Name the event recorder and viewport fixture helpers so later source changes can be demonstrated against real page roots rather than string presence alone. Assert preservation per D-01 through D-04 and D-17, including no active-screen navigation work; add the responsive no-horizontal-overflow/reachable-control assertions required by D-11 and D-12. Keep existing motion vocabulary assertions as preservation checks, not a new styling specification.</action>
  <verify><automated>python -m pytest -q tests/test_ui_tokens_motion_responsive.py tests/test_webview_theme.py tests/test_guided_tour.py</automated></verify>
  <done>Focused tests contain explicit VIS-01/VIS-03 and responsive fixture failures that cannot pass until navigation and layout reliability are implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add pre-visible theme transaction coverage</name>
  <files>tests/test_webview_theme.py</files>
  <behavior>
    - A fresh profile yields Light before the desktop view becomes visible.
    - A saved theme is applied before first paint.
    - A user action makes one root palette mutation, immediately persists exactly one theme value, and does not require global Save.
    - Open gate, dialog, and tour surfaces receive the same palette transaction as their underlying page.
  </behavior>
  <action>Extend the existing bridge/WebEngine-oriented test file with failing VIS-02 coverage for bootstrap ordering and idempotent user persistence. Use a fake QSettings/bridge seam plus DOM assertions to distinguish pre-visible bootstrap from a later settings event. Assert root custom-property ownership and absence of palette-tween/crossfade behavior per D-05 through D-09; include settings-acknowledgement re-entry/idempotency cases from the edge probe so duplicate/concurrent notifications cannot produce a second visible transaction.</action>
  <verify><automated>python -m pytest -q tests/test_ui_tokens_motion_responsive.py tests/test_webview_theme.py tests/test_guided_tour.py</automated></verify>
  <done>Focused tests fail until fresh/saved first-frame themes, immediate persistence, atomic overlays, and duplicate acknowledgement behavior satisfy VIS-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add tooltip, tour, focus, DPI, and console-error coverage</name>
  <files>tests/test_guided_tour.py, .planning/phases/04-visual-artifact-reliability/04-VALIDATION.md</files>
  <behavior>
    - Hover and keyboard focus reveal the exact long model name via an anchored ARIA-related tooltip without resizing its field; leave/blur hides it.
    - Tooltip/card/spotlight geometry stays within the current viewport after resize, scroll, target reveal, and DPI/layout changes.
    - Setup gate remains modal; tour tab order includes highlighted real action and controls, always reaches Exit, and excludes dimmed unrelated controls.
    - Representative WebEngine scenarios collect no console errors.
  </behavior>
  <action>Extend the existing Node reducer and QtWebEngine fixture rather than adding a test framework. Add failing VIS-04/VIS-05 tests for exact textContent, aria-describedby, hover/focus lifecycle, grapheme/encoding-bearing model values, collision bounds, and no source-field reflow per D-10. Add a viewport/DPI helper that records geometry after target minimum-reveal and rAF scheduling, checks focus containment and console capture for gate/tour/theme scenarios, and asserts the CSS/real-control spotlight contract per D-13 through D-16 and D-18. Cover the 1360x860, 1220px, 820px, 640px, and 480x560 matrix plus continuous arbitrary resize assertions, including the `app/desktop/main.py` pre-visible-theme and minimum-size seam. Treat empty model values as a harmless hidden-tooltip state, not an exception. Only after every Wave 0 seam passes, update 04-VALIDATION.md to set `wave_0_complete: true` and `nyquist_compliant: true`; leave both false if any seam is still missing or failing.</action>
  <verify><automated>python -m pytest -q tests/test_ui_tokens_motion_responsive.py tests/test_webview_theme.py tests/test_guided_tour.py</automated></verify>
  <done>Wave 0 covers every item listed in 04-VALIDATION.md, including the previously missing interaction, WebEngine, DPI, focus, and console seams.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend/bridge -> UI DOM | Model and settings values cross into rendered UI text. |
| modal/tour overlay -> real controls | Keyboard and pointer input must be limited to intended interactive controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | model-name rendering | medium | mitigate | Wave 0 tests require text-only model display/tooltip behavior; implementation uses textContent, never interpolated HTML. |
| T-04-02 | Elevation of Privilege | setup gate/tour focus routing | medium | mitigate | Wave 0 locks modal/tour allowed-focus assertions and real-control-only interaction. |
| T-04-03 | Denial of Service | resize/observer geometry path | low | accept | Fixture captures console errors and later implementation coalesces updates; local layout-only input has limited impact. |
| T-04-SC | Tampering | package installs | high | mitigate | No package-manager install is allowed in this phase; no new dependency is planned. |
</threat_model>

<verification>
Run the focused three-file suite after every task. Wave 0 is complete only when every 04-VALIDATION.md gap has a substantive failing-then-passing seam; run `python -m pytest -q` after the wave.
</verification>

<success_criteria>
The existing UI tests—not a parallel framework—can detect failures for VIS-01 through VIS-05 before any Phase 4 production implementation lands.
</success_criteria>

<output>
Create `.planning/phases/04-visual-artifact-reliability/04-01-SUMMARY.md` when done.
</output>
