---
phase: 04-visual-artifact-reliability
plan: 03
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - app/desktop/main.py
  - app/ui/index.html
  - app/ui/app.css
  - app/ui/app.js
autonomous: true
requirements: [VIS-04, VIS-05]
must_haves:
  truths:
    - "A long local-model name stays one-line ellipsized yet exposes its exact full text on pointer hover and keyboard focus."
    - "Every required action remains reachable through stacking and vertical scrolling at supported small windows without horizontal page scroll."
    - "Tour/gate focus, spotlight/card geometry, and DPI/resize behavior stay coherent without console errors, page replay, or an SVG mask."
  artifacts:
    - "app/desktop/main.py"
    - "app/ui/index.html"
    - "app/ui/app.css"
    - "app/ui/app.js"
  key_links:
    - "model display -> shared ARIA tooltip -> rAF viewport clamp"
    - "resize/scroll/DPI/layout events -> coalesced live target measurement -> CSS spotlight/card"
  prohibitions:
    - statement: "MUST NOT silently redesign, flatten, or remove the approved beta-5 hard-shadow and embedded-press visual identity while repairing visual reliability."
      category: values
      status: unresolved
      verification: null
      resolution: flagged-unverified
      reason: "Human visual comparison remains required because this bespoke preservation constraint is perceptual."
---

<objective>
Make long values, small layouts, tour geometry, and modal focus reliable while retaining the Phase 3 CSS spotlight and beta-5 interaction language.

Purpose: eliminate overflow, stale geometry, focus leakage, and tooltip accessibility defects without expanding product scope.
Output: responsive/tooltip/overlay reliability in the existing static shell.
</objective>

<execution_context>
@C:/Users/marsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/marsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-visual-artifact-reliability/04-CONTEXT.md
@.planning/phases/04-visual-artifact-reliability/04-UI-SPEC.md
@.planning/phases/04-visual-artifact-reliability/04-RESEARCH.md
@.planning/phases/04-visual-artifact-reliability/04-PATTERNS.md
@.planning/phases/04-visual-artifact-reliability/04-VALIDATION.md
@.planning/phases/03-empty-launch-guided-demo/03-03-SUMMARY.md
<interfaces>
From app/ui/app.js: `positionTourSpotlight()` measures the real target; `trapFocus(scope, e)` is the existing scoped modal primitive; `renderGuidedTour()` owns the Phase 3 overlay.
From app/ui/index.html: `#ai-model-name`, `#guided-tour-overlay`, `#tour-spotlight-box`, `#guided-tour-card`, and `#btn-tour-exit` are existing semantic targets.
From app/desktop/main.py: `MainWindow.setMinimumSize(1080,680)` currently blocks D-12's required very-small physical window state and must be relaxed or removed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add non-reflowing accessible model-value tooltip and narrow layout rules</name>
  <files>app/desktop/main.py, app/ui/index.html, app/ui/app.css, app/ui/app.js</files>
  <behavior>
    - Long/encoded/grapheme model names render as single-line ellipsis and retain exact tooltip text on hover/focus.
    - Tooltip is anchored, collision-clamped, ARIA-related, and hidden after hover/focus leaves without changing field dimensions.
    - At normal, breakpoint, and very-small widths secondary controls stack and vertical scroll exposes required actions with no horizontal scroll.
  </behavior>
  <action>Relax or remove `MainWindow.setMinimumSize(1080,680)` in `app/desktop/main.py`, because retaining it makes D-12 and the 480x560 physical test state impossible; no replacement Phase-4 minimum may be imposed. Add only the semantic hook(s), one shared tooltip surface, and existing-system CSS/JS needed for the model display. Use textContent for tooltip population and preserve the source field with `min-width:0`, nowrap, hidden overflow, and ellipsis; position outside normal flow in a requestAnimationFrame read/write pass. Support pointer hover and keyboard focus/blur with viewport collision adjustment per D-10, including empty and encoding/grapheme cases from the edge probe. Extend responsive rules at existing breakpoints and very-small windows so secondary controls stack and page containers use vertical scrolling while every required action remains reachable, per D-11 and D-12. Preserve spacing, token, shadow, and press contracts from D-17; do not introduce a dependency or another component system.</action>
  <verify><automated>python -m pytest -q tests/test_ui_tokens_motion_responsive.py tests/test_webview_theme.py tests/test_guided_tour.py</automated></verify>
  <done>VIS-04 and responsive reachability tests pass for long values, focus/hover tooltip behavior, narrow layouts, and no horizontal page overflow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Coalesce live tour geometry and scope tour focus</name>
  <files>app/ui/app.js, app/ui/app.css, app/ui/index.html</files>
  <behavior>
    - Resize, scroll, monitor/DPI/layout changes schedule one live remeasure before paint; target is minimally revealed then spotlight/card are clamped inside viewport.
    - The open setup gate remains modal; an active tour cycles among its highlighted real action and card controls, with Exit reachable and unrelated controls excluded.
    - Geometry movement neither fades/restarts tour progress nor requests page entrance; overlay remains pointer-transparent except existing real targets/card.
  </behavior>
  <action>Replace direct geometry writes with one requestAnimationFrame-coalesced scheduler used by resize, capture-scroll, visual viewport/layout observers where available, and tour rendering. In its callback reveal an off-screen real target by the minimum nearest scroll per D-14, then remeasure and clamp the complete spotlight rectangle, arrow, and card against current viewport edges during resize per D-13 and DPI/layout changes per D-15. Avoid observing and resizing the same element in a feedback loop; preserve console-clean behavior. Scope keyboard focus handling separately for the runtime setup gate and active guided step: enumerate only the highlighted real control plus tour card controls, keep `#btn-tour-exit` reachable, and block focus leakage per D-16. Retain the Phase 3 pointer-transparent CSS box-shadow spotlight and real-control interaction per D-18; no SVG mask or synthetic hit layer is permitted.</action>
  <verify><automated>python -m pytest -q tests/test_ui_tokens_motion_responsive.py tests/test_webview_theme.py tests/test_guided_tour.py</automated></verify>
  <done>VIS-05 automated scenarios pass for target reveal, resize/scroll/DPI geometry, focus containment, reachable Exit, viewport clamping, and empty WebEngine console-error capture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend model value -> tooltip text | Potentially arbitrary local text must remain inert DOM text. |
| overlay focus/pointer policy -> application controls | Dimmed controls must not receive interaction while policy says they are unavailable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Tampering | model tooltip | medium | mitigate | Populate tooltip with textContent and retain ARIA/geometry tests for arbitrary local names. |
| T-04-08 | Elevation of Privilege | gate/tour interaction | medium | mitigate | Scoped focus allowed sets plus pointer-transparent overlay preserve only intended real-control interaction. |
| T-04-09 | Denial of Service | resize/observer scheduler | medium | mitigate | Coalesce events into one rAF callback and avoid ResizeObserver write loops; focused fixture captures console errors. |
| T-04-SC | Tampering | package installs | high | mitigate | No package-manager install is allowed in this phase. |
</threat_model>

<verification>
Run the focused UI suite after each task, then `python -m pytest -q` after Wave 2. Record failures honestly; do not weaken coverage or substitute mock-only evidence for packaged QtWebEngine behavior.
</verification>

<success_criteria>
VIS-04 and VIS-05 work across tested sizes/DPI in the existing QtWebEngine-safe UI architecture with all required actions accessible.
</success_criteria>

<output>
Create `.planning/phases/04-visual-artifact-reliability/04-03-SUMMARY.md` when done.
</output>
