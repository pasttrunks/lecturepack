---
phase: 04-visual-artifact-reliability
plan: 02
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - app/desktop/main.py
  - app/desktop/bridge.py
  - app/ui/index.html
  - app/ui/app.js
  - app/ui/app.css
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03]
must_haves:
  truths:
    - "Fresh and saved profiles present their resolved theme before the first visible WebEngine frame."
    - "Only a real navigation to a different page may run the established page entrance; all state and overlay paths update in place."
    - statement: "Approved beta-5 palette, hard shadows, pressed movement, typography, and animation timing remain perceptually unchanged after reliability fixes."
      verification: "Beta-5 screenshot/video comparison in Plan 04"
  artifacts:
    - "app/desktop/main.py"
    - "app/desktop/bridge.py"
    - "app/ui/app.js"
    - "app/ui/index.html"
    - "app/ui/app.css"
  key_links:
    - "QSettings bootstrap -> pre-visible root data-theme -> complete CSS custom-property palette"
    - "setScreen changed-target guard -> existing LP.motion.nav -> page entrance selector"
  prohibitions:
    - statement: "MUST NOT silently redesign, flatten, or remove the approved beta-5 hard-shadow and embedded-press visual identity while repairing visual reliability."
      category: values
      status: unresolved
      verification: null
      resolution: flagged-unverified
      reason: "Flagged for the required human beta-5 visual comparison; no descriptor is fabricated in fallback auto mode."
---

<objective>
Stabilize first-frame theme ownership and page-animation ownership without changing the approved visual language.

Purpose: remove theme flash/repaint and event-triggered entrance replay at their existing desktop, root, and navigation owners.
Output: atomic theme lifecycle and idempotent navigation/rendering behavior.
</objective>

<execution_context>
@C:/Users/marsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/marsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-visual-artifact-reliability/04-CONTEXT.md
@.planning/phases/04-visual-artifact-reliability/04-UI-SPEC.md
@.planning/phases/04-visual-artifact-reliability/04-RESEARCH.md
@.planning/phases/04-visual-artifact-reliability/04-PATTERNS.md
@.planning/phases/04-visual-artifact-reliability/04-01-SUMMARY.md
<interfaces>
From app/desktop/main.py: the current page-load then immediate `win.show()` lifecycle is the strictly necessary pre-visible readiness owner.
From app/desktop/bridge.py: `get_bootstrap() -> str` supplies the canonical QSettings theme; `set_setting(key, value)` is the existing persistence route.
From app/ui/app.js: `setScreen(name)` owns screen visibility and `LP.motion.nav`; `setTheme(theme)` owns `#app[data-theme]` and theme control labels.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve and apply the bootstrap theme before visibility</name>
  <files>app/desktop/main.py, app/desktop/bridge.py, app/ui/index.html, app/ui/app.js, app/ui/app.css</files>
  <behavior>
    - Fresh QSettings resolves Light, while a valid saved selection resolves before the first visible frame.
    - Startup applies a theme without writing it back; a user control persists once immediately.
    - Gate, dialog, guided tour, and page inherit the same root palette atomically.
  </behavior>
  <action>Use `app/desktop/main.py` as the strictly necessary owner to obtain/inject the canonical saved-or-Light root theme and hold `win.show()` until that root theme is ready, because the current static-dark shell plus post-ready JS bootstrap is already visible too late for D-06. Change the bridge QSettings fallback to Light per D-05; do not create a new desktop architecture. Split synchronous idempotent palette application from user-request persistence in the existing UI theme owner; make settings_changed acknowledgement safely reapply/no-op without a second transaction, resolving the VIS-02 idempotency/concurrency probe. Retain the current root CSS-variable palettes and direct button press interaction per D-07, update every open overlay in the same root transaction per D-08, and persist without a global Save per D-09. Remove only theme-wide palette transitions that make independent elements tween; do not alter beta-5 token values, hard shadows, press transforms, typography, or add a crossfade.</action>
  <verify><automated>python -m pytest -q tests/test_ui_tokens_motion_responsive.py tests/test_webview_theme.py tests/test_guided_tour.py</automated></verify>
  <done>Theme bootstrap is pre-visible, user selection is immediately persisted once, and all open overlay/page surfaces resolve through one root palette change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make navigation and overlay close paths idempotent</name>
  <files>app/ui/app.js</files>
  <behavior>
    - Active navigation clicks do nothing to page entrance state.
    - Backend/options/live-progress/log updates mutate targeted controls without page-root replacement or animation replay.
    - Gate, dialog, dropdown, and guided-tour close paths retain the underlying page position without a layout jump.
  </behavior>
  <action>Add an early changed-target guard to `setScreen` before `LP.motion.nav`, then audit its existing callers so only genuine user navigation requests a page transition per D-01 and D-02. Keep backend/status/options rendering narrowly targeted and avoid hide/unhide/reclass/replacement of the current page root per D-04. Normalize each overlay close path to restore only the overlay/focus state and preserve the existing page rather than invoking same-screen navigation, per D-03. Preserve the established `lprail` entrance and all beta-5 motion tokens exactly, per D-17; resolve repeated/re-entrant state updates as no-ops rather than deferred repaint work.</action>
  <verify><automated>python -m pytest -q tests/test_ui_tokens_motion_responsive.py tests/test_webview_theme.py tests/test_guided_tour.py</automated></verify>
  <done>Only changed-page user navigation produces the established entrance animation; every tested active-nav, backend, option, and overlay-close path updates in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QSettings/desktop bridge -> initial DOM | Persisted UI preference becomes browser-visible state. |
| backend events -> live UI updates | External status/model values drive targeted DOM updates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Tampering | theme bootstrap/settings event | medium | mitigate | Validate/normalize theme selection at existing owner and make acknowledgement idempotent; tests prove root-only palette change. |
| T-04-05 | Tampering | live DOM rendering | medium | mitigate | Retain text-only targeted updates for backend values; no HTML insertion is introduced. |
| T-04-06 | Denial of Service | repeated UI events | low | accept | Active-page and duplicate-theme guards bound redundant DOM work; local events have no remote amplification. |
| T-04-SC | Tampering | package installs | high | mitigate | No package-manager install is allowed in this phase. |
</threat_model>

<verification>
After each task run the focused three-file suite. After Wave 1, run `python -m pytest -q` and retain actual output for the Phase 4 handoff.
</verification>

<success_criteria>
VIS-01 through VIS-03 are mechanically guarded without changing the approved beta-5 visual system.
</success_criteria>

<output>
Create `.planning/phases/04-visual-artifact-reliability/04-02-SUMMARY.md` when done.
</output>
