---
phase: 01-packaging-release
plan: 06
type: execute
wave: 6
depends_on:
  - 01-05
files_modified:
  - build/**
  - dist/**
  - dist-release/**
  - dist-release-staging/**
  - release-backups/**
  - docs/evidence/v1.2.0/release/test_collection_output.txt
  - docs/evidence/v1.2.0/release/TEST_RECONCILIATION.md
  - docs/evidence/v1.2.0/release/full_pytest_output.txt
  - docs/evidence/v1.2.0/release/development_selftest_output.txt
  - docs/evidence/v1.2.0/release/SOURCE_VALIDATION.md
  - docs/evidence/v1.2.0/release/build_output.txt
  - docs/evidence/v1.2.0/release/packaged_selftest_output.txt
  - docs/evidence/v1.2.0/release/extracted_selftest_output.txt
  - docs/evidence/v1.2.0/release/PACKAGED_VALIDATION.md
  - docs/HANDOFF_PHASE_1.md
autonomous: true
gap_closure: true
requirements:
  - REQ-version-bump
  - REQ-packaging-spec-audit
  - REQ-packaged-build
  - REQ-test-framework
  - REQ-test-suite-pass
  - REQ-self-test
  - REQ-test-reconciliation
  - REQ-privacy-safety
  - REQ-architecture-layers
must_haves:
  truths:
    - "Current pytest collection and the complete source suite pass with actual counts and unabridged command/exit evidence after the gap fixes."
    - "The development self-test exits zero and reports LecturePack v1.2.0 before packaging begins."
    - "The direct documented `.venv\\Scripts\\python.exe build_release.py` command exits zero and its captured stdout contains the exact contiguous schema-v1 13-event success sequence defined by Plan 01-05, proving D-01 through D-11 ran inside the builder."
    - "The published ZIP and root release documents identify v1.2.0; required runtime inventory is complete; forbidden models/secrets/config/jobs/transcripts/slides/media are absent."
    - "Every ZIP/runtime digest independently recomputes to the exact SHA256SUMS and BUILD_MANIFEST entry, including root CPU Whisper files exactly once."
    - "Both the published onedir executable and a fresh ZIP extraction under a path containing spaces pass finite-timeout `--selftest` runs."
    - "The previous release remains available until staged validation passes, no staging directory remains after success, and any generated backup/failure directory is reported without being committed."
    - "Phase 1 evidence continues to disclose strict architecture conformance as NO and assigns the unchanged 47-item baseline debt to Phase 2."
  flagged_assumptions:
    - "[edge:REQ-version-bump:idempotency] The corrected identity invariants and current rebuild pass once; a second full PyInstaller rebuild is not required, so binary byte-for-byte reproducibility remains unclaimed."
    - "[edge:REQ-version-bump:concurrency] Version consumers remain immutable, but concurrent builder invocation is unsupported and not used for release evidence."
    - "[edge:REQ-packaged-build:unclassified] Real build, inventory, digest, staged-publication markers, and two frozen executions are the concrete backstops; the original probe classification remains unresolved."
    - "[edge:REQ-packaged-build:concurrency] The real validation intentionally runs one release writer only and makes no multi-writer guarantee."
  prohibitions:
    - statement: "MUST NOT claim gap closure from static tests, mocks, historical logs, or an unextracted development tree."
      status: unresolved
      flagged: true
    - statement: "MUST NOT bundle models, credentials, user configuration, jobs, transcripts, slides, or original lecture media."
      status: unresolved
      flagged: true
    - statement: "MUST NOT mutate an original lecture video, real LecturePackData directory, provider credential, or live Groq service during validation."
      status: unresolved
      flagged: true
  artifacts:
    - path: "docs/evidence/v1.2.0/release/full_pytest_output.txt"
      provides: "Current complete post-gap source-suite output"
    - path: "docs/evidence/v1.2.0/release/build_output.txt"
      provides: "Direct builder preflight, backup, staging, validation, publication, command, and exit transcript"
    - path: "dist-release/LecturePack-portable-1.2.0.zip"
      provides: "Corrected and transactionally published portable release"
    - path: "dist-release/SHA256SUMS.txt"
      provides: "ZIP and complete runtime checksums"
    - path: "dist-release/BUILD_MANIFEST.json"
      provides: "v1.2.0 release/runtime manifest with complete unique CPU checksum coverage"
    - path: "docs/evidence/v1.2.0/release/PACKAGED_VALIDATION.md"
      provides: "Independent document, inventory, digest, exclusion, staging, and frozen-execution proof"
    - path: "docs/HANDOFF_PHASE_1.md"
      provides: "Honest gap-execution handoff awaiting independent GSD re-verification"
  key_links:
    - from: "tests/test_packaging_and_safety.py"
      to: "full_pytest_output.txt"
      via: "all new release safety/identity failure paths appear as PASSED current-run nodes"
      pattern: "test_packaging_and_safety"
    - from: "build_release.py"
      to: "build_output.txt"
      via: "the direct documented command emits `RELEASE_EVENT` JSON Lines with the exact Plan 01-05 schema/order between COMMAND and EXIT_CODE records"
      pattern: "RELEASE_EVENT"
    - from: "dist-release/LecturePack-portable-1.2.0.zip"
      to: "SHA256SUMS.txt and BUILD_MANIFEST.json"
      via: "independent SHA-256 recomputation for ZIP and every declared runtime entry"
      pattern: "sha256"
    - from: "README-FIRST.txt, RELEASE_NOTES.md, THIRD_PARTY_NOTICES.txt"
      to: "published ZIP"
      via: "ZIP current headings/banners are read and compared with 1.2.0 after publication"
      pattern: "v1\\.2\\.0"
---

<objective>
Produce fresh source and real-package evidence from the corrected identity and transactional builder, then hand Phase 1 back for independent verification.

Purpose: Prove the two structured gaps are closed in the actual portable artifact and the documented direct command, not only in unit tests.
Output: Current pytest/self-test logs, rebuilt release and integrity metadata, two independent frozen launch logs, packaged-validation report, and updated handoff.
</objective>

## Artifacts this phase produces

- Current collection, reconciliation, full-suite, and development self-test evidence.
- A fresh v1.2.0 onedir and portable ZIP built through the hardened direct entry point.
- Complete runtime/document/exclusion/digest inventories and two frozen launch transcripts.
- A handoff that records actual results and requests independent Phase 1 re-verification without claiming it prematurely.

<execution_context>
@C:/Users/marsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/marsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-packaging-release/01-CONTEXT.md
@.planning/phases/01-packaging-release/01-VERIFICATION.md
@.planning/phases/01-packaging-release/01-04-SUMMARY.md
@.planning/phases/01-packaging-release/01-05-SUMMARY.md
@docs/DECISIONS.md
@docs/WINDOWS_PORTABLE_INSTALL.md
@build_release.py
@LecturePack.spec
</context>

<task_scope>
Authorized phase: Phase 1 - Packaging &amp; Release gap closure validation.
Exact goal: Re-run source gates, build through the hardened direct command, independently validate the corrected portable release, and update the Phase 1 handoff.
Permitted files: Generated `build/**`, `dist/**`, `dist-release/**`, `dist-release-staging/**`, and `release-backups/**`; the nine exact evidence files and `docs/HANDOFF_PHASE_1.md` listed in frontmatter. Production source/tests/docs are read-only.
Required tests: Focused packaging module, current collection, complete pytest suite, development self-test, direct real build, onedir self-test, clean-extracted self-test, exact runtime/document/exclusion inventory, and independent digest comparison.
Non-goals: No source fix during validation, no dependency install/download, no clean-machine/SmartScreen approval claim, no live Groq/key/content use, no real user data/media, and no Phase 2 architecture repair.
Required completion evidence: All raw logs with exact commands/timeouts/exits, regenerated ZIP/checksums/manifest, `PACKAGED_VALIDATION.md`, actual pytest result in the handoff/summary, and `01-06-SUMMARY.md` ready for independent re-verification.
</task_scope>

<nyquist_warning>
The missing Validation Architecture artifact remains acknowledged. This plan compensates with current real commands, independent recomputation, explicit non-vacuous inventories, and two frozen executions; none may be replaced with inspection-only or mocked proof.
</nyquist_warning>

<tasks>

<task type="auto">
  <name>Task 1: Re-run collection, the full source suite, and development self-test</name>
  <read_first>
    AGENTS.md
    docs/PRODUCT_SPEC.md
    docs/ARCHITECTURE.md
    docs/DECISIONS.md
    docs/IMPLEMENTATION_PLAN.md
    .planning/phases/01-packaging-release/01-04-SUMMARY.md
    .planning/phases/01-packaging-release/01-05-SUMMARY.md
    tests/test_packaging_and_safety.py
    lecturepack/__main__.py
    lecturepack/app.py
    pytest.ini
  </read_first>
  <files>docs/evidence/v1.2.0/release/test_collection_output.txt, docs/evidence/v1.2.0/release/TEST_RECONCILIATION.md, docs/evidence/v1.2.0/release/full_pytest_output.txt, docs/evidence/v1.2.0/release/development_selftest_output.txt, docs/evidence/v1.2.0/release/SOURCE_VALIDATION.md</files>
  <action>First run the complete `tests/test_packaging_and_safety.py` module; stop before generated-output mutation on any failure. Run `.venv\Scripts\python.exe -m pytest --collect-only -q` and `.venv\Scripts\python.exe -m pytest -v` from the repository root. Capture each exact command, complete stdout/stderr, and actual exit code before evaluating it; do not hardcode a remembered count. Update `TEST_RECONCILIATION.md` from the current collector, explicitly accounting for source test functions, parametrized expansions, and the Plan 01-04/01-05 additions. Run `.venv\Scripts\python.exe -m lecturepack --selftest` through a non-shell subprocess with a 120-second timeout and retain exact command, timeout state, complete output, and actual exit code. Require collection/full suite exit zero with zero failures/errors and development timeout false/exit zero/`SELFTEST PASS`/`LecturePack v1.2.0`. Update `SOURCE_VALIDATION.md` after each command before propagating failure, retaining the Phase 1 strict-architecture disclosure from AD-15 and links to current raw logs. Do not weaken/rename/delete tests, use historical output, invoke owner-specific packaged validation, access a provider, or use real LecturePackData/media.</action>
  <verify>
    <automated>$e='docs\evidence\v1.2.0\release'; $cl=Get-Content -Raw -LiteralPath "$e\test_collection_output.txt"; $fl=Get-Content -Raw -LiteralPath "$e\full_pytest_output.txt"; $sl=Get-Content -Raw -LiteralPath "$e\development_selftest_output.txt"; if($cl -notmatch '(?m)^COMMAND: \.venv\\Scripts\\python\.exe -m pytest --collect-only -q$' -or $cl -notmatch '(?m)^EXIT_CODE: 0$' -or $cl -notmatch '[0-9]+ tests collected'){throw 'collection evidence invalid'}; if($fl -notmatch '(?m)^COMMAND: \.venv\\Scripts\\python\.exe -m pytest -v$' -or $fl -notmatch '(?m)^EXIT_CODE: 0$' -or $fl -notmatch '[0-9]+ passed'){throw 'full-suite evidence invalid'}; foreach($m in @('TIMEOUT: false','EXIT_CODE: 0','SELFTEST PASS','LecturePack v1.2.0')){if($sl -notmatch [regex]::Escape($m)){throw "development selftest missing $m"}}</automated>
  </verify>
  <acceptance_criteria>Focused packaging tests, collection, and the complete suite exit zero using the actual new count; reconciliation explains that count; the development self-test passes within 120 seconds; all evidence is complete/current and written before evaluation.</acceptance_criteria>
  <done>The post-gap source tree has a clean, auditable release gate and the real development entry point still launches as v1.2.0.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild directly and independently validate the published portable release</name>
  <read_first>
    .planning/phases/01-packaging-release/01-CONTEXT.md
    .planning/phases/01-packaging-release/01-VERIFICATION.md
    .planning/phases/01-packaging-release/01-05-SUMMARY.md
    docs/evidence/v1.2.0/release/SOURCE_VALIDATION.md
    build_release.py
    LecturePack.spec
    README-FIRST.txt
    RELEASE_NOTES.md
    THIRD_PARTY_NOTICES.txt
    docs/WINDOWS_PORTABLE_INSTALL.md
  </read_first>
  <files>build/**, dist/**, dist-release/**, dist-release-staging/**, release-backups/**, docs/evidence/v1.2.0/release/build_output.txt, docs/evidence/v1.2.0/release/packaged_selftest_output.txt, docs/evidence/v1.2.0/release/extracted_selftest_output.txt, docs/evidence/v1.2.0/release/PACKAGED_VALIDATION.md</files>
  <action>Run the documented command exactly as `.venv\Scripts\python.exe build_release.py`, with no external cleanup wrapper, no force option, and no pre-mutation helper outside the builder. Capture exact `COMMAND: .venv\Scripts\python.exe build_release.py`, complete stdout/stderr, and actual terminal `EXIT_CODE: N` in `build_output.txt` before evaluation. Parse only lines prefixed `RELEASE_EVENT ` as JSON and require every record to have `schema == 1`, contiguous integer `seq` from 1, `event`, and `status`. On success require exactly the Plan 01-05 ordered event names: `preflight_complete`, `backup_complete`, `staging_created`, `pyinstaller_complete`, `onedir_inventory_complete`, `zip_inventory_complete`, `digests_complete`, `frozen_onedir_complete`, `frozen_extracted_complete`, `rollback_ready`, `publication_complete`, `staging_cleanup_complete`, `build_complete`. Validate all exact required types/values from that contract: zero preflight errors; Vulkan `absent|complete`; backup count/destination shape; exact staging path/non-empty invocation ID; PyInstaller exit zero; both inventories zero missing/forbidden plus ZIP document identity pass; positive logical-path count/zero mismatches; both frozen exits zero/non-timeout and extracted path-space true; boolean prior-release state/nonnegative rollback count; publication not restored; owned/removed staging true; terminal status pass, version 1.2.0, and exact ZIP name. Prose/PASS markers are summaries only and cannot substitute for any event. If the command fails, parse the exact contiguous `failure_detected` then applicable `rollback_complete`, `failure_log_complete`, `staging_cleanup_complete`, and terminal failed `build_complete` fields from Plan 01-05; retain the compact log/previous release, record failure honestly, and stop without a source workaround.

After success, independently inspect the live outputs rather than trusting event/report PASS markers. Open the actual `LecturePack-portable-1.2.0.zip` with `System.IO.Compression.ZipArchive`; require root `SHA256SUMS.txt`, `BUILD_MANIFEST.json`, and `RELEASE_NOTES.md` beside it and `manifest.version == 1.2.0`. Build normalized logical-path sets from the actual onedir and ZIP central directory and require `LecturePack.exe`, `_internal/`, FFmpeg/ffprobe, every root `WHISPER_BINS + WHISPER_CPU_DLLS` member, the complete Vulkan set iff enabled, and the exact three shipped documents. Read each document directly from its ZIP entry as UTF-8 and validate its designated current heading/banner against exact v1.2.0 text; do not accept a summary marker. Independently reject application-root model payloads, credentials/secrets, user configuration, job/LecturePackData, transcript/slide/candidate data, and original media in both actual sets.

Parse `SHA256SUMS.txt` data lines into a unique logical-path to digest map and `BUILD_MANIFEST.json.files` into the same shape; reject duplicate logical paths and require exact logical-path-set equality, not subset coverage. Require the exact set to contain one ZIP path, FFmpeg under `bin/`, every CPU Whisper file at app root, and optional Vulkan under `bin/vulkan/` only as a complete set. Recompute the ZIP digest from the actual archive file. For every runtime logical path, recompute SHA-256 twice from independent actual payloads: the correct onedir file location and the corresponding `LecturePack/<logical-path>` ZIP entry stream. Require onedir digest == ZIP-entry digest == SHA256SUMS digest == manifest digest and verify recorded byte sizes against both payloads. This independent validation is the evidence; `PACKAGED_VALIDATION.md` PASS lines only summarize it.

Independently run `dist/LecturePack/LecturePack.exe --selftest` with an argument-list process and 120-second timeout; retain one exact normalized absolute `EXECUTABLE_PATH`, an exact `COMMAND` whose executable/argument resolves to that path plus `--selftest`, `TIMEOUT_SECONDS: 120`, `TIMEOUT: false`, complete stdout/stderr, and `EXIT_CODE: 0` in `packaged_selftest_output.txt`. Extract the published ZIP into a unique validated OS-temp child whose leaf contains a space and did not exist, repeat actual inventory/exclusion/document checks, and run its EXE identically into `extracted_selftest_output.txt`. Parse both logs directly and require path existence/expected onedir-vs-extraction location, command/path equality, finite timeout fields, PASS/version after execution, and absence of `Traceback`, `ImportError`, `ModuleNotFoundError`, fatal Qt/QSS/stylesheet text, or `QThread: Destroyed while thread is still running`. Write `PACKAGED_VALIDATION.md` before removing only that validated temp child; include exact digests, counts, document identities, runtime uniqueness, exclusions, both frozen results, parsed event order, prior-release/backup/failure-log disposition, and links to raw logs. Confirm the exact staging child is absent after success. Never commit generated `release-backups` contents and never inspect original lecture/user/provider data.</action>
  <verify>
    <automated>$ErrorActionPreference='Stop'; $release='dist-release'; $app=(Resolve-Path -LiteralPath 'dist\LecturePack').Path; $zipName='LecturePack-portable-1.2.0.zip'; $zipPath=(Resolve-Path -LiteralPath (Join-Path $release $zipName)).Path; $buildLog=Get-Content -Raw -LiteralPath 'docs\evidence\v1.2.0\release\build_output.txt'; if($buildLog -notmatch '(?m)^COMMAND: \.venv\\Scripts\\python\.exe build_release\.py$' -or $buildLog -notmatch '(?m)^EXIT_CODE: 0$'){throw 'direct builder command/exit missing'}; $events=@(Get-Content -LiteralPath 'docs\evidence\v1.2.0\release\build_output.txt' | Where-Object {$_.StartsWith('RELEASE_EVENT ')} | ForEach-Object {$_.Substring(14) | ConvertFrom-Json}); $names=@('preflight_complete','backup_complete','staging_created','pyinstaller_complete','onedir_inventory_complete','zip_inventory_complete','digests_complete','frozen_onedir_complete','frozen_extracted_complete','rollback_ready','publication_complete','staging_cleanup_complete','build_complete'); if($events.Count -ne $names.Count){throw 'release event count mismatch'}; for($i=0;$i -lt $names.Count;$i++){if($events[$i].schema -ne 1 -or $events[$i].seq -ne ($i+1) -or $events[$i].event -ne $names[$i] -or $events[$i].status -ne 'pass'){throw "release event schema/order mismatch at $i"}}; if($events[0].error_count -ne 0 -or @('absent','complete') -notcontains $events[0].vulkan){throw 'preflight event values invalid'}; if($events[1].unexpected_count -lt 0){throw 'backup event invalid'}; if($events[2].path -ne 'dist-release-staging' -or [string]::IsNullOrWhiteSpace($events[2].invocation_id)){throw 'staging event invalid'}; if($events[3].exit_code -ne 0){throw 'pyinstaller event invalid'}; foreach($j in @(4,5)){if($events[$j].required_missing -ne 0 -or $events[$j].forbidden_count -ne 0){throw 'inventory event invalid'}}; if($events[5].document_identity -ne 'pass' -or $events[6].logical_path_count -le 0 -or $events[6].mismatch_count -ne 0){throw 'ZIP/digest event invalid'}; foreach($j in @(7,8)){if($events[$j].exit_code -ne 0 -or $events[$j].timeout -ne $false){throw 'frozen event invalid'}}; if($events[8].path_contains_space -ne $true -or $events[9].rollback_entry_count -lt 0 -or $events[10].prior_release_restored -ne $false -or $events[11].owned -ne $true -or $events[11].removed -ne $true -or $events[12].version -ne '1.2.0' -or $events[12].release_zip -ne $zipName){throw 'publication terminal events invalid'}; if(Test-Path -LiteralPath 'dist-release-staging'){throw 'staging remained after success'}; Add-Type -AssemblyName System.IO.Compression.FileSystem; $za=[IO.Compression.ZipFile]::OpenRead($zipPath); try{$zipEntries=@{}; foreach($entry in $za.Entries){$n=$entry.FullName.Replace('\','/'); if($zipEntries.ContainsKey($n)){throw "duplicate ZIP entry $n"}; $zipEntries[$n]=$entry}; $cpu=@('whisper-cli.exe','whisper.dll','ggml.dll','ggml-base.dll','ggml-cpu-alderlake.dll','ggml-cpu-cannonlake.dll','ggml-cpu-cascadelake.dll','ggml-cpu-haswell.dll','ggml-cpu-icelake.dll','ggml-cpu-sandybridge.dll','ggml-cpu-skylakex.dll','ggml-cpu-sse42.dll','ggml-cpu-x64.dll'); $ff=@('bin/ffmpeg.exe','bin/ffprobe.exe'); $vk=@('bin/vulkan/whisper-cli.exe','bin/vulkan/libwhisper.dll','bin/vulkan/ggml.dll','bin/vulkan/ggml-base.dll','bin/vulkan/ggml-cpu.dll','bin/vulkan/ggml-vulkan.dll','bin/vulkan/libgcc_s_seh-1.dll','bin/vulkan/libstdc++-6.dll','bin/vulkan/libwinpthread-1.dll','bin/vulkan/libgomp-1.dll'); $docs=@{'README-FIRST.txt'='LECTURE PACK v1.2.0 - PORTABLE RELEASE';'THIRD_PARTY_NOTICES.txt'='THIRD-PARTY NOTICES FOR LecturePack v1.2.0';'RELEASE_NOTES.md'='(?m)^# LecturePack v1\.2\.0\b'}; $required=@('LecturePack/LecturePack.exe')+($ff|ForEach-Object{"LecturePack/$_"})+($cpu|ForEach-Object{"LecturePack/$_"})+($docs.Keys|ForEach-Object{"LecturePack/$_"}); if($events[0].vulkan -eq 'complete'){$required+=($vk|ForEach-Object{"LecturePack/$_"})}; foreach($n in $required){if(-not $zipEntries.ContainsKey($n)){throw "required ZIP entry missing $n"}}; if(-not @($zipEntries.Keys|Where-Object {$_.StartsWith('LecturePack/_internal/')}).Count){throw 'ZIP _internal missing'}; foreach($doc in $docs.Keys){$reader=[IO.StreamReader]::new($zipEntries["LecturePack/$doc"].Open(),[Text.Encoding]::UTF8,$true); try{$text=$reader.ReadToEnd()}finally{$reader.Dispose()}; if($text -notmatch $docs[$doc]){throw "ZIP document identity mismatch $doc"}}; $forbidden='(?i)^LecturePack/(models|LecturePackData|jobs|transcript|frames|candidates|accepted|rejected)(/|$)|(^|/)(credentials|secrets|config|settings)\.json$|(^|/)(api_key\.txt|\.env)$|(^|/)ggml-[^/]*\.(bin|gguf)$|\.(mp4|m4v|mov|mkv|avi|webm|wav|flac)$'; if(@($zipEntries.Keys|Where-Object {$_ -match $forbidden}).Count){throw 'forbidden ZIP payload found'}; $disk=@(Get-ChildItem -LiteralPath $app -Recurse -Force|ForEach-Object {$_.FullName.Substring($app.Length+1).Replace('\','/')}); if(@($disk|Where-Object {("LecturePack/$_") -match $forbidden}).Count){throw 'forbidden onedir payload found'}; $sumMap=@{}; foreach($line in Get-Content -LiteralPath (Join-Path $release 'SHA256SUMS.txt')){if($line -match '^([0-9a-fA-F]{64})\s{2}(.+)$'){$n=$Matches[2].Replace('\','/'); if($sumMap.ContainsKey($n)){throw "duplicate checksum path $n"}; $sumMap[$n]=$Matches[1].ToLowerInvariant()}}; $manifest=Get-Content -Raw -LiteralPath (Join-Path $release 'BUILD_MANIFEST.json')|ConvertFrom-Json; if($manifest.version -ne '1.2.0'){throw 'manifest version mismatch'}; $manifestMap=@{}; foreach($item in $manifest.files){$n=([string]$item.file).Replace('\','/'); if($manifestMap.ContainsKey($n)){throw "duplicate manifest path $n"}; $manifestMap[$n]=$item}; if(@(Compare-Object @($sumMap.Keys|Sort-Object) @($manifestMap.Keys|Sort-Object)).Count){throw 'checksum/manifest logical path sets differ'}; $expected=@($zipName)+$ff+$cpu; if($events[0].vulkan -eq 'complete'){$expected+=$vk}; if(@(Compare-Object @($expected|Sort-Object) @($sumMap.Keys|Sort-Object)).Count){throw 'runtime checksum logical path set is not exact'}; $zipHash=(Get-FileHash -Algorithm SHA256 -LiteralPath $zipPath).Hash.ToLowerInvariant(); if($sumMap[$zipName] -ne $zipHash -or ([string]$manifestMap[$zipName].sha256).ToLowerInvariant() -ne $zipHash){throw 'ZIP digest mismatch'}; foreach($logical in ($expected|Where-Object {$_ -ne $zipName})){$diskPath=Join-Path $app ($logical.Replace('/','\')); if(-not (Test-Path -LiteralPath $diskPath -PathType Leaf)){throw "runtime missing on disk $logical"}; $diskHash=(Get-FileHash -Algorithm SHA256 -LiteralPath $diskPath).Hash.ToLowerInvariant(); $entryName="LecturePack/$logical"; if(-not $zipEntries.ContainsKey($entryName)){throw "runtime missing in ZIP $logical"}; $stream=$zipEntries[$entryName].Open(); $sha=[Security.Cryptography.SHA256]::Create(); try{$zipHashEntry=([BitConverter]::ToString($sha.ComputeHash($stream))).Replace('-','').ToLowerInvariant()}finally{$sha.Dispose();$stream.Dispose()}; $declared=([string]$manifestMap[$logical].sha256).ToLowerInvariant(); if($diskHash -ne $zipHashEntry -or $diskHash -ne $sumMap[$logical] -or $diskHash -ne $declared){throw "runtime digest mismatch $logical"}; if((Get-Item -LiteralPath $diskPath).Length -ne [int64]$manifestMap[$logical].size_bytes -or $zipEntries[$entryName].Length -ne [int64]$manifestMap[$logical].size_bytes){throw "runtime size mismatch $logical"}}}finally{$za.Dispose()}; function Check-FrozenLog([string]$path,[string]$expectedExe,[bool]$mustExist,[bool]$mustContainSpace){$raw=Get-Content -Raw -LiteralPath $path; $em=[regex]::Match($raw,'(?m)^EXECUTABLE_PATH: (.+)$'); $cm=[regex]::Match($raw,'(?m)^COMMAND: (.+)$'); if(-not $em.Success -or -not $cm.Success){throw "$path command/path missing"}; $exe=$em.Groups[1].Value.Trim(); $cmd=$cm.Groups[1].Value.Trim(); if($mustExist -and -not (Test-Path -LiteralPath $exe -PathType Leaf)){throw "$path executable absent"}; if($expectedExe -and [IO.Path]::GetFullPath($exe) -ne [IO.Path]::GetFullPath($expectedExe)){throw "$path executable path mismatch"}; if($mustContainSpace -and $exe -notmatch '\s'){throw "$path path lacks space"}; if($cmd -notmatch [regex]::Escape($exe) -or $cmd -notmatch '(?:^|\s)--selftest(?:\s|$)'){throw "$path command does not match executable"}; foreach($marker in @('TIMEOUT_SECONDS: 120','TIMEOUT: false','EXIT_CODE: 0','SELFTEST PASS','LecturePack v1.2.0')){if($raw -notmatch [regex]::Escape($marker)){throw "$path missing $marker"}}; if($raw -match '(?i)Traceback|ImportError|ModuleNotFoundError|QThread:\s*Destroyed while thread is still running|(?:fatal|error).*(?:Qt|QSS|stylesheet)|(?:Qt|QSS|stylesheet).*(?:fatal|error)'){throw "$path contains fatal marker"}}; Check-FrozenLog 'docs\evidence\v1.2.0\release\packaged_selftest_output.txt' (Join-Path $app 'LecturePack.exe') $true $false; Check-FrozenLog 'docs\evidence\v1.2.0\release\extracted_selftest_output.txt' '' $false $true</automated>
  </verify>
  <acceptance_criteria>The direct builder exits zero and its exact parsed 13-event schema/order/values prove D-01 through D-11; independent code opens the actual ZIP, validates current headings and required/forbidden entries, proves exact checksum/manifest logical-path-set equality, and recomputes every ZIP/FFmpeg/CPU/optional-Vulkan digest from both onedir and ZIP payloads; frozen logs are parsed for exact command/path/timeout/exit/fatal fields; no summary PASS marker substitutes for evidence.</acceptance_criteria>
  <done>A fresh, corrected, fail-closed v1.2.0 portable release is published and independently proven from source inputs through clean extraction.</done>
</task>

<task type="auto">
  <name>Task 3: Update the Phase 1 handoff for independent re-verification</name>
  <read_first>
    docs/HANDOFF_PHASE_1.md
    docs/evidence/v1.2.0/release/SOURCE_VALIDATION.md
    docs/evidence/v1.2.0/release/PACKAGED_VALIDATION.md
    docs/evidence/v1.2.0/release/full_pytest_output.txt
    docs/evidence/v1.2.0/release/build_output.txt
    .planning/phases/01-packaging-release/01-VERIFICATION.md
  </read_first>
  <files>docs/HANDOFF_PHASE_1.md</files>
  <action>Update the handoff date/status and record the actual current collector/full-suite result, development self-test, direct builder exit, document identity result, complete runtime checksum result, ZIP digest/size, both independent frozen self-tests, staging/backup/failure-log disposition, and generated artifact paths from Tasks 1-2. State that Plans 01-04 through 01-06 executed and that the two prior structured gaps have current executor evidence ready for independent GSD re-verification; do not edit `01-VERIFICATION.md` or claim the independent verifier has passed before it runs. Preserve AD-15 language: strict architecture conformance is still NO, the unchanged 47-item baseline remains Phase 2 debt, and no new violation may enter Phase 1. Set the resume point to independent Phase 1 re-verification/security gate and prohibit Phase 2/release approval until those gates complete. Include the actual pytest output requested by AGENTS and any honest remaining backstop/human-only items.</action>
  <verify>
    <automated>$h=Get-Content -Raw -LiteralPath 'docs\HANDOFF_PHASE_1.md'; foreach($v in @('01-04','01-05','01-06','independent','re-verification','Strict architecture conformance','Phase 2','PACKAGED_VALIDATION.md')){if($h -notmatch [regex]::Escape($v)){throw "handoff missing $v"}}; if($h -match '(?im)^.*independent verification.*(?:pass|complete)'){throw 'handoff prematurely claims verifier success'}</automated>
  </verify>
  <acceptance_criteria>The handoff contains actual current results and artifact links, makes no premature independent-verification claim, preserves the architecture debt boundary, and gives the correct re-verification resume point.</acceptance_criteria>
  <done>Phase 1 has an honest, evidence-backed handoff ready for the verifier and owner approval gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Current source/tests to release evidence | Logs must come from current commands and retain failures before evaluation. |
| Release inputs and cleanup paths to direct builder | The builder must enforce aggregate input validation, exact non-reparse targets, and transactional backups itself. |
| Previous release to staged publication | Prior artifacts remain available until all content, integrity, and frozen checks accept the replacement. |
| Published ZIP to clean extraction | Transported files must preserve identity, runtime completeness, exclusions, integrity, and frozen launch behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-30 | Spoofing | Current-run source/build evidence | high | mitigate | Capture exact direct commands/exits and parse contiguous schema-v1 JSON events with the exact Plan 01-05 names, order, types, and values; reject prose/history/mock substitutes. |
| T-01-31 | Elevation of Privilege | Cleanup/reparse paths | high | mitigate | Require direct-builder markers plus Plan 01-05 tests for exact allowlisted repo children and non-following reparse rejection. |
| T-01-32 | Tampering | Backup/staged publication | high | mitigate | Require backup/staging/prior-release/publication markers and independently confirm no leftover staging after success. |
| T-01-33 | Tampering | Runtime/docs/ZIP | high | mitigate | Open the actual ZIP, read document entries, compare exact checksum/manifest logical sets, and recompute every runtime digest from both onedir and ZIP streams. |
| T-01-34 | Information Disclosure | Portable payload | high | mitigate | Inspect onedir, ZIP, and extraction for models, secrets, config, jobs, transcripts, slides, candidates, and media. |
| T-01-35 | Denial of Service | Frozen runtime | high | mitigate | Run onedir and clean-extracted EXEs with finite 120-second timeouts and reject fatal import/Qt/QThread markers. |
| T-01-36 | Repudiation | Prior-release preservation/failure | medium | mitigate | Retain builder publication/failure markers and compact logs; update handoff with actual backup/failure disposition. |
</threat_model>

<verification>
Confirm every evidence file in frontmatter is non-empty and current, the full suite and development self-test exit zero, the direct builder event stream matches the exact 13-event success contract, actual ZIP/onedir inspection and independently recomputed digest equality pass, both frozen logs have matching command/path/timeout/exit/fatal fields, `dist-release-staging` is absent after success, generated backup contents are untracked, and the handoff requests rather than impersonates independent re-verification. Treat report PASS markers only as summaries.
</verification>

<success_criteria>
- Current source collection/full suite/development self-test all pass with actual raw output.
- The hardened direct builder runs without an external safety wrapper and emits the exact machine-readable Plan 01-05 success contract proving D-01 through D-11.
- Independent inspection of the actual ZIP/onedir proves v1.2.0 headings, exact required/forbidden inventory, exact checksum/manifest logical-set equality, and every ZIP/FFmpeg/CPU/optional-Vulkan digest from both payloads.
- Both onedir and clean-extracted frozen executables pass.
- Prior-release/staging/backup state is explicitly accounted for and no generated backup is committed.
- The handoff is ready for independent Phase 1 re-verification and retains Phase 2 architecture debt honestly.
</success_criteria>

<output>
Create `.planning/phases/01-packaging-release/01-06-SUMMARY.md` when done.
</output>
