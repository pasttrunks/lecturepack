---
phase: 01-packaging-release
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tests/test_alignment.py
  - docs/evidence/v1.2.0/release/test_collection_output.txt
  - docs/evidence/v1.2.0/release/TEST_RECONCILIATION.md
  - docs/evidence/v1.2.0/release/full_pytest_output.txt
  - docs/evidence/v1.2.0/release/development_selftest_output.txt
  - docs/evidence/v1.2.0/release/architecture_privacy_audit_output.txt
  - docs/evidence/v1.2.0/release/REQUIREMENT_TRACEABILITY.md
  - docs/evidence/v1.2.0/release/SOURCE_VALIDATION.md
autonomous: true
requirements:
  - REQ-core-conversion
  - REQ-privacy-safety
  - REQ-transcription
  - REQ-slide-extraction
  - REQ-alignment
  - REQ-export-formats
  - REQ-job-lifecycle
  - REQ-study-workspace
  - REQ-provider-neutral-transcription
  - REQ-groq-transcription
  - REQ-stability
  - REQ-architecture-layers
  - REQ-test-framework
  - REQ-test-suite-pass
  - REQ-self-test
  - REQ-test-reconciliation
must_haves:
  truths:
    - "Pytest collection completes without an import or collection error and the evidence records the actual collected-case count."
    - "The full source suite completes with zero failures and zero errors, and its complete output is retained as release evidence."
    - "The development CLI self-test exits zero, prints SELFTEST PASS, and reports LecturePack v1.2.0 from an offscreen temporary data directory."
    - "A pre-validation alignment test module directly proves greatest-overlap assignment, equal-overlap tie-to-earlier behavior, at-least-one segment per slide, and exactly-one slide assignment per source segment."
    - "Every one of the thirteen verified-not-built Phase 1 requirements has a durable mapping to current executable pytest node IDs and assertion categories; missing or insufficient coverage is recorded as a blocking gap rather than inferred from a generic suite pass."
    - "Read-only targeted architecture and privacy audits run as current commands, persist command/exit records, and pass before source validation can be accepted."
    - "[edge:REQ-privacy-safety:empty] Empty or absent optional inputs fail closed without creating a network request, plaintext credential, or original-video mutation."
    - "[edge:REQ-privacy-safety:ordering] No pipeline ordering or product mode can bypass per-job online consent or the private-local default."
    - statement: "[edge:REQ-privacy-safety:idempotency] Repeating validation and supported job operations preserves privacy and no-deletion invariants."
      verification: backstop
    - "[edge:REQ-privacy-safety:concurrency] Parallel Transcribe and Detect Slides execution retains the same consent, secret-store, and atomic-publication boundaries as sequential execution."
    - "[edge:REQ-transcription:empty] Missing, empty, or failed transcription output cannot silently replace an existing valid canonical transcript."
    - statement: "[edge:REQ-transcription:encoding] Transcript text and safely escaped Windows paths retain their intended Unicode content through canonical JSON/SRT/TXT output."
      verification: backstop
    - "[edge:REQ-alignment:boundary] Exact overlap boundaries and equal-overlap ties resolve deterministically to the earlier slide."
    - statement: "[edge:REQ-alignment:adjacency] A segment that only touches a slide boundary is handled consistently with the greatest-overlap contract."
      verification: backstop
    - statement: "[edge:REQ-alignment:empty] Empty slide or segment collections produce a defined non-crashing result without inventing mappings."
      verification: backstop
    - "[edge:REQ-alignment:ordering] Equal-overlap results preserve chronological slide order and assign each segment once."
    - "[edge:REQ-alignment:precision] Floating-point timestamp comparisons retain the documented earlier-slide tie break rather than depending on iteration accident."
    - "[edge:REQ-provider-neutral-transcription:concurrency] Backend progress, result, and cancellation signals remain provider-neutral when transcription runs beside slide detection."
    - "[edge:REQ-groq-transcription:adjacency] Overlapping chunk boundaries are deduplicated without dropping distinct neighboring words."
    - "[edge:REQ-groq-transcription:empty] Missing credentials, missing consent, empty provider responses, and incomplete chunk sets fail closed and preserve prior canonical output."
    - "[edge:REQ-groq-transcription:ordering] Successful chunks merge in source order with timestamp offsets before the canonical raw files publish."
    - "[edge:REQ-groq-transcription:idempotency] Re-running an identical online request reuses fingerprinted successful chunks and publishes the same canonical transcript."
    - "[edge:REQ-groq-transcription:concurrency] Upload concurrency remains bounded, cancellation is cooperative, and an eligible fallback does not cancel the slide branch."
    - "[edge:REQ-stability:idempotency] Repeated cancellation or close requests cannot resurrect a retired worker or restart the pipeline."
    - "[edge:REQ-stability:concurrency] Process-tree termination targets only LecturePack-owned PIDs while unrelated processes survive."
    - statement: "[edge:REQ-architecture-layers:adjacency] Runtime calls cross only adjacent UI, controller, service, and infrastructure boundaries."
      verification: backstop
    - statement: "[edge:REQ-architecture-layers:ordering] Dependency direction remains UI to Controller to Service to Infrastructure, never the reverse."
      verification: backstop
    - "[edge:REQ-architecture-layers:concurrency] Widgets stay on the main Qt thread while external tools and internal compute use the approved process/worker boundaries."
    - "[edge:REQ-test-suite-pass:boundary] The release gate accepts exactly zero test failures and zero collection errors; any nonzero result blocks completion."
    - "[edge:REQ-test-suite-pass:precision] The reported test total comes from pytest collection rather than a source-function scan."
    - "[edge:REQ-test-reconciliation:boundary] Parameterized cases are counted at the pytest item boundary, not collapsed into one function."
    - "[edge:REQ-test-reconciliation:precision] The reconciliation distinguishes 149 source test functions from the two additional collected parameter variants if the current collector confirms that relationship."
  flagged_assumptions:
    - "[edge:REQ-core-conversion:unclassified] The probe could not classify core conversion; the plan treats the full existing suite as the release backstop and keeps this assumption visible."
    - "[edge:REQ-privacy-safety:adjacency] The probe's generic adjacency question does not map cleanly to P1-P7; no new privacy boundary is inferred and the item remains flagged."
    - "[edge:REQ-slide-extraction:unclassified] The probe could not classify slide extraction; current deterministic CV tests are relied upon without inventing a new Phase 2 edge fixture."
    - "[edge:REQ-export-formats:unclassified] The probe could not classify export formats; existing format and safety tests are relied upon and this remains flagged."
    - "[edge:REQ-job-lifecycle:unclassified] The probe could not classify job lifecycle; existing cancel/resume/atomic-write tests are relied upon and this remains flagged."
    - "[edge:REQ-study-workspace:unclassified] The probe could not classify Study behavior; existing workspace and provenance tests are relied upon and this remains flagged."
    - "[edge:REQ-architecture-layers:empty] An empty-input interpretation is not meaningful for an architectural layering rule; no behavior is invented and the item remains flagged."
    - "[edge:REQ-test-framework:unclassified] The probe could not classify the test framework; collection plus the complete pytest run is the operational evidence and this remains flagged."
  prohibitions:
    - statement: "MUST NOT delete, hide, weaken, rename, or alter a test merely to make the release suite pass or to force a historical count."
      status: unverified
      flagged: true
    - statement: "MUST NOT present mocked output, static inspection, or historical logs as proof that the current external-tool or packaged integration ran."
      status: unverified
      flagged: true
    - statement: "MUST NOT perform live Groq validation, read a production API key, or send lecture content during this release validation plan."
      status: unverified
      flagged: true
    - statement: "MUST NOT run the owner-specific packaged-validation entry point or write validation state into the user's real LecturePackData directory."
      status: unverified
      flagged: true
  artifacts:
    - path: "docs/evidence/v1.2.0/release/test_collection_output.txt"
      provides: "Complete current pytest collection output"
    - path: "tests/test_alignment.py"
      provides: "Executable acceptance coverage for every REQ-alignment clause"
    - path: "docs/evidence/v1.2.0/release/TEST_RECONCILIATION.md"
      provides: "Function-count versus collected-item reconciliation with command provenance"
    - path: "docs/evidence/v1.2.0/release/full_pytest_output.txt"
      provides: "Complete source-suite pass/fail record"
    - path: "docs/evidence/v1.2.0/release/development_selftest_output.txt"
      provides: "Development entry-point self-test output"
    - path: "docs/evidence/v1.2.0/release/architecture_privacy_audit_output.txt"
      provides: "Executable AST/source audit evidence for architectural dependency direction and privacy/safety source constraints"
    - path: "docs/evidence/v1.2.0/release/REQUIREMENT_TRACEABILITY.md"
      provides: "Requirement-to-pytest-node/assertion-category matrix with current-run PASS or blocking GAP disposition"
    - path: "docs/evidence/v1.2.0/release/SOURCE_VALIDATION.md"
      provides: "Release-source validation summary with exact commands and exit codes"
  key_links:
    - from: "tests/test_alignment.py"
      to: "lecturepack/services/export_service.py"
      via: "Public ExportService.align_and_export execution followed by aligned.json assertions"
      pattern: "align_and_export"
    - from: "tests/test_packaging_and_safety.py"
      to: "docs/evidence/v1.2.0/release/full_pytest_output.txt"
      via: "The complete pytest invocation includes the new release regression tests from Plan 01-01"
      pattern: "test_packaging_and_safety"
    - from: "lecturepack/__main__.py"
      to: "lecturepack/app.py"
      via: "python -m lecturepack --selftest dispatches to run_selftest"
      pattern: "run_selftest"
  probe_accounting:
    surfaced_edges: 34
    authored_truths_or_backstops: 26
    flagged_assumptions: 8
---

<objective>
Reconcile the current pytest collection, run the entire source suite, and prove the development application self-test with durable, current-run evidence.

Purpose: Close the known alignment-acceptance coverage gap, then establish that the already-built conversion, privacy, transcription, slide, alignment, export, lifecycle, Study, provider, stability, and layering behavior remains release-ready after the packaging changes.
Output: Alignment acceptance tests, collection reconciliation, full pytest output, development self-test output, and a source-validation summary.
</objective>

## Artifacts this phase produces

- A current pytest collection transcript and an explicit explanation of function count versus collected cases.
  - A focused alignment contract module covering every REQ-alignment acceptance clause before the release validation run.
  - A pre-validation clause audit that blocks collection if any of the other twelve verified-not-built requirements lacks a concrete executable assertion.
- Full, unabridged pytest output from the current tree.
- A real development-entry self-test transcript reporting release 1.2.0.
- A concise validation summary recording exact commands, exit codes, counts, and any warnings.

<execution_context>
@C:/Users/marsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/marsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-packaging-release/01-RESEARCH.md
@.planning/phases/01-packaging-release/01-PATTERNS.md
@.planning/phases/01-packaging-release/01-01-SUMMARY.md
@.planning/codebase/TESTING.md
@docs/TEST_PLAN.md
</context>

<task_scope>
Authorized phase: Phase 1 — Packaging &amp; Release.
Exact goal: Add the missing executable REQ-alignment acceptance coverage, reconcile current pytest collection, pass the full suite, and validate the development self-test.
Permitted files: `tests/test_alignment.py` plus only the seven named files under `docs/evidence/v1.2.0/release/`; all production source and all other tests are read-only in this plan.
Required tests: The complete `tests/test_alignment.py` module, a pre-validation clause audit of all thirteen traceability rows, `pytest --collect-only -q`, the complete `pytest -v` suite, and `python -m lecturepack --selftest` through `.venv\Scripts\python.exe`.
Non-goals: No test weakening, source fixes, Phase 2 reliability work, live Groq call, new dependency, real user data, or owner-specific packaged validation.
Completion evidence: Raw collection/full-suite/self-test/audit logs, reconciliation, requirement-traceability and source-validation reports, actual pytest output in the summary.
</task_scope>

<prohibition_probe_notes>
The recall pass was run across all Phase 1 requirements and the precision pass dropped routine correctness/hygiene items already owned by tests or code review. Generic injection, path-traversal, and broad compliance concerns are canon — covered by `$gsd-secure-phase` and existing security review; they are not minted here. The retained product-specific prohibitions across the three plans are descriptor-less and explicitly flagged unverified; no wired-check descriptor fields were invented.
</prohibition_probe_notes>

<tasks>

<task type="auto">
  <name>Task 1: Clause-audit release requirements and add deterministic alignment coverage</name>
  <read_first>
    docs/PRODUCT_SPEC.md
    docs/ARCHITECTURE.md
    docs/DECISIONS.md
    docs/IMPLEMENTATION_PLAN.md
    docs/TEST_PLAN.md
    .planning/REQUIREMENTS.md
    lecturepack/services/export_service.py
    lecturepack/services/transcript_store.py
    lecturepack/models/job.py
    tests/test_exports.py
    tests/test_integration.py
    tests/test_product_modes.py
    tests/test_packaging_and_safety.py
    tests/test_groq_transcription.py
    tests/test_study_workflow.py
    tests/test_transcription_backend_contract.py
    tests/test_transcript_formats.py
    tests/test_slide_detection.py
    tests/test_detection_targets.py
    tests/test_job_persistence.py
    tests/test_scheduler_and_engines.py
    tests/test_study_workspace_v12.py
    tests/test_stability_phase.py
    tests/test_ui_v11.py
  </read_first>
  <files>tests/test_alignment.py, docs/evidence/v1.2.0/release/REQUIREMENT_TRACEABILITY.md</files>
  <action>After reading every AGENTS.md-mandated project document, perform the clause audit before collection or release evidence is run. For each of the thirteen traceability rows enumerated in Task 3, split every sentence/list item in that requirement's `Description` and `Acceptance` fields in `.planning/REQUIREMENTS.md` into stable clause IDs (`&lt;REQ-ID&gt;:C01`, `C02`, ...). Inspect the actual body of every concrete candidate pytest node named in Task 3 and write an initial `REQUIREMENT_TRACEABILITY.md` entry for every clause with the exact node ID or targeted audit check, the concrete assertion category, and `STATUS: COVERED|GAP`; a test name or generic suite pass is not coverage. Include `PREVALIDATION_STATUS: PASS|GAP` and, for every gap, a concrete missing-contract statement. The known REQ-alignment gaps are authorized for closure in `tests/test_alignment.py`; any gap in the other twelve rows must remain a concrete blocking GAP and stop this plan before Task 2, because no other test or production file is authorized here. Do not silently broaden test scope.

Create `tests/test_alignment.py` as one focused module that exercises the public `ExportService.align_and_export` path with temporary `Job` data and reads the resulting `transcript/aligned.json`; do not extract or duplicate the production algorithm. Add exactly these concrete nodes: `test_alignment_assigns_segment_to_greatest_overlap` uses at least two adjacent slide intervals and a segment with unequal positive overlaps and asserts assignment to the interval with the larger overlap; `test_alignment_equal_overlap_tie_goes_to_earlier_slide` uses an exact equal-overlap boundary case and asserts the earlier chronological slide wins; `test_alignment_gives_every_slide_a_segment` includes a slide with no source dialogue and asserts every aligned slide has at least one segment, with the uncovered slide receiving the established `id == -1` placeholder; `test_alignment_maps_every_source_segment_exactly_once` uses multiple uniquely identified source segments, flattens non-placeholder aligned segments, and asserts every source ID appears once and only once across all slides. Re-audit the four REQ-alignment clauses against these exact nodes, and set pre-validation PASS only when all thirteen rows have clause-level executable coverage. Keep all media synthetic and under `tmp_path`, use the existing atomic JSON/job patterns, do not touch original videos, and do not modify production code or any existing test.</action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_alignment.py -v; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; $tr=Get-Content -Raw -LiteralPath 'docs\evidence\v1.2.0\release\REQUIREMENT_TRACEABILITY.md'; foreach($req in @('REQ-core-conversion','REQ-privacy-safety','REQ-transcription','REQ-slide-extraction','REQ-alignment','REQ-export-formats','REQ-job-lifecycle','REQ-study-workspace','REQ-provider-neutral-transcription','REQ-groq-transcription','REQ-stability','REQ-architecture-layers','REQ-test-framework')){if($tr -notmatch [regex]::Escape($req)){throw "pre-validation traceability row missing: $req"}}; if($tr -notmatch '(?m)^PREVALIDATION_STATUS: PASS$' -or $tr -match 'STATUS: GAP'){throw 'pre-validation clause audit has blocking gaps'}</automated>
  </verify>
  <acceptance_criteria>All four named nodes pass through the public alignment/export path and together assert every REQ-alignment clause: greatest overlap, equal tie to earlier, every slide covered, and every source segment assigned exactly once. The other twelve rows are audited against actual test bodies before collection; any missing clause is recorded as a concrete blocking GAP and prevents Task 2.</acceptance_criteria>
  <done>The known REQ-alignment acceptance gap is closed and all thirteen requirement rows have clause-level pre-validation coverage before collection and release validation begin.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile source test functions with collected pytest cases</name>
  <read_first>
    .planning/phases/01-packaging-release/01-RESEARCH.md
    .planning/phases/01-packaging-release/01-PATTERNS.md
    .planning/codebase/TESTING.md
    tests/test_stability_phase.py
    tests/test_ui_v11.py
    tests/test_alignment.py
    docs/evidence/v1.2.0/release/REQUIREMENT_TRACEABILITY.md
    pytest.ini
  </read_first>
  <files>docs/evidence/v1.2.0/release/test_collection_output.txt, docs/evidence/v1.2.0/release/TEST_RECONCILIATION.md, docs/evidence/v1.2.0/release/SOURCE_VALIDATION.md</files>
  <action>First require `REQUIREMENT_TRACEABILITY.md` to contain `PREVALIDATION_STATUS: PASS` and no `STATUS: GAP`; otherwise stop before running collection. Create `docs/evidence/v1.2.0/release/`, run the current virtual-environment interpreter with `pytest --collect-only -q`, capture the complete output and the real exit code, and fail the task on any collection/import error. The raw log must contain `COMMAND: .venv\Scripts\python.exe -m pytest --collect-only -q` and the actual `EXIT_CODE: N` in addition to unabridged process output; write both records before evaluating success so a failed collection remains durable. Before propagating a nonzero exit, initialize/update `SOURCE_VALIDATION.md` with the same exact command, exit code, and `COLLECTION_STATUS: PASS|FAIL`, linking the retained raw log. Count source `test_*` function definitions separately with a deterministic repository scan, then use the collector node IDs to identify every parametrized expansion. Write `TEST_RECONCILIATION.md` with the exact commands, current counts, and the concrete expansion explanation. Accept the observed collector result; do not modify tests to match a remembered number. Reconcile both the pre-plan baseline (149 functions plus two parametrized expansions = 151 items) and the current tree after Task 1 (four additional alignment functions/cases, therefore 153 functions and 155 items if no unrelated drift); record those values only if this run confirms them.</action>
  <verify>
    <automated>$e='docs\evidence\v1.2.0\release'; New-Item -ItemType Directory -Force -Path $e | Out-Null; $cmd='.venv\Scripts\python.exe -m pytest --collect-only -q'; $o=&amp; .venv\Scripts\python.exe -m pytest --collect-only -q 2&gt;&amp;1; $c=$LASTEXITCODE; @("COMMAND: $cmd") + $o + @("EXIT_CODE: $c") | Set-Content -Encoding utf8 "$e\test_collection_output.txt"; @('# Source Validation',"COLLECTION_COMMAND: $cmd","COLLECTION_EXIT_CODE: $c","COLLECTION_STATUS: $(if($c -eq 0){'PASS'}else{'FAIL'})",'COLLECTION_LOG: test_collection_output.txt') | Set-Content -Encoding utf8 "$e\SOURCE_VALIDATION.md"; if($c -ne 0){exit $c}; if(-not ($o -match '[0-9]+ tests collected')){throw 'pytest collection total missing'}; $raw=Get-Content -Raw -LiteralPath "$e\test_collection_output.txt"; if($raw -notmatch '(?m)^COMMAND: \.venv\\Scripts\\python\.exe -m pytest --collect-only -q$' -or $raw -notmatch '(?m)^EXIT_CODE: 0$'){throw 'collection command/exit record missing'}</automated>
  </verify>
  <acceptance_criteria>Collection exits zero; the unabridged output is saved; the report states the actual source-function and collected-item totals and accounts for every difference without changing the suite.</acceptance_criteria>
  <done>The historical count question is resolved by current pytest evidence and parameterized-item accounting.</done>
</task>

<task type="auto">
  <name>Task 3: Run the complete source suite and real development self-test</name>
  <read_first>
    .planning/phases/01-packaging-release/01-01-SUMMARY.md
    docs/evidence/v1.2.0/release/TEST_RECONCILIATION.md
    tests/test_alignment.py
    lecturepack/__main__.py
    lecturepack/app.py
    pytest.ini
  </read_first>
  <files>docs/evidence/v1.2.0/release/full_pytest_output.txt, docs/evidence/v1.2.0/release/development_selftest_output.txt, docs/evidence/v1.2.0/release/architecture_privacy_audit_output.txt, docs/evidence/v1.2.0/release/REQUIREMENT_TRACEABILITY.md, docs/evidence/v1.2.0/release/SOURCE_VALIDATION.md</files>
  <action>Run `.venv\Scripts\python.exe -m pytest -v` from the repository root and capture all stdout/stderr plus `COMMAND: .venv\Scripts\python.exe -m pytest -v` and the actual `EXIT_CODE: N` in `full_pytest_output.txt`. Write these records before evaluating success. Invoke the real module entry point with a finite 120-second subprocess deadline, not a shell invocation: use `Start-Process` with redirected stdout/stderr and `Wait-Process -Timeout 120` (or an equivalent short Python `subprocess.run(..., timeout=120)` driver). `development_selftest_output.txt` must record the exact `COMMAND`, `TIMEOUT_SECONDS: 120`, `TIMEOUT: true|false`, complete stdout/stderr, and actual `EXIT_CODE` (use 124 for a forced timeout) before testing the result; require `TIMEOUT: false`, exit zero, `SELFTEST PASS`, and `LecturePack v1.2.0`.

Run a separate finite, read-only `.venv\Scripts\python.exe -c ...` AST/source audit and persist its exact command, `TIMEOUT_SECONDS`, assertion results, and actual exit code in `architecture_privacy_audit_output.txt`. The architecture category must parse imports under `lecturepack/`, classify UI/controller/service/infrastructure modules, and fail any dependency direction that violates the four-layer rule in `docs/ARCHITECTURE.md`; the privacy category must fail production use of `shell=True`/`os.system`, telemetry/analytics SDK imports, unapproved network-client modules outside the documented model-download/localhost/Groq adapters, university-portal targets, or plaintext API-key persistence outside `secret_store.py`. Record `ARCHITECTURE_CHECK: PASS|FAIL` and `PRIVACY_CHECK: PASS|FAIL`. Behavioral P3/P5/P7 and online-consent/secret assertions remain tied to the pytest nodes below; the audit is not a substitute for those tests.

Create `REQUIREMENT_TRACEABILITY.md` with the following thirteen rows, preserving each concrete node ID and assertion category:

| Requirement | Current executable mapping | Assertion categories |
|---|---|---|
| REQ-core-conversion | `tests/test_integration.py::test_integration`; `tests/test_product_modes.py::test_transcript_only_mode`; `tests/test_product_modes.py::test_slides_only_mode` | real JobController orchestration with mock external executables; persisted stage completion; mode-specific artifacts |
| REQ-privacy-safety | `tests/test_packaging_and_safety.py::test_export_and_reexport_never_delete_job_or_candidates`; `tests/test_groq_transcription.py::test_backend_requires_privacy_before_secret_or_upload`; `tests/test_groq_transcription.py::test_http_client_retries_429_redacts_key_and_uses_audio_only`; `tests/test_study_workflow.py::test_whisper_arg_construction`; targeted `PRIVACY_CHECK` | no deletion; consent before secret/upload; redaction/audio-only body; argument-list path safety; static telemetry/network/shell/plaintext-secret audit |
| REQ-transcription | `tests/test_integration.py::test_integration`; `tests/test_transcription_backend_contract.py::test_local_pipeline_keeps_canonical_raw_output_and_provider_provenance`; `tests/test_transcript_formats.py::test_srt_vtt_shape` | inspect/extract/transcribe stage completion; canonical raw outputs/provenance; timestamped formats |
| REQ-slide-extraction | `tests/test_slide_detection.py::test_slide_detection`; `tests/test_detection_targets.py::test_balanced_meets_targets`; `tests/test_detection_targets.py::test_no_fade_or_caption_false_positive` | real deterministic worker; ground-truth targets; transition/caption false-positive guards |
| REQ-alignment | `tests/test_alignment.py::test_alignment_assigns_segment_to_greatest_overlap`; `tests/test_alignment.py::test_alignment_equal_overlap_tie_goes_to_earlier_slide`; `tests/test_alignment.py::test_alignment_gives_every_slide_a_segment`; `tests/test_alignment.py::test_alignment_maps_every_source_segment_exactly_once` | greatest temporal overlap; exact equal-overlap tie to earlier slide; at least one segment per slide; every source segment assigned to exactly one slide |
| REQ-export-formats | `tests/test_exports.py::test_exports`; `tests/test_transcript_formats.py::test_serialize_registry_all_formats`; `tests/test_study_workspace_v12.py::test_study_exports_include_user_data_without_mutating_sources` | PDF/HTML/TXT/SRT/JSON artifacts; serializer registry; escaped annotated Study exports/provenance |
| REQ-job-lifecycle | `tests/test_job_persistence.py::test_job_persistence`; `tests/test_scheduler_and_engines.py::test_cancellation_stops_both_branches`; `tests/test_scheduler_and_engines.py::test_stage_cache_reused_when_settings_unchanged`; `tests/test_packaging_and_safety.py::test_export_and_reexport_never_delete_job_or_candidates` | atomic persistence/reopen; cancellation; resume/cache skip; original/candidate no-deletion |
| REQ-study-workspace | `tests/test_study_workspace_v12.py::test_deterministic_overview_uses_loaded_backend_and_old_job_empty_state`; `tests/test_study_workspace_v12.py::test_slide_bookmark_note_and_resume_survive_restart`; `tests/test_study_workspace_v12.py::test_completed_job_lands_on_study_and_quick_navigation_works`; `tests/test_study_workspace_v12.py::test_study_exports_include_user_data_without_mutating_sources` | deterministic overview; old-job empty state; atomic user state/restart; navigation; escaped exports and source immutability |
| REQ-provider-neutral-transcription | `tests/test_transcription_backend_contract.py::test_capability_contract_is_explicit_and_json_safe`; `tests/test_transcription_backend_contract.py::test_controller_accepts_injected_backend_without_provider_logic`; `tests/test_transcription_backend_contract.py::test_local_pipeline_keeps_canonical_raw_output_and_provider_provenance`; `tests/test_transcription_backend_contract.py::test_old_job_defaults_to_local_and_controller_records_provider_metadata` | QObject contract/capabilities; controller seam; canonical outputs; fail-closed local default and metadata |
| REQ-groq-transcription | `tests/test_groq_transcription.py::test_size_aware_chunk_plan_has_overlap_and_stays_in_order`; `tests/test_groq_transcription.py::test_merge_offsets_sorts_and_deduplicates_overlap`; `tests/test_groq_transcription.py::test_backend_mock_integration_resume_and_no_secret_persistence`; `tests/test_groq_transcription.py::test_controller_online_failure_falls_back_without_stopping_slide_branch` | 23 MiB-aware ordered overlap; timestamp merge/dedup; resumable fake-server cache/secret hygiene; local fallback and branch isolation |
| REQ-stability | `tests/test_stability_phase.py::test_context_repair_detach_is_nonblocking`; `tests/test_stability_phase.py::test_main_window_close_cancels_pipeline_and_repair_nonblocking`; `tests/test_stability_phase.py::test_owned_process_tree_terminated_but_unrelated_process_survives`; `tests/test_stability_phase.py::test_backend_actual_value_persists_and_reloads` | close latency; controller cancel; PID-scoped process trees; runtime backend persistence |
| REQ-architecture-layers | `tests/test_transcription_backend_contract.py::test_controller_accepts_injected_backend_without_provider_logic`; targeted `ARCHITECTURE_CHECK` | provider logic excluded from controller; whole-tree AST dependency-direction audit |
| REQ-test-framework | `tests/test_integration.py::test_integration`; `tests/test_slide_detection.py::test_slide_detection`; `tests/test_ui_v11.py::test_selected_tile_pixels_show_accent_outline` | pytest/pytest-qt collection; real controller integration with mock executables; real CV worker; pixel-level Qt assertion |

For every pytest mapping, require the exact node ID to appear in `test_collection_output.txt` and as `PASSED` in the current `full_pytest_output.txt`; for each targeted mapping require its PASS marker and exit zero. Consume the Task 1 clause audit rather than substituting a generic suite pass: require `PREVALIDATION_STATUS: PASS`, retain every stable clause ID (`<REQ-ID>:C01`, `C02`, ...), exact pytest node ID or targeted check, and concrete assertion category, then promote each clause to current-run `STATUS: COVERED` only when its mapped node is collected and PASSED or its targeted audit passes with exit zero. The four REQ-alignment clauses must map to the four exact Task 1 node IDs above. If a prevalidated node is absent/did not pass or a targeted audit fails, change that clause to `STATUS: GAP`, write the concrete missing contract in both `REQUIREMENT_TRACEABILITY.md` and `SOURCE_VALIDATION.md`, persist all raw logs, and fail without modifying production source or any test beyond `tests/test_alignment.py`. Only when every clause under all thirteen rows retains executable current-run coverage may the report state `OVERALL_STATUS: PASS`. Keep live Groq validation and clean-machine real-video proof explicitly deferred; fake-server/current-suite evidence must not be mislabeled as either.

Write or update `SOURCE_VALIDATION.md` immediately after each command, including failing/timeout results, before any nonzero exit is propagated. It must record exact commands, timeouts, exit codes, collected/passed counts, warning summary, traceability status, audit status, and links to all raw logs. Do not invoke `--run-packaged-validation`, do not use user data or real media, and do not make a live provider request.</action>
  <verify>
    <automated>$e='docs\evidence\v1.2.0\release'; $pcmd='.venv\Scripts\python.exe -m pytest -v'; $p=&amp; .venv\Scripts\python.exe -m pytest -v 2&gt;&amp;1; $pc=$LASTEXITCODE; @("COMMAND: $pcmd") + $p + @("EXIT_CODE: $pc") | Set-Content -Encoding utf8 "$e\full_pytest_output.txt"; $out="$e\selftest.stdout.tmp"; $err="$e\selftest.stderr.tmp"; $scmd='.venv\Scripts\python.exe -m lecturepack --selftest'; $proc=Start-Process -FilePath '.venv\Scripts\python.exe' -ArgumentList @('-m','lecturepack','--selftest') -PassThru -NoNewWindow -RedirectStandardOutput $out -RedirectStandardError $err; $to=$false; try{Wait-Process -Id $proc.Id -Timeout 120 -ErrorAction Stop}catch{$to=$true; Stop-Process -Id $proc.Id -Force -ErrorAction SilentlyContinue; Wait-Process -Id $proc.Id -ErrorAction SilentlyContinue}; $proc.Refresh(); $sc=if($to){124}else{$proc.ExitCode}; $s=@(); if(Test-Path -LiteralPath $out){$s+=Get-Content -LiteralPath $out}; if(Test-Path -LiteralPath $err){$s+=Get-Content -LiteralPath $err}; @("COMMAND: $scmd","TIMEOUT_SECONDS: 120","TIMEOUT: $($to.ToString().ToLowerInvariant())") + $s + @("EXIT_CODE: $sc") | Set-Content -Encoding utf8 "$e\development_selftest_output.txt"; Remove-Item -LiteralPath $out,$err -Force -ErrorAction SilentlyContinue; @('','## Verification command results',"FULL_PYTEST_COMMAND: $pcmd","FULL_PYTEST_EXIT_CODE: $pc","SELFTEST_COMMAND: $scmd",'SELFTEST_TIMEOUT_SECONDS: 120',"SELFTEST_TIMEOUT: $($to.ToString().ToLowerInvariant())","SELFTEST_EXIT_CODE: $sc",'FULL_PYTEST_LOG: full_pytest_output.txt','SELFTEST_LOG: development_selftest_output.txt') | Add-Content -Encoding utf8 "$e\SOURCE_VALIDATION.md"; $fl=Get-Content -Raw -LiteralPath "$e\full_pytest_output.txt"; $sl=Get-Content -Raw -LiteralPath "$e\development_selftest_output.txt"; $al=Get-Content -Raw -LiteralPath "$e\architecture_privacy_audit_output.txt"; $tr=Get-Content -Raw -LiteralPath "$e\REQUIREMENT_TRACEABILITY.md"; if($pc -ne 0 -or $fl -notmatch '(?m)^EXIT_CODE: 0$'){exit $(if($pc -ne 0){$pc}else{1})}; if($to -or $sc -ne 0 -or $sl -notmatch '(?m)^TIMEOUT: false$' -or $sl -notmatch 'SELFTEST PASS' -or $sl -notmatch 'LecturePack v1\.2\.0'){exit 1}; if($al -notmatch '(?m)^ARCHITECTURE_CHECK: PASS$' -or $al -notmatch '(?m)^PRIVACY_CHECK: PASS$' -or $al -notmatch '(?m)^EXIT_CODE: 0$'){exit 1}; foreach($req in @('REQ-core-conversion','REQ-privacy-safety','REQ-transcription','REQ-slide-extraction','REQ-alignment','REQ-export-formats','REQ-job-lifecycle','REQ-study-workspace','REQ-provider-neutral-transcription','REQ-groq-transcription','REQ-stability','REQ-architecture-layers','REQ-test-framework')){if($tr -notmatch [regex]::Escape($req)){throw "traceability row missing: $req"}}; if($tr -notmatch '(?m)^OVERALL_STATUS: PASS$' -or $tr -match 'STATUS: GAP'){exit 1}</automated>
  </verify>
  <acceptance_criteria>The full suite exits zero with zero failures/errors and no regression warning; every raw log preserves exact command and exit/timeout records before evaluation; the development self-test finishes inside 120 seconds and prints the required PASS/version markers; both targeted audits pass; every Description/Acceptance clause under all thirteen verified-not-built requirements is backed by a current PASSED node ID or targeted audit assertion, including the four exact alignment nodes, or the task records a blocking GAP and fails.</acceptance_criteria>
  <done>All current source tests and the real development launch self-test pass with retained evidence from this execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process to release evidence | Console output becomes the auditable basis for a release decision and must retain command provenance and failures. |
| Headless self-test to filesystem/network | The self-test must remain inside a temporary data directory and avoid real media, user state, secrets, and provider traffic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Spoofing | pytest/self-test evidence | high | mitigate | Capture output directly from the current commands with their real exit codes; do not substitute historical or mocked logs. |
| T-01-06 | Tampering | test suite | high | mitigate | Validation tasks do not authorize test weakening/deletion; any failure blocks and routes to a separately scoped plan. |
| T-01-07 | Repudiation | collection reconciliation | medium | mitigate | Record exact commands, collector count, function count, parameter expansions, and raw output in the release evidence directory. |
| T-01-08 | Information Disclosure | self-test/evidence | high | mitigate | Use `--selftest` only, which creates a temporary data directory; prohibit owner-specific validation, live Groq access, and secret reads. |
| T-01-09 | Denial of Service | Qt/process tests | medium | mitigate | Use the existing finite test timeouts and require all workers/processes to reach terminal states before accepting the suite. |
</threat_model>

<verification>
Confirm all seven evidence files exist and are non-empty, each raw log contains exact `COMMAND` and actual `EXIT_CODE` records, `TEST_RECONCILIATION.md` matches the raw collector total, `full_pytest_output.txt` ends in a passing summary, the finite self-test log records `TIMEOUT: false` plus `SELFTEST PASS` for 1.2.0, both targeted audits pass, every one of the thirteen traceability rows points to current-run PASSED evidence, and `SOURCE_VALIDATION.md` retains failure details before any blocked exit.
</verification>

<success_criteria>
- Current pytest collection is explained without changing tests.
- The complete source suite has zero failures and zero errors.
- The development module self-test passes at release 1.2.0.
- All thirteen verified-not-built requirements have clause-level executable current-run traceability; any uncovered Description/Acceptance clause is a visible blocking gap.
- Architecture/privacy targeted audits pass with durable command/timeout/exit evidence.
- Evidence was produced by the current commands and contains no live-provider claim.
- Every unresolved spec-less edge assigned to this plan is present as a truth/backstop or visible flagged assumption.
</success_criteria>

<output>
Create `.planning/phases/01-packaging-release/01-02-SUMMARY.md` when done.
</output>
