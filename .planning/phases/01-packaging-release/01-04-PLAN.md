---
phase: 01-packaging-release
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - README-FIRST.txt
  - RELEASE_NOTES.md
  - THIRD_PARTY_NOTICES.txt
  - build_release.py
  - tests/test_packaging_and_safety.py
autonomous: true
gap_closure: true
requirements:
  - REQ-version-bump
  - REQ-packaged-build
  - REQ-test-framework
must_haves:
  truths:
    - "Every document copied into the portable release identifies LecturePack v1.2.0 in its current-release heading or banner per D-05."
    - "The build tool's human-facing label identifies v1.2.0 and remains a non-authoritative consumer of lecturepack.__version__."
    - "The v1.2.0 release notes describe the Study workspace, provider-neutral/private-local default, opt-in Groq transcription, stability work, and portable release honestly without rewriting historical release sections."
    - "Focused regression tests enumerate the complete shipped-document set and fail when any current-release identity drifts from 1.2.0."
  flagged_assumptions:
    - "[edge:REQ-version-bump:idempotency] The identity gate is a deterministic read-only invariant; repeated test runs must produce the same result, but this plan does not claim a second real package rebuild until Plan 01-06."
    - "[edge:REQ-version-bump:concurrency] Concurrent immutable version imports were already proven by Plan 01-01/verification; this plan introduces no mutable version state and does not claim to resolve concurrent release-build behavior."
  prohibitions:
    - statement: "MUST NOT leave a shipped current-release banner, heading, or build-tool label identifying an obsolete LecturePack release."
      status: unresolved
      flagged: true
    - statement: "MUST NOT modify an original lecture video, user job data, transcript, slide, credential, or provider configuration while correcting release identity."
      status: unresolved
      flagged: true
  artifacts:
    - path: "README-FIRST.txt"
      provides: "v1.2.0 portable-release banner, contents, and limitations"
    - path: "RELEASE_NOTES.md"
      provides: "Current v1.2.0 release description followed by preserved historical notes"
    - path: "THIRD_PARTY_NOTICES.txt"
      provides: "v1.2.0 third-party-notices banner"
    - path: "tests/test_packaging_and_safety.py"
      provides: "Complete shipped-document and build-label identity regression coverage"
  key_links:
    - from: "lecturepack/__init__.py"
      to: "README-FIRST.txt, RELEASE_NOTES.md, THIRD_PARTY_NOTICES.txt"
      via: "focused test compares each current-release heading/banner with lecturepack.__version__"
      pattern: "1\\.2\\.0"
    - from: "build_release.py"
      to: "tests/test_packaging_and_safety.py"
      via: "static build-tool label is checked against the canonical imported VERSION"
      pattern: "LecturePack v1\\.2\\.0"
---

<objective>
Close the release-identity half of the verification failure by synchronizing every shipped current-release document and the build-tool label at v1.2.0 per D-05.

Purpose: Ensure the package a user extracts identifies itself as the same release as its executable, archive name, and manifest.
Output: Corrected v1.2.0 release documents plus focused drift tests.
</objective>

## Artifacts this phase produces

- A v1.2.0 first-read banner and accurate portable-runtime guidance.
- A top-level v1.2.0 release-notes section that preserves earlier release history.
- A synchronized v1.2.0 third-party-notices banner and build-tool label.
- Focused tests that enumerate all three shipped documents rather than checking only runtime constants.

<execution_context>
@C:/Users/marsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/marsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-packaging-release/01-CONTEXT.md
@.planning/phases/01-packaging-release/01-VERIFICATION.md
@.planning/phases/01-packaging-release/01-REVIEW.md
@.planning/phases/01-packaging-release/01-01-SUMMARY.md
@.planning/phases/01-packaging-release/01-03-SUMMARY.md
@docs/DECISIONS.md
</context>

<task_scope>
Authorized phase: Phase 1 - Packaging &amp; Release gap closure.
Exact goal: Correct the stale identities in every document copied by the release builder and in the builder's own human-facing label.
Permitted files: `README-FIRST.txt`, `RELEASE_NOTES.md`, `THIRD_PARTY_NOTICES.txt`, `build_release.py`, and `tests/test_packaging_and_safety.py` only.
Required tests: The two new named identity tests and the complete `tests/test_packaging_and_safety.py` module.
Non-goals: No builder cleanup/publication redesign yet, no Phase 2 architecture work, no dependency/network/provider change, no real media, and no generated release rebuild in this plan.
Required completion evidence: Focused and module-level pytest output, a scoped diff, and `01-04-SUMMARY.md`.
</task_scope>

<nyquist_warning>
No `01-VALIDATION.md` exists and `01-RESEARCH.md` has no Validation Architecture section. The user explicitly approved proceeding from `01-VERIFICATION.md`; both tasks therefore name exact automated tests, while real package proof remains mandatory in Plan 01-06.
</nyquist_warning>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Synchronize the portable banner, notices, and builder label</name>
  <read_first>
    AGENTS.md
    docs/PRODUCT_SPEC.md
    docs/ARCHITECTURE.md
    docs/DECISIONS.md
    docs/IMPLEMENTATION_PLAN.md
    .planning/phases/01-packaging-release/01-CONTEXT.md
    .planning/phases/01-packaging-release/01-VERIFICATION.md
    README-FIRST.txt
    THIRD_PARTY_NOTICES.txt
    build_release.py
    tests/test_packaging_and_safety.py
  </read_first>
  <files>README-FIRST.txt, THIRD_PARTY_NOTICES.txt, build_release.py, tests/test_packaging_and_safety.py</files>
  <behavior>
    - `test_shipped_release_banners_match_canonical_version` enumerates `README-FIRST.txt` and `THIRD_PARTY_NOTICES.txt`, derives the expected current identity from `lecturepack.__version__`, and fails on either stale banner.
    - The same test verifies the build script's human-facing header is synchronized while `build_release.VERSION` remains imported from the package authority.
    - Current guidance does not claim a CPU-only package when Vulkan is shipped as an optional side-by-side engine.
  </behavior>
  <action>After the required reads, add `test_shipped_release_banners_match_canonical_version` first, then make it pass. Update the `README-FIRST.txt` title and every current-release limitation heading to v1.2.0, and revise only stale current-package statements needed to describe the already-shipped CPU-primary/optional-Vulkan layout accurately. Update the `THIRD_PARTY_NOTICES.txt` title to identify LecturePack v1.2.0; preserve all license text and do not opportunistically change dependency versions not established by current release evidence. Synchronize the `build_release.py` module label at v1.2.0 while preserving `VERSION = __version__` as the executable authority. The test must read repository files with UTF-8, compare explicit expected banner text derived from the canonical version, and keep the complete shipped-document set visible for Task 2. Preserve unrelated comments/history and make no cleanup, copy, manifest, or publication behavior change in this task.</action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_packaging_and_safety.py::test_shipped_release_banners_match_canonical_version -v</automated>
  </verify>
  <acceptance_criteria>The focused test passes; the first-read banner, third-party-notices title, and build-tool label identify v1.2.0; runtime/build version authority remains imported from `lecturepack.__version__`; no license body or builder behavior changed.</acceptance_criteria>
  <done>The portable banner, notices, and builder label can no longer silently drift to the obsolete identities reported by verification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish accurate v1.2.0 release notes and close complete document coverage</name>
  <read_first>
    docs/ARCHITECTURE.md
    docs/DECISIONS.md
    .planning/REQUIREMENTS.md
    .planning/phases/01-packaging-release/01-CONTEXT.md
    .planning/phases/01-packaging-release/01-VERIFICATION.md
    RELEASE_NOTES.md
    README-FIRST.txt
    THIRD_PARTY_NOTICES.txt
    tests/test_packaging_and_safety.py
  </read_first>
  <files>RELEASE_NOTES.md, tests/test_packaging_and_safety.py</files>
  <behavior>
    - `test_all_shipped_release_documents_identify_v1_2_0` enumerates exactly `README-FIRST.txt`, `THIRD_PARTY_NOTICES.txt`, and `RELEASE_NOTES.md` and checks the designated current heading/banner in each.
    - The first release-notes heading is LecturePack v1.2.0 and its current section describes only capabilities already documented in v1.2 architecture/decisions.
    - Historical v1.1.0 and earlier sections remain release history rather than being relabeled.
  </behavior>
  <action>Add `test_all_shipped_release_documents_identify_v1_2_0` before editing the notes. The test must assert that its explicit shipped-document set is exactly the three documents copied by `build_release.py`, then validate each designated current heading/banner against `lecturepack.__version__`; older versions may remain only below the current release-notes section as history. Prepend a substantive `# LecturePack v1.2.0` section to `RELEASE_NOTES.md` describing the Study workspace/provenance, provider-neutral transcription with Private Local default, explicit-consent Groq options, non-blocking/process safety, current portable CPU plus optional Vulkan layout, and honest limitations. Do not claim live Groq validation, strict architecture conformance, code signing, bundled models, or clean-machine approval not present in evidence. Preserve the v1.1.0 and earlier sections unchanged beneath the new section. This completes D-05 across every shipped release document.</action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_packaging_and_safety.py::test_all_shipped_release_documents_identify_v1_2_0 tests/test_packaging_and_safety.py::test_shipped_release_banners_match_canonical_version -v</automated>
  </verify>
  <acceptance_criteria>Both focused tests pass; the exact three-document shipped set has a v1.2.0 current identity; the notes accurately describe existing v1.2 features and limitations; historical sections are preserved.</acceptance_criteria>
  <done>All source documents that will be copied into the next portable ZIP identify and describe v1.2.0 consistently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source release documents to portable archive | Human-facing files become authoritative user guidance once copied into the ZIP. |
| Release inputs and output paths to builder | Plan 01-05 will validate required files, reparse status, exact cleanup targets, backup moves, and staging before any mutation. |
| Staged release to published `dist-release` | Plan 01-05/01-06 will permit publication only after inventory, integrity, and frozen validation preserve the prior release. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-16 | Spoofing | Shipped release identity | high | mitigate | Exact shipped-document tests derive current headings/banners from `lecturepack.__version__` and Plan 01-06 inspects the rebuilt ZIP. |
| T-01-17 | Tampering | Required runtime/document inputs | high | mitigate | This plan makes all document identities valid; Plan 01-05 adds regular/non-empty/non-reparse preflight for the full runtime/document set before cleanup. |
| T-01-18 | Elevation of Privilege | Cleanup reparse/junction paths | high | mitigate | Plan 01-05 blocks any reparse path and limits mutation to exact resolved allowlisted repo children before delete/move. |
| T-01-19 | Tampering | Backup and staged publication | high | mitigate | Plan 01-05 transactionally backs up unexpected entries and publishes validated staging only after rollback state exists. |
| T-01-20 | Denial of Service | Partial/tampered release | high | mitigate | Plan 01-05 aggregates preflight errors and Plan 01-06 independently checks inventory, digests, documents, and both frozen launch paths. |
| T-01-21 | Repudiation | Prior-release preservation | medium | mitigate | Plan 01-05 tests failure paths and retains compact logs while leaving/restoring the previous release; Plan 01-06 retains raw current-run evidence. |
</threat_model>

<verification>
Run `.venv\Scripts\python.exe -m pytest tests/test_packaging_and_safety.py -v`, then inspect `git diff -- README-FIRST.txt RELEASE_NOTES.md THIRD_PARTY_NOTICES.txt build_release.py tests/test_packaging_and_safety.py`. Do not call the release builder in this plan.
</verification>

<success_criteria>
- D-05 is encoded as an exact automated invariant over all three shipped documents.
- Every current-release banner/heading and the build-tool label reports v1.2.0.
- Historical notes remain intact and current notes make no unsupported claims.
- The complete packaging/safety test module passes without rebuilding or mutating release outputs.
</success_criteria>

<output>
Create `.planning/phases/01-packaging-release/01-04-SUMMARY.md` when done.
</output>
