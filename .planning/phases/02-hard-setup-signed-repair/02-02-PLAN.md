---
phase: 02-hard-setup-signed-repair
plan: 02
type: tdd
wave: 1
depends_on: [02-01]
files_modified:
  - lecturepack/infrastructure/runtime_generation.py
  - lecturepack/infrastructure/runtime_inventory.py
  - lecturepack/services/runtime_bootstrap.py
  - lecturepack/services/runtime_diagnostics.py
  - tests/test_runtime_generation.py
  - tests/test_runtime_diagnostics.py
autonomous: true
requirements: [REPR-05, REPR-06, REPR-07, REPR-08, REPR-09]
must_haves:
  truths:
    - "A complete verified writable generation is selected by one canonical runtime-root resolver while the portable bundle remains read-only (D-21; REPR-07)."
    - "The four R1 ZIPs install exactly one complete generation: FFmpeg, Whisper CPU (including discovered `ggml-cpu-*.dll` members), base-English model, and runtime-required smoke audio; manifest/signature remain metadata only (R1/R2; REPR-05/06/07)."
    - "Staging validates canonical inventory, safe ZIP members, each file hash, and full admission before atomically replacing active.json (D-20, D-21; REPR-05/06/07)."
    - "Download, validation, permission, cancellation, activation, and post-activation admission failures retain/restore byte-identical previous generation and actionable journal evidence (D-12, D-13, D-22; REPR-08)."
    - "Cancellation is idempotent before the atomic pointer boundary; requests at the boundary complete that boundary and resolve deterministically (REPR-02/03/04/05/08 concurrency backstop)."
    - "A successful active generation forces canonical `assess(trigger='repair')` full admission, never a presence-only check (D-22; REPR-09)."
  artifacts:
    - "lecturepack/infrastructure/runtime_generation.py: ActiveGenerationResolver, RuntimeGenerationStore, GenerationJournal, safe_extract_verified_archive"
    - "runtime inventory/bootstrap extensions that consume the active-generation resolver"
  key_links:
    - "RuntimeBootstrapService, bridge collaborator construction, RuntimeDiagnosticsService, packaged smoke, and repair use `resolve_active_runtime_root(config)`; absent pointer alone uses the immutable bundle, while malformed or interrupted pointer/journal is setup-required with diagnostic evidence (R4)."
  prohibitions:
    - statement: "MUST NOT write repaired files into the immutable portable bundle, sys._MEIPASS, existing user jobs, videos, transcripts, or source-derived content."
      status: resolved
      verification: judgment
      resolution: "Task 2 acceptance criteria plus generation, bootstrap, and packaged-repair pytest suites require generation-only writes and preserve the immutable bundle."
    - statement: "MUST NOT use extractall, flattened component extraction, or per-file activation for the runtime archive."
      status: resolved
      verification: judgment
      resolution: "Task 1/2 acceptance criteria and `tests/test_runtime_generation.py` require strict streaming extraction and one atomic active pointer after full admission."
---

<objective>
Authorized phase: 02 — Hard Setup & Signed Repair. Deliver the transactional writable-generation foundation and make canonical admission consume it.

Purpose: A trusted archive must become a complete recoverable runtime generation, never a set of direct-to-live file replacements.
Permitted files: only listed infrastructure/bootstrap/test files. Required tests: generation transaction fault matrix; full pytest at wave end. Non-goals: UI, GitHub transport, bridge signals, manual repair, or any mutation of the portable bundle. Completion evidence: disposable damaged-generation rollback and Unicode/space-path admission tests.
</objective>

<execution_context>@C:/Users/marsh/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@docs/DECISIONS.md
@.planning/phases/02-hard-setup-signed-repair/02-CONTEXT.md
@.planning/phases/02-hard-setup-signed-repair/02-RESEARCH.md
@.planning/phases/02-hard-setup-signed-repair/02-PATTERNS.md
@lecturepack/infrastructure/runtime_inventory.py
@lecturepack/services/runtime_bootstrap.py</context>

<feature>
  <name>Atomic active runtime generation</name>
  <files>lecturepack/infrastructure/runtime_generation.py, lecturepack/infrastructure/runtime_inventory.py, lecturepack/services/runtime_bootstrap.py, lecturepack/services/runtime_diagnostics.py, tests/test_runtime_generation.py, tests/test_runtime_diagnostics.py</files>
  <behavior>Valid complete staging creates an immutable generation and atomically publishes an active pointer. Each injected failure or cancellation preserves the earlier pointer/content. Resolver selects only a complete verified active generation and repair admission executes full canonical validation.</behavior>
  <implementation>First write fault-injection tests; then create same-volume staging, strict allow-listed streaming ZIP extraction, journal recovery, atomic pointer replacement/restoration, and a single active-root resolver shared by inventory/bootstrap. Reuse `resolve_inventory` containment and RuntimeBootstrapService rather than a parallel health model.</implementation>
</feature>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify generation resolution and transactional failure behavior</name>
  <files>tests/test_runtime_generation.py</files>
  <read_first>Read `AGENTS.md`, `docs/PRODUCT_SPEC.md`, `docs/ARCHITECTURE.md`, `docs/DECISIONS.md`, `docs/IMPLEMENTATION_PLAN.md`, `tests/test_cuda_pack.py`, `tests/test_runtime_bootstrap.py`, `tests/test_runtime_packaged_smoke.py`, `lecturepack/infrastructure/runtime_inventory.py`, `lecturepack/services/runtime_bootstrap.py`, and the fixture/layout API from 02-01.</read_first>
  <behavior>- Empty pointer alone selects the immutable bundle; stale, partial, corrupt, noncanonical, wrong-schema/version, escaping, missing, or interrupted active pointers/journals yield setup-required and never select a generation.\n- Success publication switches one pointer only after all four R1 ZIP member sets reconstruct the frozen canonical inventory, including runtime-required `smoke/runtime-smoke.wav` and discovered Whisper CPU DLLs.\n- Bootstrap and RuntimeDiagnosticsService consume the identical resolver result: a valid active generation is used, while malformed pointer/journal produces setup-required diagnostic evidence without a bundled fallback.\n- Injected write/extract/hash/permission/pointer/admission failures, cancel-before-activation, and crashes after journal or pointer replacement preserve/restore the prior pointer and bytes.\n- Unicode-and-space writable roots remain valid `Path` inputs and repair trigger forces full admission.</behavior>
  <action>Create isolated tmp-path tests and injected filesystem/admission seams before implementation. Make R1/R2's exact member mapping and smoke-fixture-required admission explicit; make R4's canonical active pointer and journal schemas, first-install policy, resolver error behavior, journal recovery, and atomic Windows replace semantics explicit. Cover zero/one/many generation records, first-install/no-prior-generation failure, repeated recovery/idempotency, process-crash-after-journal, process-crash-after-pointer-replace, and cancel-at-safe-boundary races. Add narrow bootstrap/diagnostics tests that provide a valid active generation and assert both consumers use it, then malformed/missing pointer and journal cases that assert `SETUP_REQUIRED` plus diagnostic snapshot evidence without implicit bundle selection. Do not use an actual GitHub request, mutate package fixtures, or merely assert file presence.</action>
  <acceptance_criteria>Tests express exact previous-pointer and generation-byte expectations for every named failure class, prove the R1/R2 four-archive generation and smoke fixture are mandatory, prove valid active-generation selection by bootstrap and diagnostics, prove malformed/missing pointer or journal becomes setup-required diagnostic evidence without bundled fallback, and prove `trigger='repair'` invokes full rather than lightweight admission through R4's sole resolver.</acceptance_criteria>
  <verify><automated>python -m pytest tests/test_runtime_generation.py tests/test_runtime_diagnostics.py -q</automated></verify>
  <done>Generation contract tests fail for absent implementation and make activation semantics unambiguous.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement verified staging, journaled activation, and canonical root resolution</name>
  <files>lecturepack/infrastructure/runtime_generation.py, lecturepack/infrastructure/runtime_inventory.py, lecturepack/services/runtime_bootstrap.py, lecturepack/services/runtime_diagnostics.py, tests/test_runtime_generation.py, tests/test_runtime_diagnostics.py</files>
  <read_first>Read `AGENTS.md`, `docs/PRODUCT_SPEC.md`, `docs/ARCHITECTURE.md`, `docs/DECISIONS.md`, `docs/IMPLEMENTATION_PLAN.md`, Task 1 tests, `02-RESEARCH.md` Patterns 2–3 and R1–R4, `02-PATTERNS.md` runtime-generation guidance, `lecturepack/infrastructure/runtime_inventory.py`, and `lecturepack/services/runtime_bootstrap.py` once before editing.</read_first>
  <behavior>- `safe_extract_verified_archive` implements R1's exact four ZIP member rules, rejects traversal, duplicates, directories/symlinks/special entries, case collisions, missing/extra members, zero Whisper CPU DLLs, and configured compressed/uncompressed bombs.\n- `RuntimeGenerationStore` implements R4's canonical `active.json`/`repair-journal.json` schemas, fsyncs same-directory temporary JSON then uses `os.replace`, and retains/recover previous selection.\n- Active resolver integrates with inventory/bootstrap, RuntimeDiagnosticsService, packaged smoke, and repair: absent pointer alone uses immutable bundle fallback; malformed/missing pointer or journal yields setup-required evidence.</behavior>
  <action>Implement `ActiveGenerationResolver`/`resolve_active_runtime_root(config)`, `RuntimeGenerationStore`, `GenerationJournal`, and `safe_extract_verified_archive` using only R1's normalized forward-slash members from validated ReleaseManifest. Stream the four archive member sets to a private staging directory, containment-check each resolved destination, freeze concrete `ggml-cpu-*.dll` members through the canonical inventory, require the R2 smoke fixture, validate every hash/size and canonical admission before activation, and make R4 pointer replacement followed by revalidation the indivisible boundary. Wire RuntimeBootstrapService and RuntimeDiagnosticsService directly to this sole resolver; preserve bridge construction's bootstrap result rather than adding a second resolver. Implement R4's journal recovery and first-install rule exactly: no pointer uses the immutable bundle; malformed pointer or journal is setup-required; a failed first install leaves no pointer. On post-activation admission failure restore the previous pointer; never delete a prior generation during this phase. Keep bundle paths read-only and use `Path`/argument arrays for hostile Windows paths.</action>
  <acceptance_criteria>All Task 1 tests pass; R1/R2's exact complete generation is admitted only with the smoke fixture; normal bootstrap, diagnostics, packaging and repair resolve the same valid R4 active generation; malformed/missing pointer or journal produces setup-required diagnostic evidence with no implicit bundled fallback; no partial data activates.</acceptance_criteria>
  <verify><automated>python -m pytest tests/test_runtime_generation.py tests/test_runtime_bootstrap.py tests/test_runtime_diagnostics.py -q</automated></verify>
  <done>Transaction infrastructure atomically selects complete writable generations and preserves recoverability.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| validated archive→staging filesystem | Archive member names and bytes remain untrusted until constrained extraction and hashing. |
| staging→active pointer | Complete generation crosses the activation boundary. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-04 | Tampering/DoS | runtime_generation.py | high | mitigate | Strict allow-list extraction, bounds, containment, hashes, and fault-injection tests. |
| T-02-05 | Tampering/Availability | active.json/journal | high | mitigate | Same-volume atomic pointer publication, retained previous pointer, recovery and cancel-race tests. |
| T-02-06 | Tampering | active-root resolver | high | mitigate | Canonical root selection and full repair-trigger admission test. |
</threat_model>
<verification>Run targeted generation/bootstrap tests after each task, then `python -m pytest -q` at Wave 1 end.</verification>
<success_criteria>Only complete, fully admitted writable runtime generations can become active, and every failure preserves the prior runtime or stays safely gated.</success_criteria>

## Artifacts this phase produces
- `ActiveGenerationResolver`, `RuntimeGenerationStore`, `GenerationJournal`, and `safe_extract_verified_archive`.
- Active-generation inventory/bootstrap integration for `assess(trigger="repair")`.
- `tests/test_runtime_generation.py` transaction, recovery, archive, Unicode-path, and fault-matrix coverage.

<output>Create `.planning/phases/02-hard-setup-signed-repair/02-02-SUMMARY.md` when done</output>
