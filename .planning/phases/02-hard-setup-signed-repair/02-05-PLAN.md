---
phase: 02-hard-setup-signed-repair
plan: 05
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - scripts/build_signed_runtime_release.py
  - .github/workflows/release.yml
  - app/packaging/build.py
  - tests/test_runtime_packaged_repair.py
  - tests/test_runtime_packaged_smoke.py
  - tests/test_release_trust.py
  - .planning/phases/02-hard-setup-signed-repair/02-PACKAGED-UI-EVIDENCE.md
autonomous: false
requirements: [REPR-04, REPR-05, REPR-06, REPR-07, REPR-08, REPR-09]
must_haves:
  truths:
    - "Release engineering produces exactly all six AD-19 app-version assets: `LecturePack-{app_version}-RuntimeManifest-v1.json`, its raw-signature `.sig`, and exact ffmpeg, whisper-cpu, model-base-en, and smoke-fixture ZIP names; the four ZIPs reconstruct R1's canonical complete generation (D-19, D-20, D-23; REPR-04/05/06)."
    - "The release workflow runs for both a `v*` tag push and manual dispatch selecting an existing `v{app_version}` tag; before signing/publishing each path resolves `refs/tags/v{app_version}^{commit}` and HEAD, rejects missing/ambiguous/mismatched full object IDs, and proves tag-without-v, checked-out commit, manifest app_version, and `app/desktop/version.py::__version__` agree (R1/R4; REPR-04/05)."
    - "The signing workflow keeps private key material out of repository, package, diagnostics, logs, and artifacts; it records manifest/archive/wheel hashes and requires release-owner secret custody/rotation responsibility (D-19, D-17)."
    - "A disposable frozen onedir self-test loads compiled trust material, accepts the frozen vector, rejects an altered byte, and proves complete repaired active generation passes canonical admission under Unicode-and-space paths (D-21–23; REPR-07/08/09)."
    - "Final evidence includes real packaged command argument vector, exit code, duration, stdout/stderr, damage/rollback/cancel cases, final full-suite output, and durable packaged UI evidence for every gate state, both themes, narrow/high-DPI, reduced motion, input containment, and safe-boundary cancellation; mocks do not substitute for packaged proof (D-23, D-24)."
    - "[flagged assumption] REPR-06's unclassified probe is treated as explicit release-owner assurance that only this fixed layout is signed/published; no unsigned auxiliary asset is accepted."
    - "[flagged assumption] REPR-07's unclassified probe is treated as explicit first-install behavior: no prior generation leaves active pointer absent and the setup gate remains active."
    - "[flagged assumption] REPR-09's unclassified probe is treated as exact post-admission automatic-entry behavior owned by the bridge/UI; packaged proof verifies it rather than assuming it."
  artifacts:
    - "scripts/build_signed_runtime_release.py: build_signed_runtime_release CLI"
    - ".github/workflows/release.yml: release asset/signing responsibilities"
    - "tests/test_runtime_packaged_repair.py: frozen onedir repair evidence harness"
  key_links:
    - "Release builder consumes canonical inventory/layout and emits the exact six-name bytes ReleaseTrustVerifier authenticates; both workflow paths peel and compare the exact tag commit with HEAD before signing/publishing; app/packaging/build.py packaged smoke, RuntimeRepairService, RuntimeBootstrapService, and diagnostics consume the same active-root resolver contract."
  prohibitions:
    - statement: "MUST NOT publish private signing keys, secrets, unsigned alternate runtime assets, bundled user/job/demo state, or release evidence that masks failed packaged proof."
      status: resolved
      verification: judgment
      resolution: "Task 1 release-trust tests and Task 2 packaged tests prevent key/asset leakage and unsigned output; Task 3 records the non-mocked packaged UI evidence and final suite result."
---

<objective>
Authorized phase: 02 — Hard Setup & Signed Repair. Make the approved trust contract reproducible in release engineering and prove it in a real frozen disposable package.

Purpose: A production verifier is insufficient without a signing workflow and packaged evidence that the exact assets can repair a runtime safely.
Permitted files: only listed release script/workflow/packaging/test/evidence files. Required tests: frozen verifier, disposable repaired-runtime smoke, release-trigger binding, and final full pytest. Non-goals: publication, physical-machine matrix, broad Phase 5 release work, external network test dependence, or changes to portable bundle contents in place. Completion evidence: release builder audit records, real packaged Unicode+space-path repair output, and the blocking packaged UI evidence record.
</objective>

<execution_context>@C:/Users/marsh/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@docs/DECISIONS.md
@.planning/phases/02-hard-setup-signed-repair/02-CONTEXT.md
@.planning/phases/02-hard-setup-signed-repair/02-RESEARCH.md
@.planning/phases/02-hard-setup-signed-repair/02-VALIDATION.md
@app/packaging/build.py
@tests/test_runtime_packaged_smoke.py
@tests/test_release_trust.py</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Automate canonical runtime release construction and signer responsibilities</name>
  <files>scripts/build_signed_runtime_release.py, .github/workflows/release.yml, tests/test_release_trust.py</files>
  <read_first>Read `AGENTS.md`, `docs/PRODUCT_SPEC.md`, `docs/ARCHITECTURE.md`, `docs/DECISIONS.md`, `docs/IMPLEMENTATION_PLAN.md`, AD-19 in `docs/DECISIONS.md`, R1/R4 in `02-RESEARCH.md`, `app/desktop/version.py`, `app/packaging/build.py`, `.github/workflows/release.yml`, canonical inventory/layout fixtures, `release_trust.py`, and test-release-trust before editing.</read_first>
  <behavior>- Builder accepts an explicit exact application version and canonical runtime root, emits exactly the six R1 fixed exact-version names (metadata manifest/signature plus four ZIPs), and refuses inventory drift or noncanonical manifest bytes.\n- Workflow triggers on `v*` tag push and manual dispatch selecting an existing `v{app_version}` tag. Before any signing/publishing command, each route resolves the tag's peeled commit using `git rev-parse "refs/tags/v${APP_VERSION}^{commit}"`, resolves `git rev-parse HEAD`, and rejects a missing, ambiguous, or byte-for-byte unequal full object ID.\n- Only after that equality check, both routes prove stripped tag, checked-out commit, manifest app_version, and `version.py::__version__` agree.\n- Workflow obtains signing material only from the approved release-owner secret boundary and emits audit hashes/build log without private material.\n- Tests compare built output to frozen verifier/layout requirements and fail for changed/extra/missing inventory, asset name/count, bad manual tag, missing/ambiguous tag, or tag/commit/application-version disagreement on either trigger route.</behavior>
  <action>Create `build_signed_runtime_release` with explicit CLI flags for version, input runtime root, output directory, and secure signer input appropriate to CI secret injection; derive archive membership exclusively from canonical inventory/release-layout, generate exactly R1's four ZIPs plus `LecturePack-{app_version}-RuntimeManifest-v1.json` and its raw `.sig`, generate canonical UTF-8 manifest bytes from the four ZIP records, sign those exact bytes as raw detached Ed25519, and record SHA-256/size evidence. Modify `.github/workflows/release.yml` to trigger on `push.tags: v*` and manual `workflow_dispatch` selecting an existing `v{app_version}` tag. Normalize both inputs to one `APP_VERSION` and exact tag ref; before any signing or publishing command, resolve the annotated-or-lightweight peeled ref with the equivalent of `git rev-parse "refs/tags/v${APP_VERSION}^{commit}"`, resolve `git rev-parse HEAD`, require each result to be one full object ID, and fail closed if the tag is missing, ambiguous, or the IDs differ byte-for-byte. Only then allow version/manifest agreement checks, signing, asset upload, or publishing; reject a manual input that does not name an existing matching tag. Pin the approved dependency, verify package/wheel hashes at install, call the builder after inventory checks, upload only the six permitted assets/evidence, and document key custody/rotation/revocation responsibility. Never place a private key in source, test fixture, artifact, command echo, or diagnostic log; never select latest release or add an unsigned asset.</action>
  <acceptance_criteria>Tests prove builder output is accepted by ReleaseTrustVerifier, altered bytes fail, all six and only six R1 assets have exact versioned names, archive membership equals canonical inventory, and each `push.tags: v*` and manual-dispatch route peels `refs/tags/v{app_version}` to a full commit, compares it exactly to HEAD before signing/publishing, rejects missing/ambiguous/mismatched tags, then requires tag/manifest/application-version agreement. Workflow text contains no private-key literal or broad asset/release selection.</acceptance_criteria>
  <verify><automated>python -m pytest tests/test_release_trust.py -q</automated></verify>
  <done>Release owners have deterministic, auditable instructions/tooling to produce the exact assets the app will trust.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Prove frozen verifier and repaired-runtime behavior in disposable packages</name>
  <files>app/packaging/build.py, tests/test_runtime_packaged_repair.py, tests/test_runtime_packaged_smoke.py, tests/test_release_trust.py</files>
  <read_first>Read `AGENTS.md`, `docs/PRODUCT_SPEC.md`, `docs/ARCHITECTURE.md`, `docs/DECISIONS.md`, `docs/IMPLEMENTATION_PLAN.md`, `tests/test_runtime_packaged_smoke.py`, `tests/test_runtime_generation.py`, `tests/test_runtime_repair.py`, `tests/test_setup_gate_repair.py`, `02-VALIDATION.md`, and Task 1 release output contract.</read_first>
  <behavior>- A real onedir fixture copied into a Unicode-and-space installation/data path loads the compiled public key, accepts frozen vector, and rejects altered manifest byte.\n- `app/packaging/build.py` disposable smoke resolves the same active runtime root as bootstrap, diagnostics, and repair: a valid active generation is used; malformed/missing active pointer or journal produces setup-required/diagnostic evidence and never silently uses the bundled payload.\n- A disposable damaged runtime repairs through staged active generation, runs real canonical required-runtime smoke, records argv/exit/duration/stdout/stderr, and enters only after admitted.\n- Separate cancellation/validation/activation damage cases leave prior generation intact; fixture absence fails explicitly rather than skipping evidence.</behavior>
  <action>Build a non-mock packaged harness around the supplied clean onedir fixture and a local fixed-asset test server or fixture transport that preserves the production exact-URL contract. Modify `app/packaging/build.py` packaged smoke to call the sole `resolve_active_runtime_root(config)` contract, not a second resolver or a direct bundled-root fallback. Copy the fixture into hostile Unicode-and-space install and writable-data paths, create a valid active generation and prove the packaging smoke, RuntimeBootstrapService, RuntimeDiagnosticsService, and repair use it; then provide malformed/missing pointer and journal fixtures and prove setup-required plus diagnostic evidence, with no implicit bundled fallback. Damage one canonical component in disposable copies, execute the real signed repair consumer, and capture subprocess evidence fields from RuntimeValidator. Include frozen verifier checks in the packaged process, prior-generation rollback/cancellation assertions, and manifest/archive/wheel/build hashes. Do not use developer PATH/Python/Node tools as runtime proof, mutate the source fixture, skip when the fixture is missing, or call public GitHub.</action>
  <acceptance_criteria>Harness fails if compiled verifier/vector is missing, if valid repaired generation is not selected by the canonical resolver in bootstrap, diagnostics, packaged smoke, and repair, if malformed/missing pointer or journal silently falls back to the bundle instead of yielding setup-required diagnostic evidence, if real smoke lacks any captured evidence field, if prior generation changes after injected failure, or if package path handling is not Unicode/space safe.</acceptance_criteria>
  <verify><automated>python -m pytest tests/test_runtime_packaged_repair.py tests/test_runtime_packaged_smoke.py tests/test_runtime_diagnostics.py tests/test_release_trust.py -q</automated></verify>
  <done>Phase 2 has real frozen-package trust and repaired-runtime evidence, ready for Phase 5's broader physical-release matrix.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Record final packaged setup-gate evidence</name>
  <files>.planning/phases/02-hard-setup-signed-repair/02-PACKAGED-UI-EVIDENCE.md</files>
  <what-built>A disposable packaged build whose automated suites have exercised the release, resolver, repair, and setup-gate contracts.</what-built>
  <how-to-verify>1. Run the disposable packaged build produced by Task 2 and record its build hash, application version, and installed path in `.planning/phases/02-hard-setup-signed-repair/02-PACKAGED-UI-EVIDENCE.md`. 2. In both existing themes, capture screenshots or logs for gate, consent, repairing, cancellation, offline, failed, diagnostics, and ready. 3. At a narrow supported window and high-DPI setting, confirm no clipping and all actions remain reachable. 4. With reduced motion enabled, confirm progress/ready remain understandable. 5. Record keyboard, focus, pointer, and scroll containment; then cancel at an announced safe boundary and record that the previous generation remains selected until the matching cancellation result.</how-to-verify>
  <action>After Task 2's disposable packaged build and automated tests, create the one durable evidence record at the listed path. Include build hash/version/path, screenshot or captured-log references, pass/fail notes for every required state and both themes, narrow/high-DPI and reduced-motion observations, focus/pointer/keyboard/scroll containment observations, and safe-boundary cancellation evidence. Do not add a visual-test framework or substitute mocked UI output for the packaged session.</action>
  <acceptance_criteria>The evidence record exists and identifies the packaged build; it contains affirmative evidence for gate, consent, repairing, cancellation, offline, failed, diagnostics, ready, both themes, narrow/high-DPI, reduced motion, input containment, and cancellation at a safe boundary.</acceptance_criteria>
  <verify><automated>python -m pytest tests/test_setup_gate_repair.py tests/test_runtime_packaged_repair.py tests/test_runtime_packaged_smoke.py -q</automated><human-check>Review `.planning/phases/02-hard-setup-signed-repair/02-PACKAGED-UI-EVIDENCE.md` against the listed packaged observations.</human-check></verify>
  <done>Blocking human verification supplies durable packaged UI evidence without expanding the testing stack.</done>
  <resume-signal>Type "approved" after reviewing the packaged UI evidence record, or describe the failed observation.</resume-signal>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| release owner/CI secret→signed assets | Private signing authority creates public release artifacts. |
| frozen package→disposable test environment | Packaged verifier and runtime execute outside developer tooling. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-15 | Spoofing/Tampering | release workflow | high | mitigate | Exact inventory builder, secret-only signer, hash/build evidence, frozen verifier vectors. |
| T-02-19 | Tampering | release trigger/version binding | high | mitigate | Require both tag and manual existing-tag paths to verify stripped tag, checked-out commit, manifest version, and application version before signing/publishing the six exact assets. |
| T-02-16 | Information disclosure | CI/log artifacts | high | mitigate | Redact/forbid private key and sensitive diagnostics; test workflow/script sources. |
| T-02-17 | Tampering | packaged repair evidence | high | mitigate | Disposable onedir frozen-vector and real smoke tests under hostile paths. |
| T-02-18 | Availability | rollback/cancel evidence | high | mitigate | Damage/cancellation/activation fault matrix asserts retained active generation. |
</threat_model>
<verification>Run release-trust and packaged-repair suites after each task; finish Phase 2 with `python -m pytest -q` and record actual output, plus the required manual packaged gate checks from 02-VALIDATION.md.</verification>
<success_criteria>Exact signed release assets are reproducible and the compiled application proves signed repair, rollback, revalidation, and no-restart entry in a disposable hostile-path package.</success_criteria>

## Artifacts this phase produces
- `build_signed_runtime_release` CLI and exact archive/manifest/signature output contract.
- `.github/workflows/release.yml` signing/release workflow and key-custody responsibilities.
- `tests/test_runtime_packaged_repair.py` frozen verifier, hostile path, damage, rollback, and real smoke evidence harness.
- Extended verifier tests for release-builder output.

<output>Create `.planning/phases/02-hard-setup-signed-repair/02-05-SUMMARY.md` when done</output>
