---
phase: 01-runtime-contract-bootstrap
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified: [lecturepack/services/runtime_bootstrap.py, lecturepack/infrastructure/config_manager.py, tests/test_runtime_bootstrap.py, tests/test_cuda_engine.py]
autonomous: true
requirements: [RUNT-03, RUNT-04, RUNT-06, RUNT-07, RUNT-08]
must_haves:
  truths:
    - Only complete validated facts become healthy persistent state.
    - The beta-6 migration occurs exactly once and preserves later manual choices.
    - Optional selection cannot prevent healthy CPU admission.
  artifacts: [lecturepack/services/runtime_bootstrap.py, tests/test_runtime_bootstrap.py]
  key_links: [RuntimeBootstrapService consumes Wave 0 inventory/validator and ConfigManager atomic writes.]
---
<objective>Implement admission policy, persistence, exact migration, and optional fallback. Purpose: turn Wave 0 evidence into a durable CPU health decision.</objective>
<execution_context>@C:/Users/marsh/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@.planning/phases/01-runtime-contract-bootstrap/01-CONTEXT.md
@.planning/phases/01-runtime-contract-bootstrap/01-RESEARCH.md
@.planning/phases/01-runtime-contract-bootstrap/01-VALIDATION.md
@.planning/phases/01-runtime-contract-bootstrap/01-01-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Implement validated admission and one-time beta-6 migration</name><files>lecturepack/services/runtime_bootstrap.py, lecturepack/infrastructure/config_manager.py, tests/test_runtime_bootstrap.py</files><behavior>- Fresh/stale/partial/identity-changed/update/repair state uses applicable full validation; unchanged healthy state has light checks per D-03/D-04.
- With no `migration_versions.runtime_contract`, a healthy optional selection is preserved, base-English becomes default once, a different old model enters `known_whisper_models`, and one atomic healthy write includes marker 1.
- CPU failure writes no healthy facts; marker 1 never resets later manual model selection or repeats fallback migration.</behavior><action>Test first, then implement RuntimeBootstrapService with ASSESSING/HEALTHY/SETUP_REQUIRED and immutable component evidence from Wave 0. Persist only after the complete required set validates. Implement the exact resolved semantics: gate on `migration_versions.runtime_contract = 1`; absent marker atomically writes healthy facts, marker, canonical bundled `ggml-base.en.bin` default, and retained different prior model; marker 1 leaves later `whisper_model` choice untouched. Force full smoke for fresh/identity/update/repair and use light checks otherwise. Preserve unrelated comments/docstrings and never alter original video/user data/portable payload/main worktree; do not add setup/repair or Phase 2+ behavior per D-03/D-04/D-06/D-09/D-14.</action><verify><automated>pytest tests/test_runtime_bootstrap.py -q</automated></verify><done>All durable state is valid, atomic, and migration-safe.</done></task>
<task type="auto" tdd="true"><name>Task 2: Implement optional-engine preservation and bounded fallback</name><files>lecturepack/services/runtime_bootstrap.py, tests/test_runtime_bootstrap.py, tests/test_cuda_engine.py</files><behavior>- Healthy CUDA/custom preference survives independent CPU admission per D-07.
- Missing/broken optional preference resolves to CPU with requested/resolved/reason structured notice once per migration per D-08.
- Admission never probes optional providers or network.</behavior><action>After CPU admission only, independently validate/retain a healthy optional preference or select validated CPU and emit one structured visible notice. Test healthy CUDA/custom, missing executable/DLL/driver, unavailable Vulkan, no network calls, and repeated/concurrent assessments. Do not alter desktop composition here. Preserve unrelated comments/docstrings and protected user/original-video data; no setup/repair/download implementation per D-07/D-08/D-14.</action><verify><automated>pytest tests/test_runtime_bootstrap.py tests/test_cuda_engine.py -q; pytest -q</automated></verify><done>Optional failures cannot block CPU entry and emit auditable fallback facts.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| validation result to config | only complete trusted evidence may become durable health |
| saved optional preference to resolver | historical preference is untrusted availability input |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-03 | Tampering | runtime_health | high | mitigate | ASVS-L1 V5 complete in-memory validation before atomic write; stale/partial/identity-change tests prevent unhealthy admission. |
| T-01-04 | Denial of service | optional resolution | high | mitigate | ASVS-L1 V5 CPU-first bounded validation and tested CPU fallback/no-network gate. |
</threat_model>
<verification>Run targeted tests, then pytest; retain actual output without weakening tests.</verification>
<success_criteria>RUNT-03/04/06/07/08 meet their durable policy contracts.</success_criteria>
<output>Create .planning/phases/01-runtime-contract-bootstrap/01-02-SUMMARY.md when done</output>
