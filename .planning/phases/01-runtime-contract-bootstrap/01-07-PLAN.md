---
phase: 01-runtime-contract-bootstrap
plan: 07
type: execute
wave: 4
depends_on: [01-06]
files_modified: [app/desktop/bridge.py, tests/test_adapter_startup.py, tests/test_runtime_diagnostics.py, docs/HANDOFF_PHASE_1.md]
autonomous: true
gap_closure: true
requirements: [RUNT-05]
assumption_delta_decision:
  noun: required runtime identity
  decision: no-change
  rationale: bundled CPU remains the canonical required runtime; optional engines remain recoverable variants.
must_haves:
  truths:
    - "D-05/D-06: In SETUP_REQUIRED, every exposed adapter- or updater-facing bridge slot returns a stable structured setup-required no-op and never dereferences a withheld collaborator."
    - "The existing bootstrap and diagnostics transport exposes canonical admission state without a frontend/web asset edit or any normal-ready side effect."
    - "Repeated setup-required calls retain stable state and do not start jobs, navigation, optional-engine probes, demo, update checks, or persistence."
  artifacts:
    - app/desktop/bridge.py
    - tests/test_adapter_startup.py
    - tests/test_runtime_diagnostics.py
    - docs/HANDOFF_PHASE_1.md
  key_links:
    - "Backend's centralized collaborator guard gates every adapter/updater slot before dereference and emits/returns the one structured setup-required transport shape."
    - "get_bootstrap() and get_runtime_health_snapshot() carry the controller-owned admission state without rebuilding inventory."
  prohibitions:
    - "[flagged/unverified] original lecture video modification"
    - "[flagged/unverified] user-data modification"
    - "[flagged/unverified] network request"
    - "frontend/web assets and existing animations, shadows, transitions, motion, theme, and visual behavior"
---

<objective>
Make the desktop bridge a safe, stable setup-required boundary and record truthful Phase 1 repair evidence.

Purpose: no exposed QWebChannel call may crash or reach normal application behavior when runtime admission is withheld.
Output: centralized admission guard, structured no-op/error transport regressions, canonical bootstrap admission state, and an updated honest handoff.
</objective>

<execution_context>
@C:/Users/marsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/marsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/PRODUCT_SPEC.md
@docs/ARCHITECTURE.md
@docs/DECISIONS.md
@docs/IMPLEMENTATION_PLAN.md
@docs/HANDOFF_PHASE_1.md
@.planning/phases/01-runtime-contract-bootstrap/01-CONTEXT.md
@.planning/phases/01-runtime-contract-bootstrap/01-REVIEW.md
@.planning/phases/01-runtime-contract-bootstrap/01-VERIFICATION.md
@.planning/phases/01-runtime-contract-bootstrap/01-03-SUMMARY.md
@.planning/phases/01-runtime-contract-bootstrap/01-06-SUMMARY.md
</context>

## Artifacts this phase produces

- `app/desktop/bridge.py`: `Backend._setup_required_payload(operation)`, `Backend._guard_admitted_operation(operation)`, guarded `_adapter`/`_updater` slot paths, and `Backend.get_bootstrap()` fields `theme`, `version`, `runtime_health_state`, and `setup_required` while `get_runtime_health_snapshot()` remains the controller-owned detailed payload.
- `tests/test_adapter_startup.py`: table-driven SETUP_REQUIRED coverage for every adapter/updater bridge slot, stable payload/no-op behavior, repeat-call idempotence, and healthy-path preservation.
- `tests/test_runtime_diagnostics.py`: bootstrap `runtime_health_state`/`setup_required` payload and no-independent-inventory-construction regressions.
- `docs/HANDOFF_PHASE_1.md`: actual targeted/full pytest output; fixture provenance; packaged smoke argv, exit, duration, stdout, stderr, and reason; fresh-tree, corrupt-model, launch-failure, VAD-staging, bridge-guard, remaining-blocker, and Phase-1-verification status evidence.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard every adapter and updater bridge call during setup-required admission</name>
  <files>app/desktop/bridge.py, tests/test_adapter_startup.py, tests/test_runtime_diagnostics.py</files>
  <read_first>app/desktop/bridge.py; app/desktop/engine_adapter.py; app/desktop/updater.py; tests/test_adapter_startup.py; tests/test_runtime_diagnostics.py; lecturepack/services/runtime_diagnostics.py</read_first>
  <behavior>
    - Each adapter-facing and updater-facing QWebChannel slot is safe when `_adapter` or `_updater` is withheld: it returns/announces one stable structured setup-required no-op rather than raising AttributeError.
    - `get_bootstrap()` includes the current canonical admission state and setup-required payload derived from the existing runtime diagnostics/controller transport; `get_runtime_health_snapshot()` remains the unchanged canonical detailed projection.
    - Repeated calls do not construct collaborators, write settings, run updater/network work, start normal processing, probe optional engines, navigate, or emit normal-ready/fallback semantics.
  </behavior>
  <action>Write table-driven bridge regressions that instantiate Backend with SETUP_REQUIRED and invoke every slot that dereferences `_adapter` or `_updater`, including settings, model/engine controls, import/job/queue/review/study/export actions, and every update action. Introduce one private centralized guard/helper that identifies the requested operation and returns or emits a stable JSON-safe `setup_required` payload from the existing bootstrap/diagnostics admission state before collaborator access. Adapt return-valued slots to return that structured response and void slots to emit through the existing diagnostics/status transport consistently, without adding a QWebChannel signal or touching frontend/web assets. Route `get_bootstrap()` through the existing controller-owned admission information, retain healthy behavior byte-for-byte, and verify no independent inventory construction. Preserve all current motion, animations, shadows, transitions, styling, and visuals by not editing UI assets; do not implement Phase 2 setup/repair actions, per D-05/D-06/D-14.</action>
  <acceptance_criteria>Every bridge slot that would otherwise dereference a withheld adapter/updater responds deterministically with setup-required state; healthy admission retains normal collaborator behavior.</acceptance_criteria>
  <verify><automated>pytest tests/test_adapter_startup.py tests/test_runtime_diagnostics.py -q</automated></verify>
  <done>SETUP_REQUIRED is a stable bridge state with no normal-operation crash or side effect.</done>
</task>

<task type="auto">
  <name>Task 2: Update the Phase 1 handoff with complete gap-closure evidence</name>
  <files>docs/HANDOFF_PHASE_1.md</files>
  <read_first>docs/HANDOFF_PHASE_1.md; .planning/phases/01-runtime-contract-bootstrap/01-VERIFICATION.md; .planning/phases/01-runtime-contract-bootstrap/01-REVIEW.md; .planning/phases/01-runtime-contract-bootstrap/01-05-SUMMARY.md; .planning/phases/01-runtime-contract-bootstrap/01-06-SUMMARY.md</read_first>
  <action>Update the handoff only after all targeted tests, real packaged Unicode/space smoke, and full suite complete. State exactly what Plans 01-05 through 01-07 repaired; include actual targeted/full pytest commands and output, real packaged smoke command/argv/evidence, fresh-tree package evidence, corrupt-model and launch-failure evidence, VAD staging evidence, bridge-slot guard coverage, and every remaining blocker. Explicitly keep Phase 1 in verification status and do not mark it complete, do not start Phase 2, and do not claim unrun physical/release evidence passed. Preserve unrelated documentation and the approved AD-19 decision record.</action>
  <acceptance_criteria>The handoff honestly distinguishes passed automated evidence, blocking packaged-fixture/physical evidence if any, and Phase 2 work that remains out of scope.</acceptance_criteria>
  <verify><automated>pytest tests/test_beta3_packaging.py tests/test_runtime_bootstrap.py tests/test_runtime_packaged_smoke.py tests/test_whisper_path_staging.py tests/test_adapter_startup.py tests/test_runtime_diagnostics.py -q; $fixture=$env:LECTUREPACK_ONEDIR_FIXTURE; if ([string]::IsNullOrWhiteSpace($fixture) -or -not [IO.Path]::IsPathRooted($fixture)) { throw 'Set LECTUREPACK_ONEDIR_FIXTURE to an explicitly supplied absolute clean onedir path.' }; $env:LECTUREPACK_ONEDIR_FIXTURE=(Resolve-Path -LiteralPath $fixture -ErrorAction Stop).Path; if (-not (Test-Path -LiteralPath $env:LECTUREPACK_ONEDIR_FIXTURE -PathType Container)) { throw 'Clean onedir fixture is absent.' }; python -c "import os; from pathlib import Path; from app.packaging.build import check_clean_state; p=Path(os.environ['LECTUREPACK_ONEDIR_FIXTURE']); v=check_clean_state(p); assert not v, v"; pytest tests/test_runtime_packaged_smoke.py -q; pytest -q</automated></verify>
  <done>Phase 1 has an accurate handoff without prematurely declaring the phase or later repair work complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| QWebChannel caller to Backend | Untrusted UI calls may arrive while normal collaborators are intentionally absent. |
| admission diagnostics to bootstrap transport | State must be canonical and cannot make normal readiness appear true. |
| bridge action to adapter/updater | Job, update, and configuration side effects must remain unreachable until HEALTHY. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-17 | Elevation | QWebChannel bridge slots | high | mitigate | Central guard covers every adapter/updater dereference and table-driven SETUP_REQUIRED slot tests prove no collaborator side effect. |
| T-01-18 | Denial of service | bridge admission error path | high | mitigate | Return/emit stable structured no-op responses rather than unhandled attribute failures; repeat-call tests enforce idempotent state. |
| T-01-19 | Tampering | bootstrap admission transport | medium | mitigate | Reuse controller/service diagnostics snapshot and test no secondary inventory projection or readiness transition. |
| T-01-SC | Tampering | dependency installs | high | mitigate | No package-manager installation is authorized in this plan. |
</threat_model>

## ASVS-L1 applicability and completion gate

ASVS-L1 applies to local authorization and availability at the QWebChannel caller → Backend, admission diagnostics → bootstrap payload, and bridge action → withheld collaborator boundaries. T-01-17 is controlled by exhaustive guarded-slot/no-side-effect acceptance tests; T-01-18 by repeat-call stable-payload acceptance tests; and the packaged/full-suite evidence protects the prerequisite admission state. Every high-severity mitigation acceptance test is execution-blocking: this plan cannot complete if any one fails.

<verification>
Run the targeted bridge, diagnostics, packaging, bootstrap, packaged-smoke, and staging tests. For the full-suite gate, use the explicit PowerShell sequence in Task 2: require an absolute supplied `LECTUREPACK_ONEDIR_FIXTURE`, resolve it safely, validate canonical `check_clean_state()` provenance, run real Unicode/space disposable packaged smoke, then run `pytest -q` under that environment. Record actual outputs, fixture provenance, and any unexecuted physical evidence in the handoff; never replace real packaged proof with mocks.
</verification>

<success_criteria>
No normal desktop behavior is reachable or crashable before HEALTHY, and the Phase 1 handoff accurately reports all repaired and still-blocked evidence without completing the phase.
</success_criteria>

<output>
Create `.planning/phases/01-runtime-contract-bootstrap/01-07-SUMMARY.md` when done.
</output>
