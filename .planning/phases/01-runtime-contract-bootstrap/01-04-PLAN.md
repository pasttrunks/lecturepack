---
phase: 01-runtime-contract-bootstrap
plan: 04
type: execute
wave: 1
depends_on: [01-01]
files_modified: [docs/DECISIONS.md, tests/test_signing_adr_contract.py]
autonomous: false
requirements: [RUNT-09]
must_haves:
  truths: [A named human approves the full signing contract, concrete approved vectors pass, and Phase 2 remains blocked until this is true.]
  artifacts: [docs/DECISIONS.md, tests/test_signing_adr_contract.py]
  key_links: [Approved ADR values are captured by static known-good and altered-byte contract tests.]
---
<objective>Obtain and record the RUNT-09 signing/verifier ADR without selecting or installing an unapproved verifier. Purpose: establish the Phase 2 trust contract.</objective>
<execution_context>@C:/Users/marsh/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@docs/DECISIONS.md
@.planning/phases/01-runtime-contract-bootstrap/01-CONTEXT.md
@.planning/phases/01-runtime-contract-bootstrap/01-RESEARCH.md</context>
<tasks>
<task type="auto"><name>Task 1: Draft the approval-sensitive ADR and passing pending-state contract</name><files>docs/DECISIONS.md, tests/test_signing_adr_contract.py</files><action>Append a dated ADR draft naming decision owner and requiring approval of verifier/version, algorithm, signature/public-key encoding, canonical UTF-8 sorted-key manifest schema/bytes, versioned asset names, public-key embedding, custody/backup/rotation/revocation/incident communication authority, PyInstaller collection/onedir proof, release owner, approver, and retained evidence per D-10. Design `tests/test_signing_adr_contract.py` as a passing state-machine/static contract: while ADR status is `pending` or `proposed`, assert each required unresolved field is visibly pending, no verifier/dependency is added, and the Phase 2 gate remains closed. Do not import, emulate, select, or install `cryptography` or any verifier. Preserve unrelated comments/docstrings and protected user/original-video data; no Phase 2 repair implementation per D-10/D-11/D-14.</action><verify><automated>pytest tests/test_signing_adr_contract.py -q</automated></verify><done>Approval-ready ADR and a green pending-state contract exist without a verifier dependency, and the Phase 2 gate remains closed.</done></task>
<task type="checkpoint:human-verify" gate="blocking"><name>Task 2: Obtain named-human approval of every signing decision</name><files>docs/DECISIONS.md, tests/test_signing_adr_contract.py</files><action>Pause at this blocking approval boundary. The pending branch must continue to pass its raw pytest contract while the named human supplies every selected value and owner. Preserve unrelated comments/docstrings and protected user/original-video data; do not install a verifier or perform Phase 2 repair work before the named human supplies every selected value and owner per D-10/D-11/D-14.</action><what-built>Approval-ready ADR and passing pending-state static contract; no verifier dependency and a closed Phase 2 gate.</what-built><how-to-verify>1. Run `pytest tests/test_signing_adr_contract.py -q` and confirm the pending-state branch passes while identifying all unresolved fields and closed Phase 2 gate. 2. Review and choose every required ADR field. 3. Name the approver, signing/release owner, key custodian, backup/rotation/revocation authority, and incident path. 4. Confirm Phase 2 stays blocked pending the post-checkpoint contract task.</how-to-verify><resume-signal>Provide approved exact values and named owners, or reject with changes.</resume-signal><verify><automated>pytest tests/test_signing_adr_contract.py -q</automated></verify><done>Named human approval provides exact contract values; until Task 3 passes, the Phase 2 gate remains closed.</done></task>
<task type="auto" tdd="true"><name>Task 3: Record approved values and make post-approval vectors concrete</name><files>docs/DECISIONS.md, tests/test_signing_adr_contract.py</files><behavior>- ADR status becomes approved and records exact selected verifier/version, approver, accountable signing/release/key-lifecycle owners, encoding, canonical manifest contract, versioned asset contract, PyInstaller proof, and retained evidence.
- The approved branch’s known-good vector passes and altered-byte vector rejects under the selected contract.
- The contract opens the Phase 2 gate only when every approved field and both vectors are concrete and passing; no verifier dependency is installed unless separately authorized.</behavior><action>Only after Task 2 approval, replace pending ADR fields in `docs/DECISIONS.md` with exact selected values, named approver and accountable owners. Update the state-machine/static contract’s approved branch to require approved status, selected verifier/version, algorithm and encoding, canonical manifest schema/bytes, exact-version asset contract, public-key embedding, custody/backup/rotation/revocation/incident authority, PyInstaller collection/onedir proof, release owner, approver, retained evidence, and concrete known-good/altered-byte vectors. Use existing approved capabilities only and do not add/install a verifier dependency unless separately authorized. Run the approved branch raw pytest and then full pytest; if either fails, record the blocker and keep the Phase 2 gate closed. Preserve unrelated comments/docstrings and never alter original video/user data/portable payload/main worktree per D-10/D-11/D-13/D-14.</action><verify><automated>pytest tests/test_signing_adr_contract.py -q; pytest -q</automated></verify><done>The approved branch’s static contract and vectors pass, and only then may the Phase 2 gate open.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| release signer to later repair verifier | future repair authorization depends on the approved signed-manifest contract |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-06 | Spoofing | signer/verifier contract | high | mitigate | ASVS-L1 V4/V6 named-human ADR approval plus concrete known-good vector gate; Phase 2 remains blocked on failure. |
| T-01-07 | Tampering | canonical manifest | high | mitigate | ASVS-L1 V5/V6 approved canonical bytes/schema/versioned asset fields and altered-byte rejection test. |
| T-01-08 | Repudiation | signing operations | high | mitigate | ASVS-L1 V6 ADR names custodian, release owner, approver, rotation/revocation and retained evidence; test/doc gate proves record exists. |
</threat_model>
<verification>Tasks 1 and 2 run raw passing pytest against the pending branch. Task 3 runs raw passing pytest against the approved branch, then full pytest. No expected-nonzero test workflow is permitted. No Phase 2 execution is authorized until Task 3 passes.</verification>
<success_criteria>RUNT-09 is concretely approved and testable, without prematurely installing a verifier.</success_criteria>
<output>Create .planning/phases/01-runtime-contract-bootstrap/01-04-SUMMARY.md when done</output>
