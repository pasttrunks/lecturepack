---
phase: 03-empty-launch-guided-demo
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - app/ui/index.html
  - app/ui/app.css
  - app/ui/app.js
  - app/ui/bridge.js
  - tests/test_guided_tour.py
autonomous: true
requirements:
  - DEMO-01
  - DEMO-02
  - DEMO-03
  - DEMO-04
  - DEMO-06
must_haves:
  truths:
    - "First launch presents a non-blocking prompt offering Start guided demo and Skip for now."
    - "Tour spotlight renders dark scrim with orange structural border cutout on target elements."
    - "Tour step cards include Next/Back, Exit demo, and keyboard arrow controls."
    - "Settings screen provides a Replay guided tour action."
  artifacts:
    - path: "tests/test_guided_tour.py"
      provides: "Automated verification for tour state machine, DOM spotlighting, and Settings replay"
---

<objective>
Implement the guided tour overlay UI, spotlight element highlighting, 4-step tour progression, and Settings replay action.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build guided tour spotlight overlay, step navigation, and Settings replay button</name>
  <files>app/ui/index.html, app/ui/app.css, app/ui/app.js, app/ui/bridge.js, tests/test_guided_tour.py</files>
  <action>
    Add `.lp-tour-overlay` markup and CSS in `app/ui/index.html` and `app/ui/app.css` using dark scrim, orange `#FF7A00` border glow, and smooth Studio transitions.
    Implement tour state machine in `app/ui/app.js`: Step 1 (Empty Home & New Job), Step 2 (Process view), Step 3 (Review view), Step 4 (Study & Exports).
    Add Next, Back, keyboard arrow listeners, and persistent Exit demo action.
    Add "Replay guided tour" button under Settings in `index.html` and wire handler to trigger tour restart.
    Add tests in `tests/test_guided_tour.py` verifying tour initialization, step navigation, spotlight positioning, exit cleanup, and Settings replay.
  </action>
  <verify>
    <automated>python -m pytest tests/test_guided_tour.py -q</automated>
  </verify>
  <done>Guided tour spotlight overlay, 4-step tour flow, Exit action, and Settings replay button are verified.</done>
</task>
</tasks>
