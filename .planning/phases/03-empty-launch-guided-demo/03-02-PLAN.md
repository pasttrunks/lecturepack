---
phase: 03-empty-launch-guided-demo
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - app/assets/demo/demo_lecture.mp4
  - app/desktop/paths.py
  - app/desktop/bridge.py
  - app/desktop/engine_adapter.py
  - tests/test_demo_session_isolation.py
autonomous: true
requirements:
  - DEMO-05
  - DEMO-07
  - DEMO-08
must_haves:
  truths:
    - "LecturePack bundles a 60-second rights-clear synthetic CS lecture video under app/assets/demo/demo_lecture.mp4."
    - "Demo processing executes inside an isolated temp directory under %TEMP%\\LecturePack\\demo_<session_id>."
    - "Idempotent demo cleanup sweeps leftover demo temp directories on exit, tour finish, and startup."
  artifacts:
    - path: "tests/test_demo_session_isolation.py"
      provides: "Automated verification for demo workspace isolation and temp directory sweep"
---

<objective>
Package the synthetic demo lecture asset, create the isolated demo session workspace manager, and implement idempotent temp directory cleanup.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Bundle demo media asset and implement isolated temp session manager with sweep</name>
  <files>app/assets/demo/demo_lecture.mp4, app/desktop/paths.py, app/desktop/bridge.py, app/desktop/engine_adapter.py, tests/test_demo_session_isolation.py</files>
  <action>
    Create synthetic demo asset bundle under `app/assets/demo/demo_lecture.mp4`.
    Add `DemoSessionManager` in `app/desktop/paths.py` creating `%TEMP%\LecturePack\demo_<session_id>` workspace and providing a `sweep_demo_sessions()` cleanup function.
    Hook `sweep_demo_sessions()` into application startup and shutdown.
    Update `Backend.start_demo_job()` to route processing through the demo session workspace without mutating persistent user settings or library state.
    Add unit tests in `tests/test_demo_session_isolation.py` proving temp directory isolation, non-mutating job execution, and cleanup sweep.
  </action>
  <verify>
    <automated>python -m pytest tests/test_demo_session_isolation.py -q</automated>
  </verify>
  <done>Synthetic demo asset is packaged, demo sessions run isolated under temp, and abandoned demo folders are swept automatically.</done>
</task>
</tasks>
