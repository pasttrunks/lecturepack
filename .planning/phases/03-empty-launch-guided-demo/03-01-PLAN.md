---
phase: 03-empty-launch-guided-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/desktop/bridge.py
  - app/ui/app.js
  - app/ui/index.html
  - tests/test_empty_home.py
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
must_haves:
  truths:
    - "A healthy boot opens Home with no active job automatically opened."
    - "Existing jobs remain visible in the library side panel and activate only when explicitly clicked."
    - "Synthetic or temporary demo jobs are marked session-scoped and never written to library.json."
  artifacts:
    - path: "tests/test_empty_home.py"
      provides: "Automated verification for empty Home boot and library isolation"
---

<objective>
Implement empty Home startup, initial empty active job payload, and library isolation so healthy boots never auto-open the latest job or pollute library.json.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement empty Home boot signal and library isolation</name>
  <files>app/desktop/bridge.py, app/ui/app.js, app/ui/index.html, tests/test_empty_home.py</files>
  <action>
    Modify `Backend.on_ui_ready` in `app/desktop/bridge.py` so that healthy boot initializes `active_job` with empty payload `{"id": "", "title": ""}`.
    Update `app/ui/app.js` to render the empty Home view when `active_job` has id `""`.
    Ensure `library.json` persistence skips jobs flagged as `session_scoped`.
    Add tests in `tests/test_empty_home.py` verifying empty boot, explicit job opening, and non-persistence of session-scoped jobs.
  </action>
  <verify>
    <automated>python -m pytest tests/test_empty_home.py -q</automated>
  </verify>
  <done>Healthy boot opens empty Home without auto-opening jobs, and library persistence ignores session-scoped jobs.</done>
</task>
</tasks>
