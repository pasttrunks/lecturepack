---
phase: 02-real-lecture-import-processing
plan: 04
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
files_modified: []
autonomous: false
requirements:
  - SC-04
  - SC-05
  - SC-06
  - WI-01
  - WI-07

must_haves:
  truths:
    - "Transcript Only completes on a normal local video in the packaged build (SC-04)"
    - "Slides Only completes on a normal local video in the packaged build (SC-05)"
    - "Study Pack completes on a normal local video in the packaged build (SC-06)"
    - "All three modes are tested by importing the bundled polar bears demo video through the normal Source/Browse path, not the demo button (D-10)"
    - "The packaged build is freshly built from current code including all Phase 1 + Phase 2 fixes (D-10)"
  artifacts: []
  key_links:
    - "scripts/release_build.py -> app/packaging/build.py -> dist/ onedir -> manual import -> start_processing(mode) -> pipeline completes"
---

<objective>
Build a fresh packaged app and run the end-to-end acceptance test: import the bundled polar
bears demo video through the normal Source/Browse path (not the demo button), then run all
three output modes (Transcript Only, Slides Only, Study Pack) to completion. This is the
phase's approval/evidence gate — the code-level fixes in Plans 01-03 prove the wiring is
correct, but only a real packaged run proves the pipeline actually finishes.

Purpose: The ROADMAP's Phase 2 approval gate explicitly requires "All three output modes
completing on a real local video in the packaged build." Plans 01-03 fix the code and add
regression tests, but those tests mock the pipeline. This plan closes the loop with physical
evidence.

Output: Evidence log confirming each mode completed, captured as part of 02-04-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-real-lecture-import-processing/02-CONTEXT.md
@.planning/phases/02-real-lecture-import-processing/02-01-SUMMARY.md
@.planning/phases/02-real-lecture-import-processing/02-02-SUMMARY.md
@.planning/phases/02-real-lecture-import-processing/02-03-SUMMARY.md

Build references:
@scripts/release_build.py
@app/packaging/build.py
</context>

<tasks>

<task type="human">
  <name>Task 1: Build fresh packaged app</name>
  <files>scripts/release_build.py</files>
  <action>
  Run scripts/release_build.py to produce a fresh onedir build in dist/. This creates (or
  refreshes) .venv-release, installs locked requirements, and runs build.py inside the clean
  venv. The packaged output flattens bin/Release/* into bin/*, populates smoke/, and includes
  all Phase 1 pruning + Phase 2 code fixes.

  After the build completes, verify the output directory exists and contains the expected
  runtime layout: bin/whisper-cli.exe, bin/ffmpeg.exe, bin/ffprobe.exe,
  models/ggml-base.en.bin, smoke/runtime-smoke.wav.
  </action>
  <verify>
    <human>Packaged build exists in dist/ with correct runtime layout</human>
  </verify>
  <done>Fresh packaged build produced from current code with all Phase 1 + Phase 2 fixes.</done>
</task>

<task type="human">
  <name>Task 2: Run all three output modes on polar bears video via normal import</name>
  <files></files>
  <action>
  Launch the freshly built packaged app from dist/. On a clean profile (delete or rename
  any existing LecturePackData directory to simulate a clean install):

  1. Wait for the first-run setup checklist to appear and show all items as Ready.
  2. Skip or complete the guided demo.
  3. Use Source > Browse to import the bundled polar bears demo video (10 seconds, 4 slides)
     through the NORMAL import path — do not use the demo button.
  4. Confirm: the video appears in Source, a thumbnail is generated, and the job appears in
     Recent with a valid status.
  5. Run Transcript Only mode. Wait for completion. Record whether it succeeds or fails.
  6. Import the same video again (or use the same job if retry is available).
  7. Run Slides Only mode. Wait for completion. Record whether it succeeds or fails.
  8. Import the same video again.
  9. Run Study Pack mode. Wait for completion. Record whether it succeeds or fails.

  For each mode, capture:
  - Whether start_processing reached the pipeline (no "Setup needed" error)
  - Whether the pipeline completed (no crash, no hang, output artifacts generated)
  - The job's final lifecycle state (COMPLETE expected)
  - Any error messages in the log

  Also verify:
  - The top-left indicator reflects the active job during processing (SC-09)
  - Failed jobs (if any) stay visible with their error (D-06)
  - Slide sensitivity buttons are disabled during processing (D-08)
  - Demo still works after all normal processing runs (SC-01)
  </action>
  <verify>
    <human>All three output modes complete on the polar bears video imported through the normal path. Evidence captured in SUMMARY.</human>
  </verify>
  <done>Transcript Only, Slides Only, and Study Pack all complete on a normal local video in
  the packaged build. Phase 2 approval/evidence gate satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — this plan runs existing code in the existing packaged environment.

## STRIDE Threat Register

No new threats — acceptance testing only, no code changes.
</threat_model>

<verification>
1. Packaged build produced from current code
2. All three output modes complete on polar bears video via normal import
3. Demo still works (SC-01)
4. Job lifecycle visible throughout (SC-03)
5. Sensitivity lock active during processing (D-08)
</verification>

<success_criteria>
- Fresh packaged build produced including all Phase 1 + Phase 2 fixes
- Transcript Only completes on a normal local video in the packaged build
- Slides Only completes on a normal local video in the packaged build
- Study Pack completes on a normal local video in the packaged build
- The bundled polar bears demo video is used, imported through Source/Browse (not demo button)
- Phase 2 approval/evidence gate fully satisfied
</success_criteria>

<output>
Create `.planning/phases/02-real-lecture-import-processing/02-04-SUMMARY.md` when done
</output>
