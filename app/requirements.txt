# LecturePack desktop shell + engine runtime dependencies.
# The shell needs PySide6 with the WebEngine/WebChannel add-ons (shipped in the
# standard PySide6 wheel). The engine deps below mirror the repo root
# requirements.txt so `app/` can be installed on its own.

# Desktop shell (WebEngine + WebChannel come with the PySide6 addons wheel)
PySide6>=6.7.0

# Engine
opencv-python-headless>=4.8.0
scikit-image>=0.22.0
Pillow>=10.0.0
imagehash>=4.3.0
img2pdf>=0.5.0
ReportLab>=4.0.0
Jinja2>=3.1.0
