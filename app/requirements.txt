# LecturePack desktop shell + engine runtime dependencies.
# The shell needs PySide6 with the WebEngine/WebChannel add-ons (shipped in the
# standard PySide6 wheel). The engine deps below mirror the repo root
# requirements.txt so `app/` can be installed on its own.

# Desktop shell (WebEngine + WebChannel come with the PySide6 addons wheel)
PySide6>=6.7.0

# Engine
opencv-python-headless>=4.8.0
scikit-image>=0.22.0
Pillow>=10.0.0
imagehash>=4.3.0
img2pdf>=0.5.0
ReportLab>=4.0.0
Jinja2>=3.1.0
cryptography==49.0.0  # Approved AD-19 Ed25519 verifier; Windows x64 wheel hash recorded in AD-19.

# Runtime requirements that must ship in packaged builds. Root requirements.txt
# declared these; app/requirements.txt (the one CI installs) was missing them.
Send2Trash>=1.8.0  # engine_adapter.py deletes fall back to hard rm without this (BUG-14 regression)
tzdata>=2024.1     # zoneinfo on Windows (job_queue.py) — silent fallback to system-local without it
yt-dlp>=2025.1.15  # "Import from a link" — button hidden at runtime if absent
