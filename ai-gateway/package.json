{
  "name": "lecturepack-ai-gateway",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Privacy-minimal Cloudflare Worker gateway for LecturePack Study AI.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/index.js && node --check src/auth.js && node --check src/tasks.js && node --check src/providers.js && node --check src/storage.js"
  }
}
