{
  "name": "lecturepack",
  "productName": "LecturePack",
  "version": "2.0.2",
  "private": true,
  "description": "LecturePack \u2014 local lecture transcription, slides, and study packs.",
  "main": "production-main.js",
  "scripts": {
    "start": "electron .",
    "validate": "node --check production-main.js && node --check production-preload.js && node --check electron-bridge.js && node --check updater.js && node --check ../app/ui/app.js && node --check package-sidecar.mjs && node --check package-win.mjs",
    "package:sidecar": "node package-sidecar.mjs",
    "package:win": "npm run package:sidecar && node package-win.mjs"
  },
  "devDependencies": {
    "@electron/packager": "20.0.4",
    "electron": "43.2.0"
  }
}
